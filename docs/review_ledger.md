# Review ledger

Findings from the review agents (`research-director`, `falsifier`,
`acoustics-reviewer`, `readability-reviewer`). Holds ONLY unresolved findings —
working memory for the loop, not an audit log. One row per finding:
`ID | agent | severity | status | anchor | finding | resolution`.

Status is exactly one of two values: **OPEN** (not yet resolved) or **DEFERRED**
(intentionally out of scope for the current gate, with a one-line reason and the
gate it belongs to). There is no ADDRESSED/RESOLVED status — the moment a
finding is fixed and re-review-confirmed clean, its row is deleted.

**Recovering a deleted row.** Code and tests cite row ids (`F-49`, `AC-17`,
`RD-45`) as their traceability device, and a confirmed-fixed row is DELETED from
this file — so a cited id that is not in the table below is not an error, it is a
row that was closed. Recover it from git, which is the audit trail (RR-25):

```
git log -S 'RD-49' -p -- docs/review_ledger.md
```

`-p` matters: without it the command prints commit subjects and you still have to
run `git show <sha>:docs/review_ledger.md` to see the row itself.

**Exception — ids that predate this file.** The ledger entered git history after the
first review rounds, so these 16 cited ids have NO ledger commit and the command
above returns nothing for them:

`AC-04 AC-05 AC-06 · F-02 F-04 F-05 F-07 F-08 F-09 F-12 F-13 F-14 F-16 · RD-02
RD-05 · RR-17`

For those, the citing comment in the code is the primary record — each was written
to explain the finding it cites, not merely to point at it — so

```
grep -rn 'F-09' src/ tests/ configs/
```

recovers the substance. `configs/` belongs in that list: RD-05's only citation in
the whole repo is in `configs/base.yaml`.

**DURABLE — the cycle-4 lane-id remap.** Four lanes worked cycle 4 in parallel, each
allocated row ids by reading this file's max, and collided four ways (RD-93…RD-100 was
allocated by ALL FOUR for four different findings). File ownership disambiguated the
citations, and the source tree was remapped in `b5af7b9`. **The four inboxes
`docs/ledger_inbox/{M,P,S,R}.md` were deliberately NOT renumbered** — they are the
lanes' own words and hold the substance (every measurement, every probe). So a row
below cites its inbox and its OLD id, and this table is how you get from one to the
other. It stays here for the same reason the 16-id exception list does: it must outlive
every old-id citation in commit messages and on the four lane branches.

| lane | class | as written in the inbox | as filed here |
|---|---|---|---|
| M | all (`RD-93…100`, `F-M*`, `AC-*-R*`, `*-B*`) | — | **unchanged** — M used scoped suffixes and collided with nothing |
| P | research-director | `RD-93…RD-102` | `RD-101…RD-110` |
| P | falsifier / acoustics / readability | `F-75…80`, `AC-44…48`, `RR-46…59` | **unchanged** |
| S | research-director | `RD-93, 94, 97` | `RD-111, RD-112, RD-113` |
| S | falsifier | `S-F*`, `S-1`, `S-2` | **unchanged** |
| S | acoustics | `AC-45…AC-49` | `AC-49…AC-53` |
| S | readability | `RR-46,47,48,50,51,52,53,54,55` | `RR-60…RR-68` |
| R | research-director | `RD-93…RD-104` | `RD-114…RD-125` |
| R | falsifier (**misfiled as `RD-`**) | `RD-105…RD-122` | `F-81…F-98` |
| R | acoustics | `AC-40…AC-49` | `AC-54…AC-63` |
| R | readability | `RR-28…RR-38` | `RR-69…RR-79` |

Note the two classes of genuine hazard this remap defused, both of which a blanket
find/replace would have caused: lane R's `AC-40…AC-43` and `RR-28…RR-38` collided with
**live OPEN rows**, and `dry_run.py:136` (lane M) cites the LIVE `AC-43` while
`gsound_sir.py:61` (lane R) cited R's NEW one — only the latter moved.

**Verified at the close of cycle 3 (2026-08-10, HEAD `ffe87b6`):** all **150**
distinct ids cited anywhere in `src/`, `tests/`, `configs/`, `README.md`,
`CLAUDE.md` and `docs/design_spec.md` are either a live row above, one of the 16
exceptions, or recoverable by `git log -S`. Re-run this sweep when closing a cycle
— citations have twice outrun the ledger (RR-40), most recently RD-75/RD-78/RD-79,
which this cycle acted on before writing them down.

## OPEN findings

### research-director

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| RD-18 | research-director | major | OPEN | plan Step 4 (QC) vs docs/research_I_paper.md:480,503; docs/design_spec.md:217 | v3 implements only 2 of Research I's 4 QC criteria, and `base.yaml` min_energy_db −60.0 is ~40 dB stricter than RI's 1e-10 (~−100 dB re 1.0). Missing: non-empty retained-path file, max retained-path file 128 MB (declared fixed at design_spec:217 but ABSENT from config = hidden default). QC governs dataset admission and E1 is "reproduce the old null", so a silently different admission rule confounds the reproduction. The onset check is a PAIRED low-vs-high mismatch (RI l.480), not an absolute bound. | PARTIALLY ACTIONABLE NOW (research-director pass 4): the two missing CONFIG KEYS — `max_path_file_mb` and a declared energy reference (dB re 1.0 FS) — are declarable today, cheaply, and should not wait for Step 4. The criterion implementations and the `max_onset_ms` → `onset_mismatch_tolerance_ms` rename belong to Step 4. Update design_spec l.217 + §6 QC-record row, not just the base.yaml comment. |
| RD-33a | research-director | major | OPEN | gate definition; src/amcd/simulators/gsound_sir.py (`render` raises NotImplementedError) | **THE DATASET-RENDER GATE.** No 720-scene real gsound render until both conditions below hold. Replaces RD-33, whose condition had drifted to "zero OPEN rows across the whole codebase" — unreachable (reviewer finding-rate has matched closure rate for three cycles) and not the binding constraint anyway. | **(i) REGRESSED IN CYCLE 4 — 59 OPEN rows now sit on the path list (4 blocker, 16 major), and its reachability is a PENDING USER DECISION: see RD-128 and the cycle-4 integration record (RD-145). Do not cite this row as stable.** **OPERATIONALIZED so it cannot drift again (RD-76).** Lifts when BOTH: (i) zero OPEN rows anchored in this EXPLICIT PATH LIST — `src/amcd/scenes/**`, `src/amcd/evaluation/**`, `src/amcd/config.py` split handling, `configs/*.yaml` split declarations — not the free text "the metric path"; AND (ii) the RD-17 probe has validated the high leg against a config-declared convergence tolerance on per-band energy / T30 / C50 through the existing ISO path. **User delegated this decision 2026-08-10** ("highest scientific accuracy; defer what cannot be done yet"): sequencing the ≤4-scene probe before the 720-scene emulated render is both the higher-accuracy order and the cheaper one, because every paired-improvement number already treats the 200,000-ray leg as converged and nothing has checked it. |
| RD-65 | research-director | minor | OPEN | src/amcd/scenes/generator.py `_disclose_and_gate_record_length` | The AC-22/RD-56 gate is the OVERALL over-limit fraction — the one aggregation invariant #9 forbids for results. research_i's 0.01 over 720 scenes permits 7 over-limit scenes that could ALL sit in the 30-scene `test_geometry_shift` (23 % of that split) and still pass, while the per-shift breakdown IS the research result. Per-split counts reach `placement_report.json` but surface only when the gate trips. | Keep the overall gate (its reasoning is right — a per-split gate lets the smallest split set the tolerance for train). ADD an always-emitted per-split WARNING naming any split whose OWN over-limit fraction exceeds the declared limit, and carry per-split counts into the E1 report table, not only the JSON. |
| RD-66 | research-director | minor | OPEN | src/amcd/pipeline.py `_code_version` | RD-59's fix is blind to its own trigger workflow: `git rev-parse HEAD` does not see UNCOMMITTED edits, and this project's loop is edit → run → review → commit. **Independently reproduced by falsifier as F-55 — see that row, which carries the stronger evidence and the wheel-install/foreign-repo cases.** | **MERGED INTO F-55 and fixed there** (content hash over declared per-stage source scopes). Close both together. |
| RD-67 | research-director | minor | OPEN | src/amcd/simulators/gsound_sir.py — LANE R, cycle 4 (was DEFERRED at Step 3; cycle 4 IS Step 3, RD-90) | Consolidates the Step-3 provenance-fill residues of RD-16 / RD-19 / RD-21 / RD-12 (all otherwise confirmed fixed) into one gate-anchored row, so five rows do not linger for one obligation. | At Step 3 the gsound leg's `IRResult.meta` must carry: INSTALLED upstream sha verified == the pinned `commit_sha`; the diffuse AND specular counts for that leg; `ambisonic_convention: "acn_n3d"` (AC-15's stamp); native IR length + `truncated` bool + discarded-tail energy in dB (RD-21) against a config-declared QC threshold; and a `PathData.speeds_of_sound` cross-check against the declared `speed_of_sound_m_s` (RD-19's free empirical check). |
| RD-08 | research-director | minor | OPEN | src/amcd/simulators/base.py (IRResult) vs design_spec §8 — LANE R, cycle 4 (was DEFERRED at Step 2, RD-90) | Spec §8 shows IRResult carrying `paths: PathData`; code omits it. Producer half of the path-conditioned-variant seam. | Lands with the gsound_sir build: path export defines PathData and populates `IRResult.paths`. Do NOT add a speculative empty field before the producer exists. Gate: Step 2. |
| RD-21 | research-director | minor | OPEN | src/amcd/simulators/gsound_sir.py IR truncation disclosure — LANE R, cycle 4 (was DEFERRED at Step 3, RD-90) | Trimming gsound's natural IR to `ir_duration` discards tail energy and can silently invalidate T30/EDT for the most reverberant scenes. | Record per (scene, leg) in canonical meta: native length, `truncated` bool, discarded tail energy in dB; QC flags above a config-declared threshold. Tracked jointly by RD-67. |
| RD-24 | research-director | minor | OPEN | src/amcd/simulators/base.py, gsound_sir.py PathData schema — LANE R, cycle 4 (was DEFERRED at Step 2, RD-90) | Schema pinned to gsound's exact keys, incl. `intensities (N,8)` whose band meaning lives only in the simulator config. A path file from a second raytracer would be uninterpretable without its config. | Make the parquet self-describing: store band edges/centres, num_bands, simulator name and commit sha in the file's own metadata. |
| RD-20 | research-director | minor | OPEN | src/amcd/pipeline.py, runtime.py, cli.py stage dispatch — INTEGRATOR QUEUE, cycle 4 (was DEFERRED at Step 3; cycle 4 IS Step 3, RD-90) | Widening stage dispatch for a runtime-only value is a nine-stage-plus-tests touch; the roadmap makes a third foreseeable. | Introduce one frozen `RunContext(verbosity, host)` in runtime.py; dispatch as `(config, run_dir, ctx)`. Do it while the dispatch signature is being touched for the worker anyway. |
| RD-69 | research-director | minor | OPEN | docs/design_spec.md §7 fixed-parameter table vs configs/base.yaml | The §7 table still declares `ir_duration 3.0s` as a fixed parameter of the study, while base.yaml now declares 4.25 s (research_i keeps the RI-pinned 3.0). CLAUDE.md requires every plan to cite design_spec sections, so a fixed-parameter table that disagrees with the reference config is a wrong authority for the next plan. | Update the §7 table to state ir_duration per config (base 4.25 s, research_i 3.0 s RI-pinned) with the AC-22 reason, as base.yaml already explains it. |

### falsifier

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| F-60 | falsifier | minor | OPEN | src/amcd/scenes/generator.py (AC-22 "realized" gate) | The gate is called "realized" but gates realized DRAWS OF AN ESTIMATE, never the rendered IR. `t60_exceeds_ir_duration` is Sabine from geometry × a scalar α; nothing compares a MEASURED decay against `ir_duration`. Two ways a truncated scene passes: (a) Sabine assumes a 4V/S mean free path and under-predicts decay in a non-diffuse geometry — base/RI's corridor family (15-30 m × 1.5-3 m) is exactly that, and also the family with the longest records; (b) the scaffold clips rt60 to [0.05, 3.0], so under dry_run the described and rendered decays already differ (recorded as `rt60_clipped`, which the gate never consults). Distinct from RD-21: this is the ESTIMATOR being the wrong instrument. | Keep the design-time gate; add the cheap realized check eval can already supply — flag any scene whose fitted T30 exceeds a config-declared fraction of `ir_duration` and count it in the eval output. Test: a scene whose RENDERED T30 exceeds the record must appear in a count, not only in a pre-render estimate. |
| F-63 | falsifier | major | OPEN | src/amcd/pipeline.py `_stats_fingerprint`; `report: None` | **THE REPORTED CIs ARE STILL REACHABLE THROUGH A CACHED STAGE.** `stats` carries no `code_version` and `report` no fingerprint at all, so a change to the code that COMPUTES the reported numbers is invisible. REPRODUCED: `bootstrap_ci`'s `ci_lower` × 0.001 in a scratch package copy → `amcd all` printed `[skip]` for ALL NINE stages, exit 0, and `ci_table.csv` is the old code's output. The chain does not save it: `stats` chains to `eval`, whose scope is ("evaluation","representations") — `stats` is in no stage's scope and `reporting` in none either. **The cycle's claim "the stage cache now protects the reported result" holds for train/infer/eval and FAILS for the two stages that produce the reported table.** | Add `code_version("stats")` with scope ("stats","evaluation") — stats imports `evaluation.metric_row.paired_improvement` — and give `report` a fingerprint over {report_format, code_version(("reporting",))}. RD-41's "terminal, only its own re-use" rationale is WRONG for `report`: its own re-use IS `report/summary.txt`. Test: patching `stats/aggregate.py` must refuse the cached stats. |
| F-64 | falsifier | major | OPEN | src/amcd/pipeline.py `_preprocess_fingerprint` vs src/amcd/data/preprocess.py | **PREPROCESS DECLARES NO `code_version`, SO THE ENCODED DATASET, THE NORMALIZATION STATS AND THE SPLIT ASSIGNMENT ARE SERVED UNDER CHANGED CODE — AND THE ERROR NAMES THE WRONG STAGE.** REPRODUCED end to end: patched `spectrogram.encode` (power × 4); `amcd all` printed `[skip] preprocess (cached)` and only `train` refused, saying "Re-run with --force to rebuild 'train'". Doing exactly what the message says completed with exit 0 and a full report while `preprocessed/train/scene_0000_high.pt` stayed BIT-IDENTICAL to the pre-change tensor — the reported metrics derive from an encoding no stage in the run produced. Same hole covers `data/splits.py` (the leakage-critical split assignment) and `normalization.compute_stats`. | Give preprocess `code_version` with scope ("data","representations"). Until then the train-refusal message is actively misleading. Test: patching `spectrogram.encode` must refuse the cached PREPROCESS, not the cached train. |
| F-65 | falsifier | major | OPEN | src/amcd/pipeline.py `_eval_fingerprint` vs src/amcd/evaluation/evaluator.py | **THE CONFIG KEY THIS CYCLE ADDED IS IN NO FINGERPRINT, SO ITS DISCLOSURE IS SERVED STALE.** `metric_edt_variance_limited_s` governs `estimator_variance_limited` → `ci_table.n_estimator_variance_limited` → summary.txt's Caveats, and appears in NO stage fingerprint (probe over every `Config` field vs every fingerprint payload). REPRODUCED: 0.15 → 5.0 on a complete run_dir → all nine stages `[skip]`, exit 0, `config.yaml` re-stamped with 5.0, `ci_table.csv` still reads the old counts; forcing eval+stats gives 3/3/2/3. **A reported disclosure column under the wrong threshold's provenance stamp — the exact F-53 failure mode, reintroduced by a key I added to fix RD-78.** | Add it to `_eval_fingerprint`. Then add the guard the CLASS needs: a test asserting every `Config` field is either in some stage fingerprint or in an explicitly declared exempt list, so the next added key cannot repeat this. |
| F-66 | falsifier | major | OPEN | src/amcd/pipeline.py `STAGE_CODE_SCOPE`; src/amcd/provenance.py `code_version` docstring | **TWO DECLARED SCOPES OMIT A REAL DEPENDENCY, AND THE DOCSTRING OVERSTATES THE TEST THAT SHOULD CATCH IT.** `eval` and `infer` both call `data.normalization.denormalize` on EVERY reported leg but neither declares `data`. MEASURED: patching `denormalize` (+10 dB) leaves `_code_version("eval")` and `_code_version("infer")` bit-identical while train's moves. Masked TODAY only because `data` is in train's scope and the chain refuses upstream first — a coincidence, not a guarantee. Separately, `provenance.code_version` claims "tests/test_stage_cache.py asserts each declared scope against the modules the stage actually imports"; the test asserts only that the stage's OWN entry-point subpackage is in scope. **This is exactly the risk I flagged when I chose scoping over a whole-package hash, and the mitigation I claimed does not exist.** | Add "data" to eval's and infer's scope. Replace the docstring claim with what is true, and make the test real: assert the declared scope is a superset of the stage's transitive `amcd.*` import closure minus `_CORE_SOURCES`. |
| F-68 | falsifier | minor | OPEN | src/amcd/evaluation/room_acoustic.py `_butter_octave_filter` KNOWN RESIDUAL paragraph | The documented contract describes behaviour the module NO LONGER HAS, and asserts something false. It still reads "stripping the guard discards its pre-ringing … it is common-mode across legs, and paired improvements are unaffected", while `_band_energy` now folds both ends back in. The "paired improvements are unaffected" half was independently FALSE before the fold too: measured on the pre-AC-36 artifacts, pred discarded systematically LESS than high in 24/24 (scene, band) cells (mean 1.7-1.9 pp), shifting paired pred−high C50 by +0.111 dB — 19 % of the improvement the run reported. | Rewrite to describe the fold, and state the leg asymmetry as measured rather than asserting its absence. Overlaps RR-38. |
| F-69 | falsifier | minor | OPEN | src/amcd/provenance.py `code_version` | **THE CACHE KEY IS HOST-DEPENDENT: it hashes macOS AppleDouble `._*.py` sidecars.** 46 sit under `src/amcd/` on this exFAT volume and match `rglob("*.py")`. MEASURED: train's scope hashes differently with and without them. On the project's declared second host (native x86_64), or any clone to APFS, the same source yields a different key, so a run_dir carried between hosts is refused with an opaque `code_version: <sha> → <sha>` diff naming no leaf — and the only remedy offered is `--force`, precisely the compliance failure the scoping rationale invokes as its own justification. The project already filters `._` elsewhere (`evaluator.py`); this glob does not. | Filter `._`-prefixed and `__pycache__` paths, or hash only git-tracked files. Test: injecting a `._probe.py` sidecar must not change any stage's `code_version`. |
| F-70 | falsifier | minor | OPEN | src/amcd/evaluation/room_acoustic.py `compute_room_acoustic_metrics` | **AC-25 TRADED A GROUND-TRUTH CONTAMINATION FOR A SELECTION ON THE DEPENDENT VARIABLE, AND ONLY THE FIRST IS DISCUSSED.** A pred NaN in ONE physically-kept band makes the metric NaN for pred in EVERY band, so that scene contributes nothing to `paired_improvement` — the reported improvement CI is conditioned on the model having produced a measurable value, i.e. model failures are removed from the estimate of how much the model helps. COUNTED (`n_pred_band_unresolved`) but not BOUNDED, and the direction is optimistic. Inert under dry_run today; live exactly where AC-37 says the decode floor first bites. | Keep the rule — it is the right fix for the contamination. ADD the bound: report the paired improvement over the full attempted population with pred-unscored scenes imputed at zero improvement, alongside the scored-population CI. Test: a run with one pred-unresolved scene must show both. |
| F-71 | falsifier | minor | OPEN | src/amcd/scenes/generator.py (`characterization: none` branch vs the AC-22 gate) | **THE RD-64 SEAM DILUTES THE RECORD-LENGTH GATE AND PRINTS A NUMBER FOR AN UNDEFINED QUANTITY.** REPRODUCED with a declared `openfield` family: `diffuse_field_validity` correctly reports `n_scenes: 0, n_uncharacterized: 3`, but the sibling `t60_over_ir_duration` block reports `n_scenes: 3, count: 0, fraction: 0.0` — the uncharacterized scenes enter the gate's denominator as PASSING, because the branch sets the flag `False` rather than omitting it. N uncharacterized scenes shrink the overall over-limit fraction by N/(N+M), so a dataset whose enclosed scenes breach the limit can pass by adding non-enclosures. Violates "never render an unscored quantity as a number" in the same helper that honours it 20 lines above. | Omit `t60_exceeds_ir_duration` for uncharacterized scenes so `_scene_is_characterized` excludes them from BOTH the fraction and the gate denominator. Test: a split of `characterization: none` scenes must report `n_scenes: 0, n_uncharacterized: N` and must not change the gate ratio. |
| F-72 | falsifier | minor | OPEN | src/amcd/diagnostics/probe.py (D0a and D0b per-scene skips) | **F-45'S DISCIPLINE STOPPED AT THE SPLIT ENUMERATION; THE PER-SCENE DROPS ARE STILL SILENT.** Both probes `continue` past a scene whose tensors or `renders/<id>/high.npy` are missing with no `(unit, reason)` logged, and both then `continue` past a split whose scenes ALL failed — so that split vanishes from the artifact entirely, the exact state F-45 closed on a different branch, and D0b's `all_clear` stays True over a split it never measured. The surviving counts are SCORED-only with no attempted denominator, so a split where 5 of 20 scenes lacked tensors reports headroom over the survivors and reads as complete. | Mirror the eval drop log: accumulate `(scene, reason)` per split, report `n_scored`/`n_attempted`, and give the all-failed split an `unscored_reason` so it reaches the verdict table as INDETERMINATE. |
| F-73 | falsifier | minor | OPEN | src/amcd/config.py `_CONFIGS_DIR` | **PACKAGE CODE ASSUMES A SOURCE-CHECKOUT LAYOUT, WHICH THE PROJECT'S OWN PROVENANCE DOCSTRING SIMULTANEOUSLY ASSUMES AWAY.** `configs/` is resolved three levels up from the module, so a wheel install into site-packages — the exact case `provenance.code_version` argues about — cannot find `base.yaml`, and `Config.load` auto-prepends `_BASE_YAML` unconditionally. Observed: `FileNotFoundError: .../configs/base.yaml`. Not platform-keyed, so not a portability BRANCH, but the same class: a host-layout assumption in package code. | Ship `configs/` as package data (`importlib.resources`) or make the config root a required input with a clear error. Test: importing `amcd.config` with no sibling `configs/` must fail actionably, not with a bare FileNotFoundError deep in `_merge_yaml`. |
| F-74 | falsifier | minor | OPEN | src/amcd/training/trainer.py; src/amcd/training/infer.py; src/amcd/config.py `stamp` | **THE COMPUTE DEVICE IS AUTO-SELECTED, NEVER RECORDED, AND NEVER FINGERPRINTED.** `_select_device()` (duplicated verbatim in two modules) falls back MPS → CUDA → CPU correctly, but the chosen device is only `emit`ed at show level; `versions.json` records six package versions, the git sha, the dirty flag and `code_version` — and no device or host. Under the project's own two-host requirement the SAME config and the SAME `code_version` produce different weights on this Mac (MPS) and on the x86 box (CUDA/CPU), with identical provenance stamps. | Stamp `device` and `platform.machine()` into `versions.json` — HUMAN provenance, not a cache key, since a device change must not invalidate an expensive artifact. Test: `versions.json` names the device the checkpoint was trained on. |

### acoustics-reviewer

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| AC-09 | acoustics-reviewer | major | DEFERRED (gate: E1 report) | src/amcd/scenes/generator.py (placement_report.json) vs RD-29 | CODE FIX RE-REVIEW-CONFIRMED (pass 3, re-confirmed pass 4): `placement_report.json` records volume, Sabine AND Eyring T60, r_c, d/r_c and DRR per split; formulas independently verified. Headline reproduced: train median DRR −5.75 dB vs test_material_shift +4.86 dB at near-identical median distance. | Code fix re-review-confirmed at passes 3 and 4; only the E1 REPORT DISCLOSURE remains, which cannot be written before the report exists. Re-statused from OPEN per RD-73 so the OPEN set means "actionable now". |
| AC-15 | acoustics-reviewer | minor | DEFERRED (gate: Step 3) | src/amcd/simulators/base.py — ANSWERS RD-25 | Upstream is ACN ordering with **N3D**, not SN3D (binding.cpp:18, :43). Docstring half re-review-confirmed at passes 3 and 4; blast radius still nil (`spatial.py` returns `{}, {}`; every live scalar metric reads `ir[0]`). | Docstring half re-review-confirmed at passes 3 and 4; blast radius nil. The VALUE can only be stamped once `GsoundSirSimulator.render` exists — tracked jointly by RD-67, same gate. Re-statused from OPEN per RD-73. |
| AC-16 | acoustics-reviewer | minor | DEFERRED (gate: E2 write-up) | src/amcd/representations/spectrogram.py vs configs/simulators/gsound_sir.yaml | The representation resolves 27 third-octave bands over a simulation with only 8 octave bands of spectral freedom: exactly 3 third-octaves per simulated band except the top band, which backs 6, and three bands sit inside the DC-88.7 Hz band. ~22 % of dimensions carry no independent simulated content. Arithmetic re-verified at production framing. | Record-only obligation: ~22 % of representation dimensions carry no independent simulated content. Nothing to implement; it is a disclosure the E2 write-up must carry. Re-statused from OPEN per RD-73. |
| AC-26 | acoustics-reviewer | major | OPEN | src/amcd/evaluation/room_acoustic.py (`min_measurable_t60_s`) — REPLACES AC-23 | The measurability floor is applied to the MEASURED value, so it censors the low tail of a noisy estimator and biases the SURVIVING per-split mean upward. MEASURED (200 realizations/point, 500+1000 Hz, noise-free): at true T60 = 0.06 s (research_i test_material_shift's Eyring median is 0.059 s) **57.5 % of realizations are suppressed and the survivors' mean reads 0.0632 s against a true-mean estimate of 0.0470 s — +34.5 % censoring bias**. At 0.05 s: 79.0 % suppressed, +53.3 %. At 0.04 s: 95.5 % suppressed, +86.7 %. This also refutes `configs/base.yaml`'s own claim that 0.05 s is "a degenerate-case guard, not an active suppression threshold" — for the split it was written for it suppresses the majority. Compounded by AC-25: the suppression is leg-wise and then intersected. | **PARTLY CLOSED, NOT CLOSED — see AC-38.** The THRESHOLD is now a per-(metric, band) constant and reproduces exactly; the acoustics-reviewer independently confirms the margin of 2.0 is well calibrated. But the DECISION is still `fitted_value < floor`, so the estimator's low tail is still censored — measured 18.8 % of T30 suppressed with +7.5 % survivor bias at true T60 = 0.04 s, and base.yaml's own declared support admits Eyring T60 = 0.0179 s, below the 500 Hz T30 floor. Superseded by **AC-38**, which carries the measurements and the fix. |
| AC-28 | acoustics-reviewer | major | OPEN | src/amcd/simulators/dry_run.py (`direct`) + the class docstring | **THE SCAFFOLD'S "DIRECT SOUND" IS NOT A DIRECT SOUND.** `direct = direct_gain * exp(-t/0.02)` is a one-pole envelope with a **7.96 Hz corner**, so only **6.06e-7** of its energy reaches the 500 Hz octave band and 3.52e-8 the 1000 Hz band. A physical direct arrival is a broadband impulse scaled by 1/d. MEASURED CONSEQUENCE (10×8×3.5 m, α 0.2, r_c = 1.19 m): **C50 reads 1.91 dB at d = 0.5, 1.0, 2.0, 4.0 and 8.0 m — identical to 3 significant figures** while the closed-form DRR the scene report publishes swings +7.55 → −16.53 dB. The declared placement axis is therefore INERT in every reported ISO-3382 metric, so `test_placement_shift` carries no acoustic difference from the id baseline at the metric level, and the class docstring's claim that the direct component "changes the direct-to-reverberant ratio (C50/DRR)" is FALSE as implemented. Second consequence: the global peak now sits 300-550 samples INTO the diffuse tail, so `_find_onset`'s documented AC-07 assumption ("the direct sound must be the loudest arrival") is violated — inert today only because the whole response starts at d/c. | Make the direct arrival broadband: a unit sample (optionally air-absorption-filtered) at index d/c scaled by 1/d, with the early-reflection envelope separate. Known-answer test: C50 through the ISO path must fall ~6 dB per doubling of d and cross 0 dB near d = r_c, matching the closed-form DRR in `placement_report.json` to a stated tolerance. Until then, state in the D0a/D0b artifacts that the placement axis is not exercised. |
| AC-30 | acoustics-reviewer | minor | OPEN | configs/base.yaml (justification (b) for `distance_range [1.0, null]`) | The ISO 3382-1 §5.3 justification the builder wrote is arithmetically checkable and is partly wrong, and the realized set refutes its conclusion. VERIFIED: d_min = 2·sqrt(V/(c·T)) reduces to 2·sqrt(α·S/(c·K)) — **volume-independent**. The two stated corners reproduce exactly (smallest shoebox: 0.41 m at α 0.05, 1.65 m at α 0.80), but "~2.6 m at the largest" is NOT a corner — it is the largest shoebox at α 0.2; its actual corners are 1.29 m and **5.16 m**. The declared support therefore spans d_min ∈ [0.41, 5.16] m, and 1.0 m sits near its BOTTOM, not "inside the band". REALIZED over 600 base scenes: **25.4 % of id and 42.5 % of test_material_shift scenes sit below their own d_min** by Sabine; 37.2 % and 92.5 % by Eyring. | Correct the third number to a real corner and state the full [0.41, 5.16] m range. Record the realized below-d_min fraction per split in `placement_report.json` (one line beside the existing `d_over_rc` summary) so the E1 report discloses it as measured rather than asserting compliance. The per-scene ISO criterion stays DEFERRED; the disclosure of how far the global floor falls short does not. |
| AC-37 | acoustics-reviewer | major | OPEN | src/amcd/representations/spectrogram.py (`min_db`, `decode`); consumed at src/amcd/evaluation/evaluator.py | **`min_db` BECOMES AN INJECTED ENERGY FLOOR IN THE DECODED PRED, AND THE REPORTED ISO METRICS HAVE ONLY ~26 dB OF ABSOLUTE-LEVEL HEADROOM.** `decode` rescales the carrier's band-frame power to `10**(env/10)`, and `env` is clamped at an ABSOLUTE `min_db`, so wherever true power is below the floor the decode BOOSTS the carrier up to it — injecting a non-decaying floor into the pred waveform INSIDE the shared Schroeder window. MEASURED with an ORACLE pred = `decode(encode(high), low)`, a DEFINITIONALLY PERFECT prediction: pred band energy exceeds high by +38.2 dB at t = 1.5 s and +152 dB at 3.0 s, with the Lundeby cut at 3.109 s. Scaling the same scene by a gain (high T30 = 0.7750 s throughout): oracle-pred T30 error +0.00 % at 0 dB, +1.48 % at −25, **+24.4 % at −30, +411 % at −35, +935 % at −40**; at −60 dB EDT reads 8.80 s against 0.806 s and C50 −12.8 dB against +4.68 dB. CONTROL at `min_db: -200`: ≤ +0.38 % everywhere — so the model, carrier, filter and Schroeder path are all innocent. `d0b_t30_jnd_frac` is breached at ~−27 dB of level. **INERT under dry_run** (worst \|ΔT30\| = 0.25 % over every declared corner) because the scaffold's level convention is `direct = 1/d` with a room-constant tail — so this will FIRST APPEAR on the emulated gsound render RD-33a gates, the most expensive place to find it, and it will look like a model failure rather than a representation constant. AC-33 declared the reference and judged it inert on dynamic-range grounds; this is the consequence that measurement did not reach. | An unexercised path producing a physically wrong result on VALID input — it needs a GUARD, not removal. **USER DECISION 2026-08-10 (RD-86): remedy (a) GUARD + (c) SWEEP; NOT (b).** (b) — a per-scene-relative decode floor — was rejected because it changes the DECODE contract, i.e. the Research II energy-domain reframe itself, and would move every decoded waveform on top of lane M's other metric changes, muddying the single fixed-seed A/B M is judged on. **(a) is assigned to lane M** and belongs in `spectrogram.encode` (fail loud when a scene's per-band peak sits closer than a config-declared N dB to `min_db`) — NOT in `data/preprocess.py`, which lane P owns and which would make the row unfinishable in its lane (RD-83's check caught this). **(c) is on the integrator queue**: the D0b level sweep needs `diagnostics/probe.py` (lane S). **KNOWN-ANSWER TEST first, it is remedy-independent, cheap, and needs no render: `decode(encode(high), low)` must reproduce high's T30 within `d0b_t30_jnd_frac` at gain ∈ {0, −20, −30, −40} dB.** |
| AC-38 | acoustics-reviewer | minor | OPEN | src/amcd/evaluation/room_acoustic.py (resolvability floor) — NARROWS AC-26 | **THE FLOOR MOVED THE CENSORING, IT DID NOT REMOVE IT, and the source comment overstates the fix.** The threshold is a per-(metric, band) constant — correct — but the decision is still `fitted_value < floor`. MEASURED, 200 realizations × 2 bands: at true T60 = 0.05 s, 1.2 % of T30 suppressed / +4.6 % survivor bias; 0.04 s → 18.8 % / +7.5 %; 0.03 s → 46.0 %; 0.02 s → 72.8 % / +13.9 %. REACHABLE: base.yaml's declared support (ceiling_absorptive α ≤ 0.98, corridor 15×1.5×2.4 m) admits Eyring T60 = 0.0179 s and the smallest shoebox 0.0190 s, both BELOW the 500 Hz T30 floor. Inert under dry_run only because the scaffold renders Sabine (0.0714 s) — `test_material_shift` sits on the floor the moment a real render replaces it. The comment "INDEPENDENT of the value being tested — the independence is the whole correction" is half right: the THRESHOLD is independent of the datum, the DECISION is not. **SEPARATELY: margin 2.0 is CONFIRMED well calibrated** — 500 Hz T30 bias +12.83 % at margin 1.0, +4.90 % at 2.0, +2.82 % at 3.0, so 2.0 lands where the bias crosses this project's own `d0b_t30_jnd_frac`. | Keep the margin and its calibration (now in base.yaml). Fix the OPERAND: decide resolvability from a leg-independent quantity already in hand — `IRResult.meta["rt60_s"]`, or `placement_report.json`'s Sabine/Eyring T60 — or do for the floor what `metric_edt_variance_limited_s` does for variance and count-and-disclose per split instead of suppressing. TEST: a 200-realization sweep at true T60 ∈ {0.02 … 0.08} must show the reported split mean tracking the TRUE value. |
| AC-39 | acoustics-reviewer | minor | OPEN | src/amcd/evaluation/room_acoustic.py (the floor's NaN reason string) | The drop log names a mechanism that is not the only one, and the other one is LIVE TODAY. The reason reads "at this decay the fitted slope measures the filter, not the room". MEASURED at a corner base.yaml's declared support admits (3×3×2.4 m, α 0.98, d 2 m, rendered T60 0.0758 s): the HIGH leg's EDT reads 0.0121 s at 500 Hz and 0.0065 s at 1000 Hz — but T30 reads 0.0852 / 0.0757 s, so the room decay is measured fine. What makes the first 10 dB short is the DRR (C50 = 45.6 / 51.1 dB), so EDT is measuring the DIRECT ARRIVAL — an ISO-3382-real quantity, not a filter artifact. Both bands drop, `n_bands_kept` = 0, and the scene loses EDT entirely under a reason that tells the E1 reader the wrong cause. | Split the reason: report `d_over_rc` / the band's C50 alongside, and word it as "the first 10 dB is set by the direct arrival and/or the filter's own ringing at this decay". |
| AC-41 | acoustics-reviewer | minor | OPEN | src/amcd/scenes/generator.py `_room_acoustics` vs src/amcd/simulators/dry_run.py `render` | **THE α CLIP DIFFERS BETWEEN THE ROOM THE REPORT DESCRIBES AND THE ROOM THE SCAFFOLD RENDERS** — the exact divergence AC-24 exists to prevent, on a second variable. `_room_acoustics` clips α to [1e-6, 1−1e-6]; `dry_run.render` clips to [0.01, 0.99]. Both then feed `acoustics.room_constant`. MEASURED (5×4×3 m): identical at α ≤ 0.99, but α = 0.995 → published R = 18706 m² vs rendered 9306 m², a **+3.03 dB gap** between `placement_report.json`'s `drr_db` and the DRR the scaffold realizes (r_c 19.29 vs 13.61 m); α = 0.999 → +10.04 dB. The T60 clip is recorded (`rt60_clipped`); the α clip is recorded nowhere. Not reachable at base/research_i's α ≤ 0.98; a config raising `ceiling_absorptive` reaches it silently. | Share ONE clip (or one guard) between the two call sites, as AC-24 did for `SABINE_K`, and stamp `alpha_clipped` into `IRResult.meta` beside `rt60_clipped`. TEST: at α = 0.995 the scaffold's `expected_drr_db` must equal the report's `drr_db`, or the scene must raise. |
| AC-42 | acoustics-reviewer | minor | OPEN | src/amcd/evaluation/room_acoustic.py (C50 branch) | **C50 HAS NO RESOLVABILITY GUARD AND NO `pred_unresolved` COUNTERPART.** T30 and EDT get a filter-derived floor and a `pred_unresolved_hz` accounting entry; C50 gets neither, and is guarded only against `late == 0`, never against `late` sitting at the float noise floor. MEASURED: with pred replaced by a 5 ms decay, `T30.pred` and `EDT.pred` are correctly NaN with AC-25 reasons while **`C50.pred` reports +203.4 dB** — finite, therefore scored, therefore pooled into the split's `pred_mean` and its CI. The paired `improved` flag is safe (a +203 dB pred simply fails to improve), so this is an ABSOLUTES defect, not an improvement one. | Give C50 what its siblings have: NaN with a reason when the late window is within a declared dB of the numeric floor, and a `pred_unresolved` entry in `band_accounting`. TEST: a degenerate pred must leave C50.pred unscored with a reason, not +203 dB. |
| AC-43 | acoustics-reviewer | minor | OPEN | src/amcd/simulators/dry_run.py (the "NOT MODELLED" note) — completes AC-28 | The missing early-reflection cluster is an acceptable scaffold simplification, but it leaves **EDT nearly inert on the placement axis** and nothing says so. Because the diffuse tail begins at the direct arrival, the first 10 dB has no reflection structure. MEASURED at 10×8×3.5 m, α 0.2: EDT = 0.7504 / 0.8056 / 0.7828 / 0.7803 / 0.7805 s at d = 0.5/1/2/4/8 m — NON-MONOTONE, 5.5 % spread — against C50's monotone swing over the same 16× range. In a real room EDT moves systematically with distance. So AC-28 made C50 live on the placement axis and left EDT essentially dead there; `test_placement_shift`'s EDT column is a plumbing result, not an acoustic one. | AC-28's own unfinished clause ("state in the D0a/D0b artifacts that the placement axis is not exercised"), now NARROWED from "the axis" to "EDT on the axis" and measured. State it in the D0a/D0b artifacts and the E1 report. Do NOT add an early-reflection model to the scaffold — that is the real simulator's job. |
| AC-36 | acoustics-reviewer | major | OPEN | src/amcd/evaluation/room_acoustic.py `_butter_octave_filter` / `_band_energy` | HALF THE DIRECT ARRIVAL'S IN-BAND ENERGY WAS DISCARDED FROM THE ISO INTEGRAL. The zero-guard fix correctly killed the `padtype="odd"` reflection (6.171 → 0.006728 at 500 Hz), but stripping the guard threw away the acausal pre-ringing of whatever sat at the onset index: 0.006728 against the 0.013228 that both an interior impulse and the analytic ∫\|H\|⁴df give — 50.9 %. The builder's "common-mode across legs" claim was CONFIRMED (≤ 0.0012 dB on paired quantities) but the bias is a monotone function of DRR — −3.71 dB at d = 0.5 m, −0.00 at 8 m — so it is NOT common-mode ACROSS SCENES. The builder's proposed alternative ("restructure so filtering sees the natural pre-onset silence") is a measured NO-OP (max\|Δ\| = 4.6e-10). | FIX APPLIED (cycle 3), awaiting re-review: the guard energy is folded back into the record's first sample — energy-conserving, keeps t=0 at the direct arrival. Verified: an onset impulse now yields 0.013228 at 500 Hz, ratio 1.0000 against an interior one. The AC-40 test then caught an asymmetry the review did not: only the LEADING guard was folded, so the record's LAST sample still lost half its energy to the trimmed post-ringing — which is precisely a record truncated mid-decay (AC-22's subject), landing in the C50 late window and the Lundeby estimate. Both ends now folded, both ends explicitly zero-padded so `padtype="constant"` replicates 0.0 rather than the edge sample. |
| AC-64 | acoustics-reviewer (integration) | blocker | OPEN | src/amcd/evaluation/room_acoustic.py:59-87 `_lundeby_truncate`, :278-329 `_shared_truncation_per_band`; commit b5af7b9's F-89 verdict | **"LUNDEBY IS FINDING A REAL CROSSING" IS FALSE, AND THE INTEGRATION BOUND THE ONE REAL RENDER PRODUCED IS REPRODUCED BY NOTHING.** (a) PROVEN at code level, not inferred: on a zero-padded leg (`_fit_to_window` always pads — AC-56) the last 10 % of the record is IDENTICALLY zero, so `noise_power` is the **1e-30 fallback constant** and the "10 dB above the noise floor" test degenerates to an ABSOLUTE 1e-29 sitting **278.3 dB (500 Hz) / 281.9 dB (1000 Hz) below the band peak** (probe: mean(noise_region)=0.000e+00, exactly-zero fraction 1.0000, over samples 3.83-4.25 s of a record whose IR ends at 0.93 s). There is no measured noise floor to cross, so b5af7b9's inference contradicts **AC-58**, which the same commit filed OPEN. (b) REPRODUCTION: the real geometry (native 44647 → padded 204000, T30 0.5441 s) gives index **47711 / 46207 (994.0 / 962.6 ms)** — PAST the native support, i.e. F-89's own "native + ringing" shape — never ~600 ms. The measured 30000 / 28778 (625 / 600 ms) is predicted by no model on the table and both probes rendered into `tempfile.TemporaryDirectory`, so the artifact is gone. (c) CONSEQUENCE, measured: a 600 ms window under-reads T30 by **-43 % at T60 = 2.0 s and -63 % at T60 = 3.0 s**, and the 25.5 ms low/high delta alone moves T30 **11.5 % / 19.7 %** at those T60 — past `d0b_t30_jnd_frac` = 0.05 — while at T60 ≤ 0.55 s it moves it 0.007 % (inert). `ir_duration: 4.25` + `max_t60_over_ir_duration_frac: 0.0` exist precisely to admit those long-T60 scenes. | Do NOT restate AC-17 on b5af7b9's reading. RE-MEASURE with the artifact RETAINED (save `low.npy`/`high.npy` to a throwaway run_dir, not a TemporaryDirectory) and record, per band: `native_ir_samples`, the last non-zero sample of the raw IR, the band-energy peak, `noise_power`, and the index. Two outcomes: if the index really is ~600 ms the real IR's tail is exactly zero (or ~280 dB down) from there, and the E1 T30/EDT for every long-T60 scene is catastrophically wrong; if it is ~994 ms the b5af7b9 verdict is simply wrong and F-89's severity drops. AC-58's remedy (truncate at the last non-zero sample and RECORD that reason instead of falling through to an absolute level) fixes both halves. KNOWN-ANSWER TEST, no render needed: synthetic decays at T60 ∈ {0.5, 1, 2, 3} s zero-padded to `ir_duration` must recover T30 within `d0b_t30_jnd_frac` AND the index must be invariant to a pure gain over ≥ 4 decades. |
| AC-65 | acoustics-reviewer (integration) | major | OPEN | src/amcd/evaluation/room_acoustic.py:252-256 (`_band_resolvable_decay_s` docstring) | **THE ONE PLACE THE RESOLVABILITY FLOORS ARE WRITTEN DOWN IS STALE ON THE MERGED TREE.** The docstring declares itself "THE ONE PLACE THOSE VALUES ARE WRITTEN DOWN (RR-39)" and states 48 kHz: 500 Hz → T30 17.881 ms / EDT 11.765 ms; 1000 Hz → 8.924 / 5.771 ms. MEASURED by calling the function on the current tree: **500 Hz → T30 20.360 / EDT 9.556 ms; 1000 Hz → 10.162 / 4.802 ms** (T30 +13.9 %, EDT −18.8 % / −16.8 %). The AC-36/F-67 energy fold moved the filter's own decay and the declared values did not follow. Corroborated from inside the ledger: AC-27's resolution quotes "f·T30 = 9.85-10.18" across 125-4000 Hz, which reproduces exactly (measured 9.88-10.18 → T30 = 20.36 ms at 500 Hz), i.e. the ledger and the docstring already disagree. `grep` confirms these four numbers appear nowhere else in `src/`, `configs/`, `tests/`, and **no test pins them** (`tests/test_metrics.py` calls `_band_resolvable_decay_s` rather than asserting its value), so the drift is unchecked. These floors × `metric_band_resolvability_margin` 2.0 gate the AC-38 disclosure that reaches `metrics.parquet`, and base.yaml's margin calibration (+12.83/+4.90/+2.82 % at margins 1/2/3) was measured against the OLD floors. | Recompute and restate the four values, re-state the margin calibration against the current floors, and add the known-answer test RR-39's "one place" claim needs: assert `_band_resolvable_decay_s` against the declared values per band, so the next filter-path change cannot move them silently. |
| AC-66 | acoustics-reviewer (integration) | minor | OPEN | external/GSound-SIR `gsSoundMedium.h:162-171`, `gsSoundMedium.cpp:226-228`, `Om Framework/om/math/omScalarMath.h:1935`; configs/simulators/gsound_sir.yaml; ledger AC-54's remedy | **AC-54's DOMAIN CONFUSION HAS A SECOND CALL SITE — AIR ABSORPTION — AND AC-54's REMEDY DOES NOT REACH IT.** `AIR_ABSORPTION` is ON (`Context.cpp:17`). `SoundMedium::getAbsorption` already halves the ISO pressure dB/m ("convert sound pressure dB/meter to sound intensity dB/meter", `gsSoundMedium.cpp:227`), then `getAttenuation` converts with `math::dbToLinear`, which is **10^(dB/20)** (`omScalarMath.h:1935`), and the result multiplies the same quantity `generate_ambisonic_ir` later takes `sqrt()` of (`binding.cpp:417,454`). Net: the realized ENERGY air attenuation is **α_ISO/4 dB per metre — a factor of 4 too weak**. Inert at the reported bands (α_ISO ≈ 0.0024 / 0.0041 dB/m at 500 / 1000 Hz → ≤ 0.4 % on T60 in a 60 m³ room, so it does NOT explain AC-54's −5.6 % residual or the 500-vs-1000 Hz split). Live on two paths the config already admits: `iso_eval_freqs` is a config list, and at 8 kHz the error is ~19 % of T60 in a small room; and 4mV scales with V while Sα scales with S, so at base.yaml's largest declared room (12×10×5 m, α 0.05) physically-correct air absorption would DOMINATE the Eyring term at high frequency. Pre-compensating α at `createbox` (AC-54's remedy) corrects reflection only and leaves this untouched. | Take it WITH AC-54 — one domain confusion, two sites, and fixing one alone leaves the config's declared acoustics still describing a room that was not rendered. Either declare in `configs/simulators/gsound_sir.yaml` that the backend applies α_air/4 (with the measured consequence per band), or pre-compensate the medium as well. GUARD: refuse an `iso_eval_freqs` above the band where the error exceeds a config-declared tolerance. Test: two renders of one large low-α room differing only in `iso_eval_freqs` extent, T30 compared against Eyring+4mV at the physical m. |
| AC-67 | acoustics-reviewer (integration) | minor | OPEN | ledger AC-54 (evidence basis); external/GSound-SIR `SoundMesh.cpp:223`, `gsSoundPropagator.cpp:1531,1578,4518`, `auralizer/src/cpp/binding.cpp:417,454` | **AC-54 IS CONFIRMED FROM SOURCE, WHICH REMOVES THE "ONE SCENE, ONE REALIZATION" OBJECTION — AND THE RENDER'S RESIDUAL IS STILL UNEXPLAINED.** Independent derivation, no render: `createbox` sets `reflec = sqrt(1-α)` (`SoundMesh.cpp:223`); `diffuseAttenuation *= getReflectivityBands()` per bounce (`:1531`); `getDistanceAttenuation` is `1/(4π(1+d²))`, an INTENSITY (`:4518`); and the synthesizer takes `sqrt(intensities)` to get pressure (`binding.cpp:417,454`). So pressure ∝ (1/d)·(1-α)^(n/4) → the per-bounce ENERGY factor is sqrt(1-α) → **α_eff = 1-sqrt(1-α)**, exactly. The 1/d law coming out CORRECT is what forces it: the two cannot both be right. AC-54's pre-compensation algebra also checks out — `sqrt(1-(1-(1-α_t)²)) = 1-α_t`. TWO CAVEATS THE ROW SHOULD CARRY. (a) A competing explanation the pre-registration did not name: `IR_THRESHOLD` and `ADAPTIVE_IR_LENGTH` are both ON (`Context.cpp:19,21`), so the native IR length is set adaptively by an energy threshold — that, not the 3.0 s `maxIRLength` cap, is why `native_ir_samples` differs between budgets (44647 vs 46544), and it prunes the tail the T30 fit sits on. None of it, nor `diffuse_depth` 100 (≈ 0.74 s of path, against a −35 dB point at 0.336 s), nor air absorption (AC-66, ≤ 0.4 %), can move a 1.8× discrimination, so the verdict stands. (b) The 500-vs-1000 Hz spread — 0.5660 vs 0.5222 s, **8.4 %, ~1.7× the project's own `d0b_t30_jnd_frac`** — is unexplained on a room with frequency-independent α, and it was measured on ONE realization of the 5,000-ray leg. That is an unbudgeted single-realization T30 uncertainty which the pre-registered rule did not account for; it is recorded here because AC-58/RD-55 will need it. | Restate AC-54's evidence as SOURCE-DERIVED with the render as confirmation, so the row does not stand or fall on one scene. Add the two caveats. When AC-54 is fixed, the known-answer test is the direct path, not T60: render one scene and check that direct-arrival ENERGY falls 6.02 dB per doubling of d (already the case) AND that T30 matches Eyring at the DECLARED α to a stated tolerance. |
| AC-68 | acoustics-reviewer (integration) | minor | OPEN | src/amcd/evaluation/room_acoustic.py:90-139 `_butter_octave_filter` | **THE OCTAVE FILTER'S CONFORMANCE CLASS IS NOWHERE DECLARED OR TESTED, WHILE THE MODULE CLAIMS "THE STANDARD ISO-3382 PATH".** ISO 3382-1 requires octave-band filters conforming to IEC 61260 class 1; nothing in `src/`, `configs/` or `docs/` names a class, and no test checks a stopband mask. MEASURED through `_band_energy` with a pure tone (dB re the tone's total energy): 500 Hz band → **−5.99 dB at 353.6 Hz and −6.00 dB at 707.1 Hz** (correct: `sosfiltfilt` squares |H|², so the −3 dB edges become −6 dB, and adjacent bands are power-complementary at the crossover — energy conservation across the decomposition CONFIRMED), but out-of-band rejection is only **−37.4 / −38.5 dB one octave out (250 / 1000 Hz) and −46.6 / −47.3 dB two octaves out (125 / 2000 Hz)**. Benign today — `createbox` applies one scalar α to all 8 simulated bands, so every band decays alike, and `iso_eval_freqs` is [500, 1000] — but it is exactly the case AC-63 (per-band absorption, upstream's vector-`createbox` overload) makes live, where a steep inter-band level difference leaks the loud band's decay into the quiet band's T30. | Not "raise the order": DECLARE. State the realized class in `_butter_octave_filter` with the measured stopband figures, and add the known-answer test the checklist wants — a tone one and two octaves out of band must land below a config-declared rejection, so any change of filter order or padding is caught. Reassess severity if `iso_eval_freqs` widens or per-band absorption lands (AC-63). |

### readability-reviewer

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| RR-24 | readability-reviewer | major | OPEN | README.md (repository map; "40+ tests"; Simulator note) | The README's prose survived the `configs/` reorg; its REPO MAP did not. It still reads `configs/  # base / dry_run / test_tiny / models/*` — neither `configs/dry_run.yaml` nor `configs/test_tiny.yaml` exists any more, and the map names none of `overlays/`, `representations/`, `simulators/`, contradicting the correct three-file-kinds section 60 lines above. The map also omits `scripts/`, and neither it nor the Simulator note links `docs/gsound_sir_setup.md` or `scripts/setup_gsound_sir.py`, so a newcomer on an x86 box is told the real backend "builds and runs natively" with no path to the installer. "40+ tests" understates a 296-test suite. | FIX APPLIED (cycle 3, after the reviewer read HEAD 660d645), awaiting re-review: repo map states the three config file kinds, adds `scripts/`; the Simulator note links `docs/gsound_sir_setup.md` and `scripts/setup_gsound_sir.py`; the test count is now "the full suite". **NOT yet done from the reviewer's NEW half:** `provenance.py` and `acoustics.py` are still absent from the map. |
| RR-32 | readability-reviewer | minor | OPEN | src/amcd/scenes/generator.py; src/amcd/config.py | Two docstrings have tipped from "why" into re-litigating the bug report, with numbers pinned to a config state that no longer holds. `_check_regimes_clear_backend_floor` embeds "measured P(d < 0.3 m) = 0.186 %/scene → ~67 % chance a 600-scene run aborts" — base.yaml now declares a 1.0 m minimum, so that measurement describes a config that can no longer be produced. `_check_split_roles` replays the F-44 reproduction transcript. Both facts are already in the ledger rows the docstrings cite. (Contrast: `_disclose_and_gate_record_length` and `_sample_positions` are the right altitude.) | Cut each to the contract plus the ledger id. |
| RR-33 | readability-reviewer | minor | OPEN | configs/overlays/*.yaml | The same AC-22 rationale for `max_t60_over_ir_duration_frac: 1.0` is written out three times, near-verbatim. Three copies of one rule is three places to update and three chances to disagree. The rest of the new overlay prose is load-bearing and should stay. | Keep the full paragraph in ONE overlay (`dry_run.yaml`, the canonical one); reduce the other two to `# plumbing overlay, no acoustic fidelity — see overlays/dry_run.yaml (AC-22)`. |
| RR-35 | readability-reviewer | minor | OPEN | src/amcd/config.py | 1218 lines is navigable ONLY because of the banner comments — and the last 85 lines have none: `_PLUGIN_REGISTRY`, `_require_registered`, `_attach_params_block`, `_PLUGIN_BLOCKS`, `_merge_layer`, `_deep_update` sit after the `Config` class under no heading, so the merge primitive F-11 turns on is findable only by grep. Separately, `Config._check` delegates to five well-named `_check_*` helpers and then keeps ~95 lines inline, including a shift-split validation block exactly as self-contained as the five that were extracted. | Add a `# ── Plugin blocks + layer merge ──` banner (matching the three existing ones), and extract the shift-split block as `_check_shift_splits` so `_check` reads as a list of named checks. No behaviour change. |
| RR-36 | readability-reviewer | minor | OPEN | src/amcd/pipeline.py (`_gen_scenes_fingerprint`, `_train_fingerprint`, `_stats_fingerprint`, `run_stage`, STAGE_UPSTREAM, STAGE_CODE_SCOPE) | The RR-32 failure mode is now pipeline.py's dominant idiom AND it duplicates. Four docstrings carry full reproduction transcripts pinned to a throwaway run; **F-53 is narrated twice** (`_train_fingerprint` and STAGE_UPSTREAM) and "a config fingerprint is blind to code changes" is stated **four times** (STAGE_FINGERPRINT, `_eval_fingerprint`, STAGE_CODE_SCOPE, `provenance.code_version`). Reading `_train_fingerprint` to learn what it covers means reading an old bug first. **Builder-introduced this cycle.** | Cut each transcript to the rule + the ledger id — `git log -S` recovers the evidence, which is what the id is for. Keep the invariant sentences ("chaining reads the upstream SENTINEL, never a recomputation"; F-58's "invalidate before mutating"; the per-aspect seed note). State the code-blindness point ONCE at `STAGE_CODE_SCOPE` and have the other three cite it. |
| RR-37 | readability-reviewer | minor | OPEN | src/amcd/simulators/dry_run.py (class docstring vs the AC-28 and AC-18 inline blocks) | Same class, plus a duplicate: the class docstring already states AC-28's before-state, and the inline block re-tells it at ten times the length with a second copy of the same measurement. With the AC-18 block, roughly a third of this module is narration of superseded implementations. Three stray blank lines at the imports. **Builder-introduced this cycle.** | Delete the duplicated AC-28 narration; keep the rule, the one-sentence AC-07 peak consequence, and the NOT MODELLED caveat (a live one). Compress the AC-18 block to what the tail models today. |
| RR-38 | readability-reviewer | minor | OPEN | src/amcd/evaluation/room_acoustic.py `_butter_octave_filter` | A 30-line docstring for a 12-line function: 3 lines of contract, ~13 of bug-report transcript, and — uniquely in this file — **the padding defect it records carries no ledger id**, while every neighbouring block cites one. The one finding whose only record is this docstring is the one a reader cannot recover context for. **Builder-introduced this cycle.** | Keep the contract, the rule in two sentences, and the KNOWN RESIDUAL paragraph (a live caveat on absolute C50). Give the padding finding a ledger id — see AC-36, raised for it. |
| RR-42 | readability-reviewer | minor | OPEN | src/amcd/provenance.py (`ALL_SOURCES`, `code_version`) | Module docstring and `git_sha`/`git_is_dirty` judged exemplary. Two blemishes: (a) `ALL_SOURCES = (".",)` — the NAME says all sources, the VALUE is a dot, and nothing says the dot means the package root; a reader meets the constant before `code_version`'s rglob. (b) The `code_version` docstring is 27 lines for a 15-line function, re-telling F-55's reproduction and duplicating `STAGE_CODE_SCOPE`'s scoping rationale. **Builder-introduced this cycle.** | (a) One half-line on the value. (b) Cut the transcript to its rule; keep the scoping rationale HERE and have `STAGE_CODE_SCOPE` point at it (per RR-36's "state it once"). |
| RR-43 | readability-reviewer | minor | OPEN | src/amcd/evaluation/evaluator.py; src/amcd/data/dataset.py | Two artifact globs silently filter `if not p.name.startswith("._")` with no explanation anywhere in the package. A reader cannot know these are AppleDouble sidecars the host filesystem creates; it reads as arbitrary magic a maintainer could delete or propagate as a convention. | One shared comment (or a small `_artifact_files` helper carrying it) naming what `._` files are. **Reviewer flags one line as out of its scope and for `research-director`: whether a host-filesystem artifact should be filtered inside PACKAGE code at all touches the cross-platform rule.** |
| RR-44 | readability-reviewer | minor | OPEN | src/amcd/scenes/generator.py `flag_key_present` | The only public helper in a module of `_`-prefixed helpers, defined AFTER its single caller, and mis-named: it is not "a key is present" but "this scene carries every flag being counted", i.e. "this scene was characterized". A reader at the call site cannot tell it encodes the RD-64 characterized/uncharacterized distinction. **Builder-introduced this cycle.** | Rename `_scene_is_characterized`, move above `_flag_counts`, one-line docstring naming the `sabine` vs `none` distinction. |
| RR-45 | readability-reviewer | minor | OPEN | src/amcd/representations/spectrogram.py (`_build_third_octave_filters` measured table) | The measured comparison table has UNLABELLED columns (`8000/256 → 18 vs 19`), and the 48 kHz row is symmetric so it gives no ordering cue — a reader cannot tell whether the new rule gained or lost a band. This is the load-bearing evidence for what the comment itself calls "a real change of coverage". **Builder-introduced this cycle.** | Label the columns (`new vs old`) or write `18 bands (was 19)`. |

## DEFERRED backlog

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| RD-46 | research-director | major | DEFERRED (gate: E1 report) | configs/research_i.yaml `ceiling_absorptive` | The split's absolutes are diagnostic-only. Was OPEN pending AC-25/AC-26. | **RESOLVED IN SUBSTANCE by cycle 3 (RD-74), re-statused to the E1-report obligation that remains.** The user's 2026-08-10 decision was to suppress absolutes below `metric_min_measurable_t60_s`; that threshold is gone, because at this split's Eyring median T60 of 0.059 s it suppressed the MAJORITY of EDT realizations and biased the survivor mean +31.2 %. Censoring an estimator on its own value is a distortion, not a disclosure. The filter-derived floor (~0.010-0.020 s) does not bite, so the numbers ARE now reported — and labelled instead, via the per-split `n_estimator_variance_limited` count. **The change is deliberate and is written into `configs/research_i.yaml` deviation 2.** What remains is E1 REPORT PROSE stating the split is high-variance diagnostic, never physical. |
| RD-04 | research-director | low | DEFERRED | configs/base.yaml (seeds.split_assignment) | Split seed must be pinned stable-for-life; changing it after E1 is a deliberate re-dataset event (leakage/faithfulness risk). | Belongs to the pre-E1 falsifier pass: add a guard against silent change to the split seed. |
| F-06 | falsifier | minor | DEFERRED | src/amcd/representations/spectrogram.py (loss); src/amcd/training/loss.py | Dual loss source of truth: `rep.loss(pred,target,delta)` takes a RAW-dB δ; a future caller wiring it against z-scored operands would silently reintroduce F-01. | Belongs to the E2 loss-architecture work (unify `build_criterion` / `rep.loss`). Mitigation in place: docstring states δ must be operand-domain; the sole caller already scales it. |
| RD-12 | research-director | minor | DEFERRED (gate: E4) | report/stats ray-budget axis label | The swept variable is the DIFFUSE ray budget with specular fixed; "reduce ray count" could be read as a total. | Config/spec/meta halves all landed and re-confirmed at pass 4. Only the E4 report axis label remains, and there is no axis to label until E4. |
| RD-14 | research-director | minor | DEFERRED (gate: Step 4) | render QC gate | Per-scene QC that raises on first failure aborts a full batch — costly under emulation. | Persist ALL renders first, write `renders/qc_failures.csv`, fail loudly after the batch. Design commitment for code that does not exist yet. |
| RD-15 | research-director | minor | DEFERRED | scripts/setup_gsound_sir.py (version selection) | Auto-fetch/auto-switch of GSound-SIR versions, side-by-side multi-version envs — wanted eventually, not needed for this gate. | The ref-addressable installer + config `commit_sha` pin + runtime installed==pinned verification already let the researcher choose any version; automation belongs to a later tooling pass. |
| RD-17 | research-director | major | DEFERRED (gate: Step 6) | ray-budget probe — **CARRIES THE ≤4-SCENE RENDER PERMISSION** | The probe's load-bearing job is validating that the HIGH leg is a CONVERGED REFERENCE — D0a headroom, every paired-improvement metric, D0b's carrier test and the E4 claim all treat it as ground truth, and `high_ray_budget: 200000` has never been validated. | ≥2 scenes spanning volume/absorption extremes; convergence measured on reported quantities (per-band energy, T30, C50) through the existing ISO path; driven through `build_simulator`, never pygsound directly; artifact labelled engineering-feasibility, NOT an E4 result. Cannot run before Steps 2/3 exist. **PERMISSION CLAUSE (2026-08-10, absorbing what a separate RD-33b row would have said — RD-77):** a real gsound render of ≤4 scenes into a throwaway run_dir is PERMITTED for this probe, and is NOT gated on RD-33a. One obligation, one gate: RD-17 already specifies the probe's content and RD-52 adds to the same artifact, so a third row at a conflicting gate is the fragmentation RD-67 exists to prevent. **CYCLE-4 SUB-GRANT (2026-08-10, user delegated the choice to the builder — RD-89b):** lane R may spend **exactly ONE** of these ≤4 scenes on a SMOKE render whose only purpose is to show `GsoundSirSimulator.render` returns a real `IRResult` through `build_simulator`. Bounds, all binding: 1 scene; throwaway `run_dir`; via `build_simulator`, never pygsound directly; NOT a dataset render; NOT an E4 result; does NOT lift RD-33a; produces no reported number. The remaining ≤3 scenes stay with the Step-6 probe, whose convergence content is unchanged. If lane R wants more than one scene, or any render beyond this smoke test, it STOPS and asks — the grant does not stretch. |
| RD-23 | research-director | minor | DEFERRED (gate: E4) | render realization variance | pygsound exposes no RNG seed, so ONE render per (scene, budget) conflates Monte-Carlo realization variance with the budget effect — and MC variance at low budgets IS the phenomenon under study. | E4 needs ≥N realizations per (scene, budget) or an explicit argument that between-scene variance dominates. Requirement on THIS gate: the render artifact layout must not foreclose adding a realization index. |
| RD-27 | research-director | major | DEFERRED (gate: E1 report) | `test_geometry_shift` comparability | RI's `test_geometry_shift` is DUAL-axis (corridor AND near_wall+far_pair), which violates invariant #10. Reproducing it verbatim imports RI's confound; deviating makes v3's split NOT comparable to RI's 0.61 baseline rel-L2. | ARBITRATED: keep invariant #10. The config-side disclosure is written into `configs/research_i.yaml`; what remains is E1 REPORT PROSE — state the non-comparability per split and relabel the split "v3-corrected single-axis geometry shift". |
| RD-28 | research-director | major | DEFERRED (gate: per-surface materials / E1 report) | material-shift comparability | RI's material shift is "ceiling_absorptive AND asymmetric_walls"; `asymmetric_walls` needs per-surface absorption, which a scalar `material_absorption` and `createbox(absorp)` cannot express. **Sign corrected (AC-20):** the scalar α applies to ALL SIX SURFACES, giving Sabine T60 0.117/0.161/0.209 s vs an area-weighted ceiling-only reading's 0.42/0.51/0.65 s — 3-4× STRONGER, not weaker. | Implementation deferred to a per-surface-materials gate; the disclosure is written into `configs/research_i.yaml` and remains an E1 report obligation. **CONTINGENCY:** gsound's `createbox(absorp)` takes scalar-or-per-band and NOT per-surface (confirmed, `docs/gsound_sir_setup.md` §4), so if per-surface proves unreachable this decision reopens — itself an argument for an eventual custom simulator. |
| RD-29 | research-director | major | DEFERRED (gate: E1 report) | placement distance distribution | RI never gives `mid_pair`/`far_pair` source-receiver distance sub-ranges numerically. Substituting the global 1.0-10.0 m range changes the distance distribution of five of six splits, and distance sets DRR → C50/D50/EDT. | Do not invent sub-ranges. The quantification exists (`placement_report.json` records the realized per-split distribution, AC-09 confirmed); what remains is the E1 report stating exactly what was substituted. See also AC-30. |
| RD-38 | research-director | minor | DEFERRED (gate: cross-model comparison / multiple raytracers) | src/amcd/config.py role grammar | The role grammar cannot express a sweep over a plugin NAME (`model.name`, `simulator.name`): `_attach_params_block` runs before `_resolve_roles` and requires a concrete name. The roadmap wants both cross-model comparison and multiple raytracers. | Recorded so it is discovered now rather than at E4. |
| RD-41 | research-director | minor | DEFERRED (gate: post-E1 caching hardening) | src/amcd/pipeline.py STAGE_FINGERPRINT | Stages still declaring `None`. **NARROWED at pass 4:** preprocess, eval and stats are now wired, so the remaining gap is diagnostics, train, infer, report. **The train/infer half is NO LONGER DEFERRABLE — see F-53**, which shows the cost rationale ("the expensive artifact is the render") does not cover a wrong reported result. | Only `diagnostics` and `report` remain genuinely deferrable. Fold train/infer into F-53. |
| RD-52 | research-director | minor | DEFERRED (gate: Step 6) | ray-budget probe artifact | AC-17's fix is exercised only by a synthetic known-answer test, because the defect is inert under dry_run by construction. Its first exercise on real data would otherwise be the expensive emulated render it was written to protect. | Add to the RD-17 probe artifact: per-leg vs shared truncation indices on REAL IRs, plus T30/C50 computed both ways. Costs nothing extra. See also AC-34, which needs the same evidence. |
| RD-55 | builder (follow-on to RD-44) | minor | OPEN | src/amcd/evaluation/room_acoustic.py | The shared window is set by the noisiest leg, so reported ISO absolutes remain a function of the low-ray budget. An E4 "metric vs ray count" curve would mix genuine convergence with a window that shrinks as the budget drops. | **RE-STATUSED FROM DEFERRED(E4) TO OPEN 2026-08-11.** A row deferred to a gate that has ARRIVED is not deferred (RD-90's rule). RD-55 predicted that the shared window is a function of the ray budget; the cycle-4 two-budget render MEASURED it (F-89: index moved 25.5/30.8 ms between 5,000 and 200,000 rays, against a 480-sample rule fixed before the run). It is now cluster C5 with F-89/AC-58/AC-64, and its Lundeby extrapolated-tail compensation is the candidate fix for all four. Paired improvement — everything E1/E2 report — is unaffected. Implement Lundeby extrapolated-tail compensation (ISO 3382-1 Annex) when E4 first needs budget-independent absolutes. Also the real closure for AC-34's residual. |
| AC-19-value | acoustics-reviewer / research-director (RD-58, RD-68) | minor | DEFERRED (gate: E2) | configs/representations/spectrogram.yaml (`min_bins_per_band` VALUE) | AC-19's MECHANISM closed in cycle 2 (config-declared ladder, recorded band description, measured in-band fractions). The VALUE is shipped as 1 — today's behaviour — because raising it is a research decision with real cost: at production framing 3 bins drops every band below ~315 Hz, i.e. all low-frequency coverage. Measured basis: the five lowest bands hold ONE FFT bin each and a 63 Hz tone peaks in the WRONG band (58.7 % in the 78.7 Hz band vs 35.5 % in 49.6 Hz). | Blast radius is bounded: reported ISO-3382 metrics are 500/1000 Hz octave-band quantities computed from DECODED WAVEFORMS, not from these bands, so this cannot corrupt an E1 headline number — it affects the learned representation and low-frequency reconstruction. The roadmap fix is **multi-resolution sampling** (paper §6); merge-or-reframe the low bands is preferred, dropping them is the fallback. Decide at E2 with `preprocessed/meta.json`'s recorded in-band fractions in hand. |


---
## Cycle 4 — the parallel-lane cycle, and its integration

**2026-08-11. Cycle 4 ran as FOUR PARALLEL LANES and has been INTEGRATED.** Protocol:
`docs/parallel_protocol.md`; partition: `docs/lanes/cycle4.yaml`; each lane's own record
(kept verbatim, and holding the substance of every row folded below):
`docs/ledger_inbox/{M,P,S,R}.md`.

### The protocol held where it was designed to

**Zero textual merge conflicts across all four lanes.** Rule 1 (ownership by file,
exclusive) did exactly what it claims. The suite came back **497 passed** after ONE
cross-lane fix, which lane P had predicted, verified and written down before handing
over. Step 4's fixed-seed `ci_table.csv` A/B against `experiments/cycle4_baseline` moved
**exactly one row** — `test_material_shift/EDT`, 7 cells — which is lane M's own
pre-registration, cell for cell.

**Where it did NOT hold: row-id allocation.** See the durable remap table in this file's
header. The protocol has no id-allocation rule and four lanes collided four ways
(RD-126).

**A correction to the step-4 claim (RD-136).** "P/S/R declared expected-neutral and are
neutral" overstates lane S: S's neutrality is **structural** (scene specs are written
before the gate runs, and every spec is byte-identical), not measured. `ci_table.csv`
*cannot discriminate* for F-71, because no shipped config declares a
`characterization: none` family — S said so itself and the integrator repeated the
stronger claim. This is RD-91's concern recurring inside the detector's own evidence.

### The two render scenes (RD-17's grant: 2 spent, ≤1 remains)

Both decision rules were **fixed in writing before the renders ran**. This project has
twice shipped a metric-path change defended on evidence chosen afterwards (AC-36, F-67).

**AC-54 (was lane R's AC-40) — UPHELD.** 5×4×3 m, α = 0.30, T30 on channel 0 through
the real ISO path. Rule: ≤0.43 s → nominal, ≥0.49 s → α_eff, else indeterminate.

```
MEASURED T30 = 0.5441 s        (500 Hz 0.5660 · 1000 Hz 0.5222)
  nominal α = 0.30      Sabine 0.343 s   Eyring 0.288 s
  α_eff = 1−√(1−α)      Sabine 0.629 s   Eyring 0.576 s
        = 0.1633
```

**GSound's effective wall absorption is 1−√(1−α), not α.** Upstream multiplies an
ENERGY-domain distance attenuation by an AMPLITUDE-domain reflectivity. Consequence, and
it is large: every Sabine/Eyring/DRR/critical-distance/d_min number in
`scenes/generator.py`, the `ir_duration: 4.25` choice, and the
`max_t60_over_ir_duration_frac` gate are computed for a room that is **not the room
gsound renders**. Inert under `dry_run` (its own scaffold, its own convention); live the
moment the real backend renders a dataset.

**F-89 (was lane R's RD-113) vs AC-58 (was its AC-44) — ADJUDICATED, and both were
partly wrong.** Lane R flagged this as an unresolved contradiction and explicitly
refused to close either row on the other's evidence, because the deciding experiment was
outside its one-scene grant. One scene, both budgets. Rule: |Δ index| > 480 samples
(10 ms) → F-89 upheld.

```
native_ir_samples    low 44647    high 46544
trunc idx   500 Hz   low 30000    high 28778    Δ 1222  (25.5 ms)
trunc idx  1000 Hz   low 28985    high 27505    Δ 1480  (30.8 ms)
```

- **F-89's CONCLUSION is UPHELD**: the shared truncation index **tracks the ray budget**.
  AC-17's rationale ("the index is noise-floor dependent, so take the min over legs") no
  longer describes the mechanism, and an E4 metric-vs-ray-count curve would move its own
  measurement window along with the metric. This is also the **first real-render
  confirmation of RD-55**, which predicted exactly this and is DEFERRED at E4.
- **F-89's stated MECHANISM does not hold as written, and MY REPLACEMENT FOR IT IS
  WITHDRAWN** — see the correction section at the end of this file (raised by AC-64).
  F-89 predicted `index = native + 240`; the measured indices (~600 ms) are far from
  that, and the HIGH budget gives the **shorter** index despite the **longer** native
  IR. But my written-up explanation — "so Lundeby is finding a real crossing" — is
  **false at code level and contradicts AC-58, which I filed in the same commit**: the
  noise region is 3.83-4.25 s, entirely zeros, so `noise_power` is the 1e-30 fallback
  and the threshold sits 278 dB below the band peak. **Why the index lands where it
  does is currently explained by nothing.**
- **AC-58's neutrality claim survives as stated** (padding per se is neutral here,
  precisely because the index lands inside native support) but does not rescue AC-17.

**Wall clock, a bare observation and NOT a feasibility finding** (RD-17 owns that):
low leg 1.0 s, high leg **181.6 s** — 182× for a 40× budget increase, superlinear, n=1.

### RD-33a, stated PER CONDITION (RD-128)

Cycle 4 **unblocks (ii)** and **lifts nothing** (RD-89c). Reported per condition because
a single sentence hid the direction of travel:

- **(ii) the RD-17 probe** — UNBLOCKED. `GsoundSirSimulator.render` returns a real
  `IRResult` through `build_simulator`; three renders have now run. The probe itself is
  cycle 5's, with ≤1 scene of the grant left.
- **(i) zero OPEN rows on the path list** — **REGRESSED, and now MEASURED.** The first
  statement of this was "roughly ten and roughly ten", because 116 of 214 rows carried
  `anchor: see inbox` and the condition was literally uncomputable (RD-INT2-2 / RR-INT-3,
  raised independently by two reviewers). Anchors are now backfilled from the inboxes, so:

  ```
  OPEN rows anchored in RD-76's explicit path list :  59  of 232
      blocker  4   AC-54 · AC-42-B1 · AC-42-R1 · F-M1
      major   16   incl. F-M2 · F-89 · AC-50 · AC-44 · F-67 · AC-36 · AC-37
      minor   39
      by tree:  src/amcd/scenes/** 26   ·   src/amcd/evaluation/** 33
  ```

  The finding rate on that path list **exceeds** the closure rate — the
  RD-33 → RD-33a → RD-76 drift pattern one level down. **Re-run this count when deciding
  RD-128**; the command is in the cycle-4 integration commit.

**This is a decision for the USER, not the builder, and it is deliberately not taken
here:** either scope (i) by SEVERITY (zero OPEN blocker/major on the path list, minors
explicitly DEFERRED at E1-report with reasons), or freeze (i) against a named row set.
Do not let it be relaxed silently by a cycle that simply stops counting.

### Accounting (RD-127 — print it, and name what is in no list)

Enumerated from the four inboxes. **~138 new findings** arrived with the merge:

```
lane M   28 = 11 F-M*  +  7 reviewer-suffix (AC-42-R1 … AC-19-R7)  +  3 builder  +  7 RD plan-time
lane P   35 = 10 RD    +  6 F-75…80  +  5 AC-44…48  +  14 RR-46…59
lane S   24 =  3 RD    +  7 S-*      +  5 AC-49…53  +   9 RR-60…68
lane R   51 = 12 RD    + 18 F-81…98  + 10 AC-54…63  +  11 RR-69…79
         ───
        138  + 11 integrator rows (RD-126…RD-136) = 149
```

Rev 1 of the integration plan named 6 taken and 13 deferred — i.e. its coverage figure
equalled its own scope, which is **RD-73's exact shape**, and RD-73 is still OPEN two
sections above. `research-director` caught it on the plan (RD-127). Every id is named
below; **none is silently absent**, and the substance of each is in its lane's inbox
under the OLD id per the header's remap table.

### Folded rows — the ones a cycle-5 planner must not miss

Full rows for findings whose consequence is a **wrong reported number**, a **false
clearance**, or a **blocker**. Everything else is enumerated in the block below, OPEN,
with its substance in the inbox named beside it.

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| AC-54 | acoustics-reviewer (lane R, was AC-40) | blocker | OPEN | src/amcd/simulators/gsound_sir.py; src/amcd/scenes/generator.py; configs/base.yaml | **EFFECTIVE ABSORPTION IS 1−√(1−α), CONFIRMED BY RENDER.** Upstream forms path energy as `getDistanceAttenuation(d) × Π reflectivity`, mixing an ENERGY-domain term with an AMPLITUDE-domain one (`SoundMesh.cpp:223`, `gsSoundPropagator.cpp:1410/1578/4518`). MEASURED on a 5×4×3 m, α=0.30 render: T30 = 0.5441 s against 0.343 s Sabine-nominal and 0.629 s Sabine-α_eff, on a rule fixed before the run. The dataset's declared acoustics describe a room that was never rendered. | Either pre-compensate at the `createbox` call site (pass `1−(1−α_target)²`) or re-derive every closed form in `scenes/generator.py` from α_eff. **Declare which in config; do not leave the two definitions coexisting.** Blocks the E1 dataset render: `ir_duration`, the record-length gate and every d_min/DRR disclosure inherit it. Evidence: `docs/ledger_inbox/archive/cycle4-R.md` (as AC-40) + this file's integration record. |
| F-81 | falsifier (lane R, was RD-105) | blocker | OPEN | configs/simulators/gsound_sir.yaml (`render_python`); src/amcd/pipeline.py `_render_fingerprint`; src/amcd/simulators/render.py | **THE RENDER CACHE IDENTITY IS HOST-DEPENDENT — a direct cross-platform violation.** `render_python` sits in `config.simulator.params`, which `_render_fingerprint` hashes WHOLE. PROBE: base.yaml → `6999713b8247…`; base.yaml + a host layer → `51033e7d57c0…`. A 720-scene dataset rendered on this Mac fails loudly on the declared x86_64 second host, demanding a byte-identical re-render. `render.py` already declares this value not a property of the dataset while `pipeline.py` uses it as part of the dataset's identity. | Hash the params that DETERMINE THE IR; exclude host-scoped and disclosure-only params from the single `_HOST_SCOPED_PARAMS` declaration `render.py` already uses. **Fix with F-82** (`max_discarded_tail_db`, a pure disclosure threshold that also invalidates the render cache) — one defect, one edit site. Test: `_render_fingerprint` invariant to `render_python`. |
| F-75 | falsifier (lane P) | blocker | OPEN (narrowed) | src/amcd/pipeline.py `_render_fingerprint`, `STAGE_FINGERPRINT` gen-scenes/render | **A REPORTED NUMBER IS STILL REACHABLE THROUGH A CACHED STAGE, AND `versions.json` VOUCHED FOR IT.** A two-character edit to `simulators/dry_run.py` leaves all nine stages `[skip]` at exit 0 while a fresh run_dir under the same code reports different CIs (`test_geometry_shift` C50 `improvement_mean` −0.6815340 cached vs −0.6824248 fresh). Worse than the F-53 pattern: `versions.json` was RE-STAMPED with a whole-package `code_version` byte-equal to the fresh run's, so the canonical provenance channel positively asserted that the new code produced the old numbers. | **FALSE-WITNESS HALF CLOSED** in lane P: `code_version_unscoped` recorded beside (never inside) the fingerprint, a stderr warning for unprotected stages, and `code_version_describes` in `versions.json`. **THE STALENESS ITSELF IS THE STANDING DECISION — see RD-107**, which the user has now ruled on (wire `render` + `gen-scenes`); NOT yet implemented. Evidence: `docs/ledger_inbox/archive/cycle4-P.md`. |
| RD-107 | research-director (lane P, was RD-99) | major | OPEN | src/amcd/pipeline.py `STAGE_FINGERPRINT` gen-scenes, render | `gen-scenes` and `render` carry NO `code_version` — F-64's defect class one link upstream. A change to `scenes/generator.py` (placement and admission sampling) or to a simulator backend is invisible to the cache. | **USER DECISION 2026-08-11: WIRE BOTH, accepting the re-render cost.** NOT yet implemented. `research-director` then found (RD-133) that the naive wiring costs more than was agreed: `provenance.py:131` unions every scope with `_CORE_SOURCES` (`config.py`, `runtime.py`, `registry.py`, `acoustics.py`, `provenance.py`), so an `acoustics.py` or `select_device` edit would invalidate a 720-scene emulated render — and scope `("simulators",)` would let a `dry_run.py` scaffold tweak invalidate the REAL dataset. Implement as: `gen-scenes` → `("scenes",)`; `render` → **active backend module + `simulators/base.py`, declared by the plugin**; and take RD-104's `src/amcd/device.py` split so `select_device` leaves `_CORE_SOURCES`. |
| AC-45 | acoustics-reviewer (lane P) | major | OPEN | src/amcd/pipeline.py `STAGE_FINGERPRINT["diagnostics"]`; src/amcd/diagnostics/probe.py | **A STALE CARRIER-CEILING CLEARANCE IS A FALSE CLEARANCE OF THE PROJECT'S OWN PREMISE.** D0b's output is not a threshold report, it is a PHYSICAL VERDICT — "CARRIER CEILING CLEARS … Proceed to E1". With no fingerprint, `_is_done` returns True on the bare sentinel and NOTHING invalidates it. MEASURED on a complete run_dir: `diagnostics=SKIP(cached)` under doubled `ir_duration`, changed `ambisonics_order`, changed ray budgets and changed `sample_rate` — every one of which changes the residuals being compared. `probe.py` also imports `evaluation.room_acoustic` with no `code_version`. | Wire `diagnostics` with `code_version(("diagnostics","evaluation","representations","data"))` + the d0a/d0b keys + `iso_eval_freqs`/`metric_onset_rel_db`/`metric_band_resolvability_margin`/`sample_rate`, chained to `preprocess`. **Rate RD-108 major, not minor.** NOTE: the integrator's queue makes `diagnostics` the standing example of the unwired path in two test files, which institutionalises the hole — fix together (RD-129). |
| S-F1 | falsifier (lane S) | major | OPEN | src/amcd/diagnostics/probe.py D0b verdict loop | **D0b CLEARS OVER ATTRITION IT CANNOT SEE — live today, no cache needed.** `_verdict` degrades to N/A only at `n == 0`, and the new coverage bracket keys on `len(dropped)`, so a metric averaged over a SUBSET of scored scenes prints as a bare number with `(PASS)`. CONFIRMED on the canonical dry run: `train` reports `n_scenes=12 n_attempted=12 dropped=0` while `T30 n=11` and `C50 n=10`, and the stage prints `CARRIER CEILING CLEARS … Proceed to E1`. The comment above the verdict states the contract as "any missing metric is insufficient coverage"; the code implements "a metric with ZERO scenes". Sub-defect: `t30_thresh` scales a nanmean over SCORED scenes while `t30_r` averages over NON-NaN-RESIDUAL scenes — two populations on the two sides of one ratio test. | Not fixable inside lane S: needs a config-declared minimum coverage in `configs/base.yaml` (no hidden defaults). Print per-metric `n`. Test: per (split, metric), `n == n_scenes == n_attempted` or the verdict is INDETERMINATE. Pairs with AC-45. |
| F-M2 | falsifier (lane M) | major | OPEN | src/amcd/evaluation/room_acoustic.py; src/amcd/stats/aggregate.py; src/amcd/reporting/tables.py | **THE REPORTED TABLE GOT QUIETER ABOUT A CAVEAT THAT STILL EXISTS.** AC-38's disclosure reaches `metrics.parquet` (`n_bands_resolvability_limited`, scene_0022/EDT = 1) but `ci_table.csv` has no such column and `summary.txt` Caveats shows only `3 high-variance`. Before the change that row read `1 partial-band`; `n_partial_band` went 1 → 0 and **nothing replaced it**. `room_acoustic.py` asserts the opposite ("a reader must be able to see which numbers carry the caveat"). Confirmed in the integrator's own step-4 A/B: the header is unchanged, so no column landed. | Spans lane P (RD-82): one `_count_true` entry in `stats/aggregate.py` beside `n_estimator_variance_limited`, one Caveats part in `reporting/tables.py`. **AC-38 MUST NOT BE CLOSED until this lands** — lane M says so itself. |
<!-- F-M7: superseded by the corrected row in the integration falsifier section below; kept out of the OPEN table so the id is not duplicated (F-103). -->
| F-89 | falsifier (lane R, was RD-113) | major | OPEN | src/amcd/evaluation/room_acoustic.py `_lundeby_truncate`, `_shared_truncation_per_band`; src/amcd/simulators/gsound_sir.py (the zero pad) | **THE SHARED TRUNCATION INDEX TRACKS THE RAY BUDGET — ADJUDICATED BY RENDER, and the mechanism is not the one proposed.** Measured on one scene at both budgets: index Δ 1222 samples (25.5 ms) at 500 Hz and 1480 (30.8 ms) at 1000 Hz, against a 480-sample tolerance fixed before the run. AC-17's rationale ("noise-floor dependent, so take the min over legs") no longer describes the mechanism. The predicted `index = native + 240` does NOT hold, and the HIGH budget yields the SHORTER index despite the LONGER native IR. **My explanation for that ("Lundeby is finding a real crossing") is WITHDRAWN — see the correction section at the end of this file and AC-64.** The index's actual position is explained by no model on the table, and the deciding artifact was destroyed by my own probe (in-memory render, no retained run_dir). | Restate AC-17 before any E1/E4 number. **First real-render confirmation of RD-55** (DEFERRED at E4), which predicted exactly this; the two should be resolved together, and RD-55's Lundeby extrapolated-tail compensation is the candidate fix. Do NOT close AC-58 on this row's evidence or vice versa. |
| AC-50 | acoustics-reviewer (lane S, was AC-46) | major | OPEN | configs/base.yaml (`distance_range` justification); src/amcd/scenes/generator.py | **THE d_min RANGE AC-30 ASKS THE INTEGRATOR TO WRITE INTO base.yaml IS ITSELF WRONG.** AC-30's [0.41, 5.16] m is the `mixed`-regime span only (α ≤ 0.80); `ceiling_absorptive` declares α ∈ [0.85, 0.98] on the SAME shoebox family, and `test_material_shift` selects exactly that regime. Over every geometry × material corner: Sabine **[0.412, 5.712] m**, Eyring **[0.417, 11.413] m**, max at 12×10×5 m, α = 0.98. Writing 5.16 would install a fresh understatement of 1.11× (Sabine) / 2.20× (Eyring) on the split whose realized shortfall is 92.5–100 %. Caught independently by lane S, its falsifier and its acoustics-reviewer. | Write the full product range, both variants. **AND NOW: these are NOMINAL-α numbers, which AC-54 has just shown is not the rendered room** — at α_eff the same corner gives ≈5.35 m Sabine / ≈8.07 m Eyring. Either resolve AC-54 first or write the range explicitly as "nominal α as configured; pending AC-54", and carry the same caveat onto `placement_report.json`'s `below_iso_min_distance` block, which is derived the same way (RD-131). |
| RD-111 | research-director (lane S, was RD-93) | major | OPEN | docs/lanes/cycle4.yaml; rows RD-65, AC-30, RR-32 | **THREE HALF-REMEDIES LOOK WHOLE, AND DELETION IS THIS PROJECT'S ONLY "RESOLVED" MARKER.** RD-65, AC-30 and RR-32 each have a residual in a file lane S does not own, declared only in a YAML comment. `generator.py` now looks clean for all three, so a post-merge reviewer is invited to delete three rows that are each ~two-thirds unmet — and the cycle's accounting counted all three as fully covered. Related: the partition test validates declared `fix:` paths against `owns` but never against the row's own ANCHOR, which is how a spanning row passes rule 4. | **DO NOT DELETE RD-65, AC-30 or RR-32 at step 7.** Residuals: RD-65 → `reporting/tables.py` (per-split over-limit counts into the E1 table); AC-30 → `configs/base.yaml` (the corrected range, and see AC-50); RR-32 → `config.py` `_check_split_roles` (the F-44 transcript). Next cycle's partition gets a `partial_residual:` list, and the partition test flags any row whose ledger anchor names a file outside its owning lane. |
| RD-126 | research-director (integrator) | major | OPEN | docs/parallel_protocol.md | **THE PROTOCOL HAS NO ROW-ID ALLOCATION RULE, AND FOUR LANES COLLIDED FOUR WAYS.** Each lane ran `research-director` on its plan and reviewers on its branch, and each numbered from this file's max — so `RD-93…RD-100` names four different findings depending on which inbox you read, and lane R's `AC-40…AC-43` / `RR-28…RR-38` collided with LIVE OPEN rows. Resolved for cycle 4 by a per-lane remap (header table), but the next cycle repeats it by construction. A blanket find/replace would additionally have corrupted lane M's citation of the live AC-43. | Add to `docs/parallel_protocol.md` "Setting up a cycle": the partition file allocates a DISJOINT id block per lane, and `tests/test_lane_partition.py` asserts the blocks do not overlap each other or the ledger's used set. Lane M's scoped-suffix convention (`F-M1`, `AC-42-R1`) is the cheaper alternative and collided with nothing — either is fine, but it must be declared. |
| RD-128 | research-director (integrator) | major | OPEN | ledger RD-33a condition (i) | **RD-33a CONDITION (i) REGRESSED THIS CYCLE AND THE INTEGRATION PLAN WAS SILENT ON IT.** ~10 new OPEN rows on `src/amcd/evaluation/**` and ~10 on `src/amcd/scenes/**`, against a queue that advances (i) by ~zero. Finding rate on the path list exceeds closure rate — the RD-33 → RD-33a → RD-76 drift pattern one level down, and RD-76 exists precisely to stop the lift condition drifting. | **USER DECISION, not the builder's.** Either scope (i) by SEVERITY (zero OPEN blocker/major on the path list; minors explicitly DEFERRED at E1-report with reasons), or freeze (i) against a named row set. Stated per condition in the integration record above so it cannot be relaxed by a cycle that stops counting. **DECIDED 2026-08-11 (user delegated to the builder): scope by SEVERITY — (i) lifts at zero OPEN blocker/major on the path list, minors explicitly DEFERRED with a reason. 20 rows to clear, was 59. See the DECIDED section. Awaiting re-review.** |
| RD-129 | research-director (integrator) | major | OPEN | src/amcd/pipeline.py; tests/test_stage_cache.py; tests/test_simulator_seam.py | Wiring `gen-scenes`/`render` (RD-107) makes `diagnostics` the **standing example of the unwired path** in two test files, institutionalising AC-45's false clearance in the same edit that closes the neighbouring holes. | Take the `diagnostics` fingerprint (AC-45 / RD-108) in the same change, or state in both test docstrings that the example is a KNOWN OPEN HOLE with its row id, not a sanctioned design. |

### Reading guide — what actually matters in each lane's remainder

Navigation only; the ROWS are two sections below. Written because 130 compact rows do not
tell a cycle-5 planner where to look first.

**Lane M — `docs/ledger_inbox/archive/cycle4-M.md`.** Fix applied in-lane, awaiting re-review:
`AC-42-B1`/`AC-42-R1`/`F-M1` (one finding, three raisers — the first AC-42 guard was a
false "float32 residue" that was really an undeclared C50 ceiling censoring the PHYSICAL
legs; removed, C50 now inherits T30's verdict only), `AC-37-R5` (the AC-37 guard maxed
over CHANNELS, leaving channel 0 — the only one the ISO path reads — unprotected),
`RR-37`, `RR-38`, `RR-45` (verified already closed), `AC-39`, `F-68`.
Still OPEN: `F-M3` `F-M4` `F-M5` `F-M6` `F-M8` `F-M9` `F-M10` `F-M11` · `F-68-R2`
`F-68-R3` `AC-37-R4` `AC-26-R6` `AC-19-R7` · `AC-28-B2` `F-69-B4` · M's plan-time
`RD-93` `RD-94` `RD-96` `RD-97` `RD-98` `RD-99` `RD-100` (all folded into the work).
Load-bearing among these: **`F-M5`** — the lane's own A/B has near-zero power (AC-37 and
AC-42 have ZERO coverage in it, AC-38's changed branch has n=1), so a quiet table is
consistent with three of four changes never executing; **`F-M4`** — residual pred-side
selection on the dependent variable, now with censoring probability a function of room
absorption, correlated with the `test_material_shift` axis; **`AC-37-R4`/`F-M3`** — the
AC-37 guard's limiting band is always the ~25 Hz single-FFT-bin band, so it is a
spectral-flatness test calibrated as a level test.

**Lane P — `docs/ledger_inbox/archive/cycle4-P.md`.** `RD-101` `RD-102` `RD-103` `RD-104` `RD-105`
`RD-106` `RD-108` `RD-109` `RD-110` · `F-76` `F-77` `F-78` `F-79` `F-80` · `AC-44`
`AC-46` `AC-47` `AC-48` · `RR-46`…`RR-59`. Load-bearing: **`AC-44`** confirms F-75/RD-107
from the physics side and widens it — eval reads `renders/<scene>/high.npy` as the
ISO-3382 REFERENCE leg, so this is not "the renders are stale", it is "the reference the
improvement is measured against is stale"; **`RD-104`** (`select_device` sits in
`provenance.py`, hence in `_CORE_SOURCES`, so editing the MPS→CUDA→CPU fallback would
invalidate every fingerprinted stage — the fix is `src/amcd/device.py`, and RD-107's
implementation should take it); **`F-77`/`F-78`/`F-79`** are broken-guard findings lane P
raised against its OWN new tests and fixed.

**Lane S — `docs/ledger_inbox/archive/cycle4-S.md`.** `RD-112` `RD-113` · `S-1` `S-2` `S-F4` `S-F5`
`S-F6` `S-F7` · `AC-49` `AC-51` `AC-52` `AC-53` · `RR-60`…`RR-68`. Load-bearing:
**`AC-49`** — `_C_TIMES_SABINE_K` is a SECOND declaration of Sabine's constant that
disagrees with `acoustics.py`'s by 0.07 % (the AC-24 shape again); **`AC-52`** — the
Eyring d_min, which carries the headline shortfall, is pinned only by an INEQUALITY;
**`AC-51`** — `critical_distance_m` and `iso_min_distance_*_m` use incompatible
absorption-area conventions and the record states neither.

**Lane R — `docs/ledger_inbox/archive/cycle4-R.md`.** `RD-114`…`RD-125` · `F-82`…`F-88`, `F-90`…`F-98` ·
`AC-55` `AC-56` `AC-57` `AC-59`…`AC-63` · `RR-69`…`RR-79`. Load-bearing: **`AC-55`**
(`diffuse_depth: 100` is physically a TIME bound, so 4.1 % of `mixed` scenes — 14.0 %
under AC-54, now CONFIRMED — have a diffuse field that dies before the T30 window
closes, and nothing checks it); **`AC-56`** (pygsound compiles `maxIRLength = 3.0 s` and
does not expose it, so RD-21's truncation QC is structurally dead and 29.4 % of every
record is guaranteed zeros); **`AC-57`** (the `acn_n3d` stamp carries an undeclared
Condon-Shortley phase = a 180° yaw — lane R measured it and stamped
`sh_condon_shortley_phase`, so this is fix-applied/awaiting re-review, and it CORRECTS
its own reviewer, which claimed the convention was wrong outright); **`F-84`** (a
zero-energy render is indistinguishable from a healthy one); **`F-85`**
(`kept_energy_percentage` fabricates 0.0 for an undefined ratio).

**Integrator — this file.** `RD-127` (the plan's coverage equalled its own scope —
RD-73's shape, now printed above) · `RD-130` (ship F-70's second CI column only with
F-M7's small-n guard, or label both uncalibrated) · `RD-131` (AC-50's range is
nominal-α and AC-54 has now falsified nominal α) · `RD-132` (the renumber mapping must
be durable and each row must name its inbox + old id — done in the header) · `RD-133`
(`_CORE_SOURCES` makes the naive RD-107 wiring far more expensive than agreed — folded
into RD-107's resolution) · `RD-134` (RD-106 must be fixed with F-81, one edit site) ·
`RD-135` (pre-register the render decision rules — done, both) · `RD-136` (lane S's
step-4 neutrality is structural, not measured — corrected above; carry the distinction
into protocol gate step 4).

---
## Resume here

**2026-08-11 — cycle 4 (four parallel lanes) MERGED AND FOLDED. NOT COMPLETE.**
HEAD after this commit; suite **497 passed**; `experiments/cycle4_baseline` is the A/B
reference. Plan: `~/.claude/plans/peppy-swinging-barto.md`, reviewed by
`research-director` before implementation (returned DRIFTING, 11 findings, all folded —
they are `RD-126`…`RD-136`).

**Integration gate status** (`docs/parallel_protocol.md`, 7 steps):

| step | state |
|---|---|
| 1 merge four lanes | **DONE** — zero textual conflicts |
| 3 full suite | **DONE** — 497 passed, one cross-lane fix |
| 4 dry run + fixed-seed A/B | **DONE** — exactly one row moved, as pre-registered |
| 2b two render scenes | **DONE** — AC-54 UPHELD, F-89 adjudicated |
| 5 fold inboxes | **DONE** — this section and the two above |
| 6 four-reviewer pass | **DONE — NOT CLEAN.** 26 new findings; two of my own claims corrected |
| 2 integrator queue | **NOT DONE** — 7 items, and RD-143 says the order is wrong |
| 7 delete confirmed rows | **CORRECTLY NOT DONE** — a row is deleted only when fixed AND re-review-confirmed, and the pass was not clean. **Zero rows deleted.** |

**Step 6 outcome.** `research-director` DRIFTING (tracking layer, not the work);
`falsifier` 7 new + 2 of my rows corrected; `acoustics-reviewer` 5 new incl. one blocker
+ one claim of mine refuted; `readability-reviewer` 14 new, no blocker. The reviewers
found defects in the INTEGRATOR's own work — a 5-row shortfall in the fold, 116 missing
anchors, a missed lane-S remap, and a withdrawn explanation — which is the pass doing
exactly its job.

**RE-ORDER THE QUEUE BEFORE STARTING IT (RD-143).** The seven items below were scoped
before the renders and do NOT contain AC-54, this cycle's own blocker; item 6 is
downstream of it and as written would record a nominal-α number AC-54 has falsified.
`research-director`'s ruling, which I accept:

> **Item 0 — backend renderability of the declared population: AC-54 + AC-55 + AC-56,
> decided as ONE change.** They are one question — is the declared scene population
> renderable by this backend at ISO-3382 fidelity? — and as configured the answer looks
> like no: T60 realized 1.14-1.98× nominal; pygsound compiles `maxIRLength = 3.0 s` and
> does not expose it, so `ir_duration: 4.25` can never be filled and RD-21's truncation
> QC is structurally dead; `diffuse_depth: 100` is physically a TIME bound that kills
> the diffuse field before the T30 window closes for 14 % of `mixed` scenes under AC-54.
> Deciding them separately invites three inconsistent partial fixes to one physical
> inconsistency. **Also take RD-144: put the absorption convention in the BACKEND's
> config, not in `scenes/generator.py`, which is backend-agnostic.**

**Item 0b — re-measure the truncation index with the artifact RETAINED** (AC-64). My
probe rendered in memory, so the evidence is gone, and the index's position is currently
explained by nothing. Half of this needs no render at all: synthetic decays at
T60 ∈ {0.5, 1, 2, 3} s zero-padded to `ir_duration`, asserting T30 within
`d0b_t30_jnd_frac` and index invariance to gain over ≥4 decades. **The gain-invariance
half fails on the current tree** — that is AC-58.

Then the seven, all scoped and justified in the approved plan:

1. **F-M2** — AC-38's missing reported column. The table is currently QUIETER about a
   live caveat than it was before cycle 4. Small: one `_count_true` in
   `stats/aggregate.py`, one Caveats part in `reporting/tables.py`.
2. **F-70 + F-M7 together** (RD-130) — imputed-population CI, and the small-n guard
   without which it ships under a demonstrably wrong `95 %` label.
3. **F-81 + F-82** (RD-134) — host-scoped and disclosure-only params out of
   `_render_fingerprint`. Cross-platform blocker.
4. **RD-107** — wire `gen-scenes` + `render`, with the per-backend render scope and the
   `device.py` split RD-133 showed are required to keep the cost to what was agreed.
5. **AC-45 / RD-108 / S-F1** (RD-129) — `diagnostics` fingerprint and a D0b verdict that
   is a function of coverage. Two independent routes to a FALSE CLEARANCE.
6. **AC-50 / AC-30** — the corrected d_min range, now needing AC-54's caveat (RD-131).
7. **RR-43** — one comment naming what `._` files are. `research-director` ruled the
   filter correct and staying: a name filter, not a platform branch.

**READ THE FINDING-CLUSTERS SECTION AT THE END OF THIS FILE BEFORE PLANNING ANYTHING.**
244 OPEN rows is a misleading count: twelve clusters hold ~70 of them and represent
**12 pieces of work**, several rows are the same defect found by three or four different
agents, and ~22 are already fixed and awaiting confirmation. The realistic
independent-work-item count is **30-40, not 244**. Plan against the clusters.

**Do this before ANY cycle-5 lane work — it is a one-line-scale change that prevents a
data-loss event: RR-80.** `docs/ledger_inbox/README.md` still instructs that inbox files
are emptied at the start of each cycle. ~130 ledger rows now name the inbox as their
primary record, so following that instruction deletes the substance of half the OPEN set.

**Read these first.**

1. **AC-54 is a confirmed blocker on the E1 dataset render.** GSound's effective
   absorption is 1−√(1−α); measured T30 0.5441 s against 0.343 s nominal, on a rule
   fixed before the run. Every closed form in `scenes/generator.py` and the
   `ir_duration`/record-length choices are computed for a room gsound does not render.
   Deciding this is upstream of most of the scenes-lane backlog.
2. **RD-33a condition (i) REGRESSED** and its reachability is now a USER decision
   (RD-128). Do not plan cycle 5 as though the gate moved: cycle 4 unblocked (ii) only.
3. **Two independent false clearances** (AC-45 cached, S-F1 live) let D0b print
   "CARRIER CEILING CLEARS — Proceed to E1" over evidence it never checked.

**≤1 scene of RD-17's ≤4-scene grant remains.** Two were spent here, both
pre-registered, both recorded in the integration record above. The 200,000-ray leg has
now executed exactly once (181.6 s, n=1, superlinear in budget) — a bare observation,
NOT a feasibility finding; RD-17 still owns that measurement.

### Folded rows — the remainder, ALL OPEN

Compact but REAL rows: the prose enumeration they replace was invisible to
`tests/test_lane_partition.py`, which parses `| ID |` rows only — so ~120 OPEN
findings sat outside the very check that exists to stop silent omissions (RD-88).
Each names its inbox and the id AS WRITTEN THERE (header remap table, RR-25).

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| F-M3 | falsifier (lane M) | major | OPEN | src/amcd/representations/spectrogram.py | AC-37's guard is a per-band MINIMUM over all 27 ladder bands, so it is a spectral-flatness test, not a level test, and its message blames the wrong cause. A 2nd-order 4 kHz lowpass — gentler than air absorption over a 4.25 s IR — trips it. Any spectrally sloped render (air absorption, frequency-dependent alpha, both roadmap items) raises. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-M3`. |
| F-M4 | falsifier (lane M) | major | OPEN | src/amcd/evaluation/room_acoustic.py; src/amcd/stats/aggregate.py | Residual pred-side selection on the DEPENDENT VARIABLE, with a new confound: pred leaves `paired_improvement` when it falls below the floor in a band the physical legs resolve, and the scenes removed are those where pred decays fastest — the largest |pred-high| errors — so `improvement_mean` is biased OPTIMISTIC. Censoring probability is now a function of room absorption, correlated with the `test_material_shift` axis. Zero coverage in the A/B. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-M4`. |
| F-M5 | falsifier (lane M) | major | OPEN | docs/lanes/cycle4-M.md pass condition; metrics/metrics.parquet | THE PASS-CONDITION A/B HAS NEAR-ZERO POWER OVER THE CODE IT VALIDATES. Of 60 (scene, ISO-metric) cells exactly ONE has `n_bands_resolvability_limited != 0`; `n_bands_pred_unresolved == 0` everywhere; no C50 leg is within 32.3 dB of AC-42's cliff; AC-37's guard raises on no scene. So '1 of 20 rows moved' is equally consistent with the change being correct and with 3 of 4 changes never having executed. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-M5`. |
<!-- F-M6: superseded by the corrected row in the integration falsifier section below; kept out of the OPEN table so the id is not duplicated (F-103). -->
| F-M8 | falsifier (lane M) | minor | OPEN | src/amcd/provenance.py; cf. src/amcd/evaluation/evaluator.py | F-69 attribution REFUTED as proven, CONFIRMED as contamination: 10 of the 27 files in the `eval` scope are AppleDouble `._*.py` sidecars and excluding them changes the digest. But writing an existing file does not rewrite its sidecar, so the intermittency mechanism is not pinned; sidecar CREATION is the live candidate. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-M8`. |
| F-M9 | falsifier (lane M) | minor | OPEN | src/amcd/evaluation/room_acoustic.py | Unexercised path that would silently lose a drop reason: `nan_reasons[(metric, leg)]` is written for a physical leg that caused a band exclusion, then unconditionally OVERWRITTEN for every leg floor-limited in a kept band. Needs a guard or test, not removal — no instance is constructible inside base.yaml's support. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-M9`. |
| F-M10 | falsifier (lane M) | minor | OPEN | src/amcd/representations/spectrogram.py; configs/representations/spectrogram.yaml | Evidence/config drift in the new AC-37 guard: the docstring says 'measured on a definitionally perfect oracle at the shipped 55 dB' while the shipped value is 50.0 and the test fixture uses 55.0. A measured claim stated against a value not in force. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-M10`. |
| F-M11 | falsifier (lane M) | minor | OPEN | src/amcd/data/normalization.py; src/amcd/data/preprocess.py | `low_mean`/`low_std` are computed and stamped into `preprocessed/meta.json` but never consumed — both legs are normalised with the HIGH stats. Emits output, contributes nothing. NOT leakage (train-only, verified). | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-M11`. |
| F-68-R2 | acoustics-reviewer (lane M) | major | OPEN | src/amcd/evaluation/room_acoustic.py `_band_energy` fold | The KNOWN RESIDUAL paragraph mis-describes what the fold does. Onset alignment puts the direct arrival ON the boundary, and the folded post-onset energy is +96.6 % at 500 Hz / +93.3 % at 1000 Hz against the same impulse in the record interior — a MAGNITUDE effect on the ISO early window, not a placement residual. Absolute C50 runs +0.06 to +1.67 dB high vs a causal reference, monotone in direct-dominance. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-68-R2`. |
| F-68-R3 | acoustics-reviewer (lane M) | minor | OPEN | src/amcd/evaluation/room_acoustic.py `_band_energy` | The fold double-counts one pad sample at each end (folded/full = 1.0000000015 vs exactly 1.0 without). Physically irrelevant at ~1.7e-28, but those two lines are the ONLY handling of the `head < guard` short-record branch, where pad samples are silently DISCARDED and energy is no longer conserved. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-68-R3`. |
| AC-37-R4 | acoustics-reviewer (lane M) | major | OPEN | src/amcd/representations/spectrogram.py | The headroom guard is calibrated on one band set and enforced on another: the limiting band is ALWAYS the 24.8 Hz band (in-band energy fraction 0.579, one the code's own docstring calls a Hann-leakage measurement) while the calibration rationale is about 500/1000 Hz OCTAVE quantities of the DECODED waveform. Fails conservatively, but operand and justification are different band sets. | See `docs/ledger_inbox/archive/cycle4-M.md` as `AC-37-R4`. |
| AC-26-R6 | acoustics-reviewer (lane M) | minor | OPEN | configs/base.yaml `metric_band_resolvability_margin` | The resolvability floor is a self-measured criterion, not the standard's, and does not say so. Margin 2.0x the filter's own decay gives 35.8 ms at 500 Hz; ISO 3382-2's BT > 16 requires 45 ms — the shipped floor is MORE permissive than the standard by ~9 ms. Not wrong, but a reader will take it for the ISO criterion. | See `docs/ledger_inbox/archive/cycle4-M.md` as `AC-26-R6`. |
| AC-19-R7 | acoustics-reviewer (lane M) | minor | OPEN | src/amcd/representations/spectrogram.py | Measured in-band fractions have drifted from the quoted ones (99.4/93.4/56.8 % quoted vs 0.9992/0.9464/0.5771 measured). 0.5-1.2 pp; no decision depends on it. | See `docs/ledger_inbox/archive/cycle4-M.md` as `AC-19-R7`. |
| AC-28-B2 | builder (lane M) | minor | OPEN | docs/review_ledger.md AC-28; src/amcd/simulators/dry_run.py | AC-28's DEFECT is fixed and verified through the real ISO path, but the ROW'S ACCEPTANCE CRITERION IS WRONG: 'C50 must fall ~6 dB per doubling and cross 0 dB near r_c' describes DRR, not C50, whose 50 ms early window contains large reverberant energy. Measured 4.91/3.37/1.62/0.58 dB per doubling, never below 0. Correct the criterion, not the scaffold. Also: AC-43's numbers are now stale (EDT spread 5.5 % -> 66.5 %). | See `docs/ledger_inbox/archive/cycle4-M.md` as `AC-28-B2`. |
| F-69-B4 | builder (lane M) | minor | OPEN | src/amcd/provenance.py | F-69 makes `test_editing_metric_code_changes_evals_version` fail in roughly half of full-suite runs on this host and pass in isolation. Fixed by lane P's `._` filter; recorded because the exact intermittency mechanism was only partly pinned. | See `docs/ledger_inbox/archive/cycle4-M.md` as `F-69-B4`. |
| RD-93 | research-director (lane M) | minor | OPEN | src/amcd/evaluation/room_acoustic.py (count-and-disclose vs the pred leg) | Plan-time finding, folded into lane M's implementation rather than deferred. | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-M.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-94 | research-director (lane M) | minor | OPEN | docs/review_ledger.md AC-38 (partial discharge — do not close) | Plan-time finding, folded into lane M's implementation rather than deferred. | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-M.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-96 | research-director (lane M) | minor | OPEN | src/amcd/evaluation/room_acoustic.py `channel_band_avg_metrics` (D0b policy freeze) | Plan-time finding, folded into lane M's implementation rather than deferred. | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-M.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-97 | research-director (lane M) | minor | OPEN | docs/lanes/cycle4-M.md (the A/B's own noise floor) | Plan-time finding, folded into lane M's implementation rather than deferred. | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-M.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-98 | research-director (lane M) | minor | OPEN | src/amcd/evaluation/room_acoustic.py (C50 inherits T30's verdict, not EDT's) | Plan-time finding, folded into lane M's implementation rather than deferred. | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-M.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-99 | research-director (lane M) | minor | OPEN | src/amcd/evaluation/room_acoustic.py (no hardcoded dB threshold to stay in-lane) | Plan-time finding, folded into lane M's implementation rather than deferred. | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-M.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-100 | research-director (lane M) | minor | OPEN | docs/lanes/cycle4-M.md (pre-registration of the expected ci_table movement) | Plan-time finding, folded into lane M's implementation rather than deferred. | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-M.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-101 | research-director (lane P) | major | OPEN | src/amcd/pipeline.py STAGE_FINGERPRINT / STAGE_UPSTREAM docstrings | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-93`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-102 | research-director (lane P) | minor | OPEN | src/amcd/pipeline.py FINGERPRINT_EXEMPT_FIELDS | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-94`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-103 | research-director (lane P) | minor | OPEN | tests/test_stage_cache.py TestEveryConfigFieldIsCoveredOrDeclaredExempt | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-95`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-104 | research-director (lane P) | minor | OPEN | src/amcd/provenance.py select_device | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-96`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-105 | research-director (lane P) | minor | OPEN | src/amcd/pipeline.py STAGE_CODE_SCOPE["preprocess"]; tests/test_stage_cache.py closure test | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-97`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-106 | research-director (lane P) | minor | OPEN | src/amcd/stats/aggregate.py; src/amcd/reporting/tables.py | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-98`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-108 | research-director (lane P) | major | OPEN | src/amcd/pipeline.py STAGE_FINGERPRINT["diagnostics"] | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-100`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-109 | research-director (lane P) | minor | OPEN | pyproject.toml; configs/ | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-101`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-110 | research-director (lane P) | minor | OPEN | docs/ledger_inbox/archive/cycle4-P.md (.git AppleDouble pack sidecars) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md` (written there as `RD-102`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-76 | falsifier (lane P) | major | OPEN | src/amcd/pipeline.py `_is_done` (legacy null-fingerprint sentinel) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-77 | falsifier (lane P) | major | OPEN | tests/test_stage_cache.py (import-closure walker dropped relative imports) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-78 | falsifier (lane P) | major | OPEN | tests/test_stage_cache.py (per-aspect `Seeds` sweep) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-79 | falsifier (lane P) | major | OPEN | src/amcd/provenance.py `_hashable_sources` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-80 | falsifier (lane P) | minor | OPEN | src/amcd/config.py `_require_configs` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-44 | acoustics-reviewer (lane P) | major | OPEN | src/amcd/pipeline.py:82-89 (_render_fingerprint), :283 (STAGE_CODE_SCOPE["preprocess"]), :322-326; src/amcd/evaluation/evaluator.py:104-110 | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-46 | acoustics-reviewer (lane P) | minor | OPEN | src/amcd/pipeline.py:436 (FINGERPRINT_EXEMPT_FIELDS["d0b_t30_jnd_frac"]); configs/base.yaml:203-207 | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-47 | acoustics-reviewer (lane P) | minor | OPEN | src/amcd/pipeline.py:291-294 (STAGE_CODE_SCOPE["eval"] rationale) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-48 | acoustics-reviewer (lane P) | minor | OPEN | src/amcd/reporting/tables.py:138-139, :48-51, :72-81 | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-46 | readability-reviewer (lane P) | minor | OPEN | src/amcd/pipeline.py (fingerprint docstring transcripts) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-47 | readability-reviewer (lane P) | minor | OPEN | src/amcd/pipeline.py `STAGE_UPSTREAM`, `_report_fingerprint` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-48 | readability-reviewer (lane P) | minor | OPEN | tests/test_stage_cache.py (test-class docstrings) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-49 | readability-reviewer (lane P) | minor | OPEN | src/amcd/provenance.py `code_version` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-50 | readability-reviewer (lane P) | minor | OPEN | src/amcd/pipeline.py `_DIAGNOSTICS_EXEMPTION` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-51 | readability-reviewer (lane P) | minor | OPEN | src/amcd/pipeline.py `FINGERPRINT_EXEMPT_FIELDS` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-52 | readability-reviewer (lane P) | minor | OPEN | src/amcd/pipeline.py `STAGE_CODE_SCOPE` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-53 | readability-reviewer (lane P) | minor | OPEN | tests/test_stage_cache.py (test-class naming) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-54 | readability-reviewer (lane P) | minor | OPEN | src/amcd/config.py (section banner) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-55 | readability-reviewer (lane P) | minor | OPEN | src/amcd/config.py `Config._check` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-56 | readability-reviewer (lane P) | minor | OPEN | src/amcd/config.py `stamp` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-57 | readability-reviewer (lane P) | minor | OPEN | src/amcd/provenance.py `select_device` | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-58 | readability-reviewer (lane P) | minor | OPEN | README.md (cache/--force documentation; cross-lane) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-59 | readability-reviewer (lane P) | minor | OPEN | tests/test_stage_cache.py (module docstring) | Raised on lane P's branch (provenance / stage cache / reported tables). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-P.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-112 | research-director (lane S) | major | OPEN | src/amcd/scenes/generator.py _disclose_and_gate_record_length | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RD-94`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-113 | research-director (lane S) | minor | OPEN | src/amcd/scenes/generator.py:399-405, 449; tests/test_acoustic_validity.py:115-121 | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RD-97`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| S-1 | falsifier (lane S) | minor | OPEN | src/amcd/acoustics.py | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| S-2 | falsifier (lane S) | minor | OPEN | tests/test_probe.py; tests/test_dataset_integrity.py:256 | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| S-F4 | falsifier (lane S) | minor | OPEN | src/amcd/scenes/generator.py _disclose_and_gate_record_length | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| S-F5 | falsifier (lane S) | minor | OPEN | src/amcd/scenes/generator.py gate denominator; tests/test_scene_placement.py RD-97 pin | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| S-F6 | falsifier (lane S) | minor | OPEN | src/amcd/scenes/generator.py per-split warning loop | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| S-F7 | falsifier (lane S) | minor | OPEN | src/amcd/scenes/generator.py _disclose_and_gate_record_length docstring | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-49 | acoustics-reviewer (lane S) | minor | OPEN | src/amcd/scenes/generator.py:27-32 (_C_TIMES_SABINE_K) vs src/amcd/acoustics.py:19 (SABINE_K) | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `AC-45`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-51 | acoustics-reviewer (lane S) | minor | OPEN | src/amcd/scenes/generator.py:338, 347-348, 357, 359-360, 371, 381, 385-386 | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `AC-47`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-52 | acoustics-reviewer (lane S) | minor | OPEN | tests/test_scene_placement.py:685-690 (test_eyring_is_the_stricter_criterion_at_high_absorption); docstring claim at :666-669 and src/amcd/scenes/generator.py:27-31 | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `AC-48`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-53 | acoustics-reviewer (lane S) | minor | OPEN | src/amcd/scenes/generator.py:288-306, :469-480 | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `AC-49`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-60 | readability-reviewer (lane S) | minor | OPEN | src/amcd/scenes/generator.py `_disclose_and_gate_record_length` | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-46`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-61 | readability-reviewer (lane S) | minor | OPEN | src/amcd/scenes/generator.py `_flag_counts` | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-47`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-62 | readability-reviewer (lane S) | minor | OPEN | src/amcd/scenes/generator.py _room_acoustics docstring and the AC-29 flag comment | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-48`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-63 | readability-reviewer (lane S) | minor | OPEN | src/amcd/diagnostics/probe.py D0b enumeration comment | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-50`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-64 | readability-reviewer (lane S) | minor | OPEN | src/amcd/diagnostics/probe.py — the per-split record schema | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-51`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-65 | readability-reviewer (lane S) | minor | OPEN | tests/test_scene_placement.py (module docstring) | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-52`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-66 | readability-reviewer (lane S) | minor | OPEN | tests/test_probe.py | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-53`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-67 | readability-reviewer (lane S) | minor | OPEN | src/amcd/scenes/generator.py (module docstring) | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-54`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-68 | readability-reviewer (lane S) | minor | OPEN | src/amcd/config.py GeometryFamily.characterization docstring | Raised on lane S's branch (scenes / QC / diagnostics). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-S.md` (written there as `RR-55`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-114 | research-director (lane R) | major | OPEN | configs/simulators/gsound_sir.yaml (render_python); src/amcd/simulators/render.py (_canonical_meta) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-93`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-115 | research-director (lane R) | major | OPEN | docs/lanes/cycle4-R.md render permission | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-94`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-116 | research-director (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py (_WORKER_SRC); tests/test_simulator_seam.py | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-95`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-117 | research-director (lane R) | major | OPEN | src/amcd/simulators/render.py (paths_{low,high}.parquet); ledger RD-23 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-96`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-118 | research-director (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py (_RECEIPT_NAME); scripts/setup_gsound_sir.py:78,300 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-97`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-119 | research-director (lane R) | major | OPEN | scripts/setup_gsound_sir.py:700; src/amcd/pipeline.py (F-65 exempt list); docs/verbosity.md; docs/gsound_sir_setup.md | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-98`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-120 | research-director (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py (path-file size; truncation_qc_flag consumer) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-99`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-121 | research-director (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py `_WORKER_SRC` (promote to its own module) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-100`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-122 | research-director (lane R) | major | OPEN | src/amcd/simulators/render.py (`paths_{low,high}.parquet` filename convention) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-101`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-123 | research-director (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py `_WORKER_SRC` (retention vs synthesis) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-102`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-124 | research-director (lane R) | major | OPEN | docs/gsound_sir_setup.md §4 (retention claim) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-103`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-125 | research-director (lane R) | major | OPEN | docs/gsound_sir_setup.md; scripts/setup_gsound_sir.py (`--sim-python`) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-104`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-82 | falsifier (lane R) | major | OPEN | configs/simulators/gsound_sir.yaml (max_discarded_tail_db); src/amcd/pipeline.py:79-92 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-106`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-83 | falsifier (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:481-492; configs/simulators/gsound_sir.yaml (max_discarded_tail_db: -60.0); configs/base.yaml:88 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-107`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-84 | falsifier (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:461-492, :494-517; src/amcd/simulators/render.py:146-160 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-108`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-85 | falsifier (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:121, :130 (_retain in _WORKER_SRC) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-109`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-86 | falsifier (lane R) | major | OPEN | src/amcd/simulators/render.py:21-28, :78-85; tests/test_simulator_seam.py:770-780 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-110`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-87 | falsifier (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:398-412 (_retention_args), :624-632 (PathRetention); tests/test_simulator_seam.py:744-746 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-111`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-88 | falsifier (lane R) | major | OPEN | src/amcd/simulators/base.py:139-155, :245-259; src/amcd/simulators/gsound_sir.py:566-593 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-112`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-90 | falsifier (lane R) | major | OPEN | src/amcd/simulators/render.py:159-183; src/amcd/simulators/gsound_sir.py:603-605 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-114`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-91 | falsifier (lane R) | major | OPEN | tests/test_simulator_seam.py:1064-1073; src/amcd/simulators/gsound_sir.py:433-443 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-115`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-92 | falsifier (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:635-650 (PathRetention), :125-126, :412 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-116`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-93 | falsifier (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:50-58, tests/test_simulator_seam.py:711-718 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-117`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-94 | falsifier (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:562, :494-517 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-118`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-95 | falsifier (lane R) | major | OPEN | src/amcd/simulators/base.py:157-216 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-119`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-96 | falsifier (lane R) | major | OPEN | tests/test_simulator_seam.py:940 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-120`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-97 | falsifier (lane R) | major | OPEN | docs/ledger_inbox/archive/cycle4-R.md (evidence section) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-121`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| F-98 | falsifier (lane R) | major | OPEN | src/amcd/simulators/render.py:154-157 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RD-122`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-55 | acoustics-reviewer (lane R) | major | OPEN | configs/simulators/gsound_sir.yaml:25-26 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `AC-41`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-56 | acoustics-reviewer (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:446-492 ; configs/simulators/gsound_sir.yaml:87 ; configs/base.yaml:45 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `AC-42`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-57 | acoustics-reviewer (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:50-58,602 ; src/amcd/simulators/base.py:294-301 ; tests/test_simulator_seam.py:711-718 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `AC-43`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-59 | acoustics-reviewer (lane R) | minor | OPEN | src/amcd/simulators/gsound_sir.py:186-195,605 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `AC-45`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-60 | acoustics-reviewer (lane R) | minor | OPEN | configs/base.yaml:113-118,131-132 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `AC-46`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-61 | acoustics-reviewer (lane R) | minor | OPEN | src/amcd/simulators/gsound_sir.py:185-195 ; src/amcd/evaluation/room_acoustic.py:129 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `AC-47`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-62 | acoustics-reviewer (lane R) | minor | OPEN | src/amcd/simulators/base.py:126 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `AC-48`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| AC-63 | acoustics-reviewer (lane R) | minor | OPEN | src/amcd/simulators/gsound_sir.py:178-179 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `AC-49`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-69 | readability-reviewer (lane R) | minor | OPEN | src/amcd/simulators/gsound_sir.py `_retention_args`, `PathRetention` | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-28`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-70 | readability-reviewer (lane R) | minor | OPEN | src/amcd/simulators/base.py:130-133,227-242 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-29`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-71 | readability-reviewer (lane R) | minor | OPEN | src/amcd/simulators/gsound_sir.py (_WORKER_SRC main()) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-30`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-72 | readability-reviewer (lane R) | minor | OPEN | tests/test_simulator_seam.py:1-10 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-31`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-73 | readability-reviewer (lane R) | minor | OPEN | docs/ledger_inbox/archive/cycle4-R.md (RD-100..RD-104) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-32`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-74 | readability-reviewer (lane R) | minor | OPEN | src/amcd/simulators/base.py:137,218-224 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-33`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-75 | readability-reviewer (lane R) | minor | OPEN | src/amcd/simulators/base.py:119-120 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-34`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-76 | readability-reviewer (lane R) | minor | OPEN | src/amcd/simulators/gsound_sir.py (_fit_to_window docstring) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-35`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-77 | readability-reviewer (lane R) | minor | OPEN | src/amcd/simulators/gsound_sir.py (_WORKER_SRC header) | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-36`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-78 | readability-reviewer (lane R) | minor | OPEN | tests/test_simulator_seam.py | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-37`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RR-79 | readability-reviewer (lane R) | minor | OPEN | README.md:139-169 | Raised on lane R's branch (render enablement, Steps 2+3). | Substance, measurements and probes in `docs/ledger_inbox/archive/cycle4-R.md` (written there as `RR-38`). Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-127 | research-director (lane INT) | major | OPEN | docs/review_ledger.md (cycle-4 integration record) | Raised by research-director against the INTEGRATION PLAN; see the plan and the integration record. | Substance, measurements and probes in `this file (integration record above)`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-130 | research-director (lane INT) | major | OPEN | docs/review_ledger.md (cycle-4 integration record) | Raised by research-director against the INTEGRATION PLAN; see the plan and the integration record. | Substance, measurements and probes in `this file (integration record above)`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-131 | research-director (lane INT) | major | OPEN | docs/review_ledger.md (cycle-4 integration record) | Raised by research-director against the INTEGRATION PLAN; see the plan and the integration record. | Substance, measurements and probes in `this file (integration record above)`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-132 | research-director (lane INT) | major | OPEN | docs/review_ledger.md (cycle-4 integration record) | Raised by research-director against the INTEGRATION PLAN; see the plan and the integration record. | Substance, measurements and probes in `this file (integration record above)`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-133 | research-director (lane INT) | major | OPEN | docs/review_ledger.md (cycle-4 integration record) | Raised by research-director against the INTEGRATION PLAN; see the plan and the integration record. | Substance, measurements and probes in `this file (integration record above)`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-134 | research-director (lane INT) | minor | OPEN | docs/review_ledger.md (cycle-4 integration record) | Raised by research-director against the INTEGRATION PLAN; see the plan and the integration record. | Substance, measurements and probes in `this file (integration record above)`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-135 | research-director (lane INT) | minor | OPEN | docs/review_ledger.md (cycle-4 integration record) | Raised by research-director against the INTEGRATION PLAN; see the plan and the integration record. | Substance, measurements and probes in `this file (integration record above)`. Not individually re-transcribed here — the inbox is committed and is the primary record. |
| RD-136 | research-director (lane INT) | minor | OPEN | docs/review_ledger.md (cycle-4 integration record) | Raised by research-director against the INTEGRATION PLAN; see the plan and the integration record. | Substance, measurements and probes in `this file (integration record above)`. Not individually re-transcribed here — the inbox is committed and is the primary record. |

### Rows the fold MISSED, added after the integration reviewer pass caught them

`research-director` (RD-INT2-1) measured the fold against its own printed accounting
and found it **5 short**, in the direction that makes coverage look complete: 70 carry-in
+ 149 claimed = 219, against 214 machine-visible. All five are live in their inboxes.
Verified individually before adding. The structural cause is its own row (RD-142): the
identity asserted is **ledger ↔ partition**, and nothing asserts **inbox ↔ ledger**, so a
finding absent from BOTH sides passes silently — RD-88's failure mode one level up.

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| AC-58 | acoustics-reviewer (lane R, was AC-44) | major | OPEN | src/amcd/evaluation/room_acoustic.py `_lundeby_truncate`; src/amcd/simulators/gsound_sir.py (the zero pad) | **THE SCHROEDER INTEGRATION LIMIT IS NOT GAIN-INVARIANT ON GSOUND IRs.** `_fit_to_window` always zero-pads, so the last 10 % of every record is EXACTLY zero, `noise_power` clamps to 1e-30 and the Lundeby threshold degenerates to a fixed ABSOLUTE 1e-29 instead of a noise-floor estimate. MEASURED over 10 decades of gain (1e-7 … 1e+3): `trunc_idx` moves 49115 → 52668, a **74 ms swing from gain alone**. ISO 3382 T30/EDT/C50 are ratios and must be invariant to a pure gain. Inert today (peak band energy ~1e-3 ≫ 1e-29) but `source_power` is a config knob and nothing guards the coupling. | **This is the OTHER HALF of the mechanism F-89 upheld by render, and the fold dropped it — while F-89's own resolution cell says "Do NOT close AC-58 on this row's evidence or vice versa."** Truncate at the last non-zero sample when the noise region is silent, recording that reason explicitly rather than falling through to an absolute level; add a gain-invariance known-answer test over several decades. Resolve WITH F-89 and RD-55. |

### Integration reviewer pass — NEW findings (gate step 6, the pass that counts)

Run on the merged tree at `2a89957` by all four reviewers. These are NEW; none duplicates
a row above. `readability-reviewer` returned no blocker and judged `metric_row.py`,
`provenance.py`, `configs/simulators/gsound_sir.yaml` and `PathData` the strongest
documentation in the repo. `research-director` returned **DRIFTING** — on the tracking
layer, not the work.

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| RD-142 | research-director | major | OPEN | tests/test_lane_partition.py; docs/ledger_inbox/*.md | **NOTHING ASSERTS INBOX ↔ LEDGER, so a finding missing from BOTH the ledger and the partition passes silently.** This is how the fold lost five rows including a blocker and a major (RD-INT2-1, now the five rows above). The existing check is ledger ↔ partition and is bidirectional and sound; it simply cannot see a finding that never became a row. RD-88's own failure mode one level up. | Add the missing direction: every `| ID |` in an inbox findings block must be an OPEN ledger row, or explicitly listed as folded-into-`<id>`. Cheap, and it is the only check that would have caught the fold's own defect. |
| RD-144 | research-director | minor | OPEN | docs/review_ledger.md AC-54 resolution; src/amcd/scenes/generator.py; configs/simulators/gsound_sir.yaml | AC-54's remedy is stated as a free binary — pre-compensate at the `createbox` call site OR re-derive every closed form in `scenes/generator.py` from α_eff — but **the second half writes ONE raytracer's energy/amplitude confusion into the backend-agnostic scene population.** `generator.py` defines the dataset's declared acoustics for EVERY simulator, so after that edit a second raytracer renders a different room from the same scene spec with nothing declaring it. Forecloses the roadmap's multiple-raytracers item. | Make the absorption convention a declared PROPERTY OF THE BACKEND in `configs/simulators/<name>.yaml`, applied at that backend's own call site; `generator.py` stays in nominal α. Same shape as the existing simulator seam. The choice is not free. |
| RD-145 | research-director | minor | OPEN | docs/review_ledger.md RD-33a row | The regression of condition (i) and the pending USER decision live in the integration record and in RD-128; **RD-33a's OWN row still reads as operationalized-and-stable** with no sign that either exists — and RD-33a's row is what a plan cites under CLAUDE.md. RD-76 was raised precisely because this gate's condition drifted while living in prose elsewhere. | One sentence in RD-33a's resolution cell pointing at RD-128 and the integration record. |
| RD-146 | research-director | minor | OPEN | docs/lanes/cycle5.yaml vs docs/parallel_protocol.md rule 4, gate step 2, planning step 3 | The staging partition puts all rows — including the ~22 that are FIX APPLIED, awaiting re-review — under `integrator_queue:` and zeroes `awaiting_re_review:`. The header prose says staging, but the machine-readable structure says "the integrator's serial queue, applied at gate step 2", and planning step 3 ("exclude fix-applied rows from every lane; assigning one invites a second fix on a first nobody checked") depends on exactly the distinction that has been emptied. | Keep `awaiting_re_review:` populated in the staging file, or add `staging: true` plus an `unassigned:` bucket the partition test counts alongside the other four. The fix-applied set must stay distinguishable before lanes are drawn. |
| RR-80 | readability-reviewer | major | OPEN | docs/ledger_inbox/README.md (the emptying instruction) vs the ~130 compact ledger rows | **AN INSTRUCTION IN THE REPO WOULD DESTROY THE SUBSTANCE OF HALF THE OPEN SET.** The inbox README still says "Files here are emptied at the start of each cycle, after the integrator has folded them in." That was true when the fold COPIED substance into the ledger. It is now false: ~130 ledger rows say the inbox IS the primary record, so emptying `{M,P,S,R}.md` when cycle 5 opens — which the README instructs — deletes the only readable record of half the OPEN set, leaving `git log -S` against a file whose id scheme differs from the ledger's. | A lane file may be emptied only once NO OPEN ledger row cites it; cycle-4 content is permanent and moves to `docs/ledger_inbox/archive/cycle4-<lane>.md` when cycle 5 opens. State the old-id/new-id caveat and point at the header remap table. |
| RR-81 | readability-reviewer | major | OPEN | docs/review_ledger.md (three OPEN tables under two organizing principles) | OPEN rows now live in **three tables governed by two incompatible schemes** — grouped by agent at the top, by consequence in the middle, by lane at the bottom — and the file states no rule for which table a row belongs in. A reader asking "what is OPEN on `evaluation/`" must scan four tables (including DEFERRED) and cannot tell whether they have seen them all. | Either a 6-line "How this file is organized" immediately after the header, or (preferred) merge the folded tables into the per-agent tables so there is exactly ONE OPEN table per agent, with the must-not-miss set as a list of ids in the resume note. |
| RR-82 | readability-reviewer | major | OPEN | README.md (repo map; Simulator note) | The repo map predates both the parallel-lane apparatus and the live render backend. The `docs/` line omits `parallel_protocol.md`, `lanes/` and `ledger_inbox/` — ~1900 lines that are the primary record for ~130 ledger rows, with nothing in README pointing at them; `scripts/` names only `setup_gsound_sir.py` while `new_lane.py`, `lane_guard.py` and `lane_preflight.py` now exist. And cycle 4 made `GsoundSirSimulator.render` live, but README gives no invocation: nothing says a real render needs a host layer setting `simulator.params.render_python`, so a newcomer on an x86 box cannot get from README to a render. Distinct from RR-24, whose open half is the `src/` map. | Add the three docs entries and the three lane scripts; add a fourth `Run` example for the real backend with a host layer, pointing at `configs/simulators/gsound_sir.yaml`'s own worked block. |
| RR-83 | readability-reviewer | major | OPEN | src/amcd/simulators/gsound_sir.py `_WORKER_SRC` | A complete 140-line program — four functions, its own docstrings and imports — lives as a raw string inside another module, splitting the module's `#:`-documented constant block from the class it documents. No highlighting, no linter, no type checker, no jump-to-definition, and `grep` for `_retain` or `main` returns a string body. **The stated reason ("this lane may not create a new file") no longer holds** — lane ownership dissolved at merge. | Promote to `src/amcd/simulators/_gsound_worker.py`, loaded by path at render time, preserving the property that matters (it imports no `amcd` and runs under a foreign interpreter). Lane R's own RR-73/RR-77 then apply to a real file. |
| RR-84 | readability-reviewer | major | OPEN | src/amcd/simulators/base.py `SceneSpec` | The dataclass every stage passes around is the only public type in this module with **no docstring and no units**. `dims`/`source_pos`/`receiver_pos` carry no unit and no frame convention; `material_absorption` carries no domain, no statement that it is a single scalar applied to ALL SIX surfaces (a fact both RD-28 and AC-54 turn on) and no frequency-independence note; `seed` does not say which aspect it derives from. `PathData` two hundred lines below documents every field with units — the contrast is the finding. | Class docstring naming the coordinate frame and that a scene is a config-sampled specification, not a render; `#:` lines giving metres, the [0,1] domain + all-six-surfaces + frequency-independent scope on `material_absorption`, and the deriving aspect on `seed`. |
| RR-85 | readability-reviewer | major | OPEN | src/amcd/evaluation/room_acoustic.py `compute_room_acoustic_metrics` | The module's primary public function **documents a 2-tuple return while returning FOUR values**. `window` is mentioned obliquely 25 lines later and `band_accounting` is never named in the docstring at all — though it is the structure carrying `n_bands_kept`, `pred_unresolved_hz` and `resolvability_limited_hz` into `ci_table.csv`'s caveat columns. The module docstring gets it right, so the two disagree. `window`'s units/format are stated nowhere (keyed by `f"{fc:g}"`, valued `(sample index, source-leg name)`). | Rewrite Returns as a 4-item list matching the signature, with units. The AC-08/AC-25/AC-17 paragraphs are contract and stay. |
| RR-86 | readability-reviewer | minor | OPEN | src/amcd/simulators/gsound_sir.py; src/amcd/provenance.py; tests/test_metrics.py | Package code and tests explain design decisions by citing a **defunct cycle-4 lane boundary** — "this lane may not create one", "lane P's cycle-4 file ownership does not include one", "a blocker this lane shipped" (in a merged tree: which lane?). A reader in cycle 6 cannot evaluate a rationale whose premise no longer exists. | Replace each with the durable half plus a ledger id. |
| RR-87 | minor (readability) | minor | OPEN | docs/review_ledger.md (render transcript); docs/lanes/cycle5.yaml (`reason:`); docs/parallel_protocol.md (line-number citations); CLAUDE.md "Parallel lanes"; src/amcd/evaluation/room_acoustic.py `_iso3382_band_metrics`; ledger resolution boilerplate | Five smaller clutter findings folded into one row: the render-measurement transcript is a THIRD copy of numbers the AC-54/F-89 rows already carry; `cycle5.yaml` repeats one 74-character `reason:` 214 times where the key exists to record per-row information; `parallel_protocol.md` cites `tables.py:144`, already stale after cycle 4 (RR-27's defect class in the file that governs every cycle); CLAUDE.md's "Parallel lanes" section restates all five protocol rules including file lists and a ledger id, in the one file whose length is a running cost; and `_iso3382_band_metrics` is ~207 lines of which ~90 narrate AC-38/AC-39/AC-42, including a 15-line account of a float32-residue implementation **that no longer exists in the file** and is duplicated in `tests/test_metrics.py`. | Each is a small independent edit; grouped so they are one decision, not five drive-bys. Name functions rather than line numbers in the protocol. |

### Integration falsifier pass — NEW findings, and two rows it CORRECTED

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| F-99 | falsifier | major | OPEN | src/amcd/evaluation/room_acoustic.py (C50 resolvability entry; the pred suppression gate) | **AC-42'S C50 GUARD IS INERT IN THE EXACT REGIME IT WAS WRITTEN FOR.** pred's C50 is suppressed only where NEITHER physical leg is T30-floor-limited — but that condition is what CREATES the C50 resolvability entry, so the guard fires iff pred is degenerate AND both physical legs are healthy. Where the physical legs ARE floor-limited (α 0.85-0.98, small rooms = `ceiling_absorptive` = **`test_material_shift`'s declared axis**) a degenerate pred's unbounded C50 is scored into `pred_mean` and `pred_ci_*`. MEASURED: physics T60 0.050 → pred NaN (guard fires); 0.030 → NaN; **0.020 → pred C50 = 199.8 scored**. Lane M's own evidence used healthy legs only, and F-M5 already recorded that AC-42 has zero coverage in the canonical run. | **COMPOSED WITH F-M2 THIS IS THE CYCLE'S MOST CONSEQUENTIAL RISK: an unbounded reported number with no caveat**, because the one column that would flag the band (`n_bands_resolvability_limited`) never reaches `ci_table.csv`. Lands on `test_material_shift`, so the first real-render E1 table is where it surfaces. Probe that confirms or kills it: run the canonical pipeline against a corner overlay (3×3×2.4 m, α ∈ [0.85, 0.98], `distance_range` [1.0, 2.0] — the config F-M5 already asks for) and assert no `metric == "C50"` row has `abs(pred_val) > 60` while `n_bands_resolvability_limited > 0`. |
| F-100 | falsifier | major | OPEN | src/amcd/pipeline.py `_gen_scenes_fingerprint`, `_render_fingerprint`; src/amcd/simulators/render.py `_HOST_SCOPED_PARAMS` | **CONFIRMS AND WIDENS F-81: `gen-scenes` is host-dependent too, and it is the CHAIN ROOT.** Both fingerprints embed `config.simulator.params` whole, including `render_python`. MEASURED: the two fingerprints differ on the single leaf `simulator.params.render_python: None → '/opt/…/bin/python'`. `_HOST_SCOPED_PARAMS` exists and is tested ONLY against `meta.json`; nothing asserts the CACHE KEY is host-neutral. Because gen-scenes is the chain root, the WHOLE chain refuses on the second host and the only remedy is `--force` — the multi-hour emulated re-render, i.e. exactly the compliance failure `code_version`'s own rationale cites. | Fix with F-81/F-82 in one change. Test: `_render_fingerprint(a) == _render_fingerprint(b)` and the same for `_gen_scenes_fingerprint`, for two configs differing only in `render_python`. |
| F-101 | falsifier | major | OPEN | src/amcd/diagnostics/probe.py (paired residual; JND threshold); src/amcd/evaluation/room_acoustic.py `channel_band_avg_metrics` | **D0b'S CARRIER-CEILING VERDICT CAN BE DECIDED BY BAND COMPOSITION RATHER THAN ACOUSTICS.** The residual is built from a NON-band-intersected unit that also applies the resolvability floor per leg, so oracle (low-ray carrier) and reference (raw high-ray) can average over DIFFERENT band sets. MEASURED at identical true T60 = 0.045 s with only different noise realizations: oracle averages [1000] while reference averages [500, 1000], giving a residual of 0.00244 from band composition alone. The oracle sits on the noisier carrier, so the asymmetry is DIRECTIONAL. AC-08 closed exactly this in eval; D0b never got it, and RD-96 covers only the AC-38 policy divergence. | Band-intersect across legs in D0b as eval does (AC-08). Third independent route to a false D0b clearance, with AC-45 (cached) and S-F1 (per-metric attrition) — resolve the three together. |
| F-102 | falsifier | minor | OPEN | src/amcd/pipeline.py (the warn call site inside `run_stage`) | The unprotected-staleness warning (F-75) is INVOCATION-scoped: it fires only if the unprotected stage is itself visited. `amcd report --force -r <dir>` regenerates `report/summary.txt` from stale renders with no warning at all. Partially mitigated by `versions.json`'s `code_version_describes`. | Fold into RD-107's implementation: warn on the chain's unprotected ANCESTORS, not only the stage being run. |
| F-103 | falsifier | minor | OPEN | tests/test_lane_partition.py `_open_ledger_ids`; docs/lanes/cycle5.yaml | **THE COVERAGE IDENTITY CANNOT DETECT A DUPLICATE LEDGER ID** — `_open_ledger_ids` returns a SET, and a duplicated id is exactly the failure cycle 4 hit four ways. Separately, with `lanes: []` the two structural partition tests iterate an empty list, so they are currently non-discriminating. The identity is not weakened; it is under-powered in the one dimension this cycle proved is real. | Assert the parsed id LIST has no duplicates before setifying. Pairs with RD-142 (nothing asserts inbox ↔ ledger) and RD-126 (no id-allocation rule) — three holes in one accounting machine. |
| F-105 | falsifier | minor | OPEN | src/amcd/stats/aggregate.py `bootstrap_ci` | Sharpens F-M7 analytically: at n=3, P(bootstrap mean == a sample extreme) = 3⁻³ = 3.7 % > α/2 = 2.5 %, so the requested percentile lands INSIDE the extreme atom and **the reported endpoint is decided by `bootstrap_n_resamples`, not by the data**. MEASURED on the same three paired improvements: `n_resamples=1000 → ci_lower −0.0572915`; `n_resamples=2000 → −0.0730212`. A 27 % change in a reported bound with the data fixed. | Take with F-70 and F-M7's small-n guard. |

**Two rows this pass CORRECTED — recorded rather than quietly edited:**

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| F-M6 | falsifier (lane M) | minor | OPEN (narrowed) | src/amcd/pipeline.py `_preprocess_fingerprint`; `STAGE_FINGERPRINT["diagnostics"]` | **THE FIRST HALF IS REFUTED ON THE MERGED TREE.** F-M6 claimed `_preprocess_fingerprint` carries no `code_version`, so AC-37's guard is bypassable through the preprocess cache. It DOES carry one, scoped `("data","representations","simulators/base.py")` — verified directly, and editing `representations/spectrogram.py` moves it, so a cached `preprocess` is refused. **Lane M measured on a branch that did not yet have lane P's F-64 fix** — a pure cross-lane artifact, and precisely what integration exists to find. | NARROWED to its live second half: `diagnostics` still has no fingerprint, and the D0b oracle is the artifact `min_db_headroom_db = 50.0` was CALIBRATED FROM, so the calibration evidence itself can be served stale. That half is AC-45 / RD-108. |
| F-M7 | falsifier (lane M) | major | OPEN (re-scoped) | src/amcd/stats/aggregate.py `bootstrap_ci`; src/amcd/reporting/tables.py | **RE-SCOPED, and the scope matters.** The finding is CONFIRMED and now analytically explained (F-105), but it is a **dry-run / RI-smoke artifact, not an E1 defect**: `configs/base.yaml` declares 30/30/40 scenes per shift split, where the bootstrap is well behaved. The integrator's own step-4 A/B evidence (mean moved, CI bit-identical) is real but is n=3 evidence. | Still worth fixing — the smoke and dry-run tables are read and quoted — but it does NOT mean the E1 CIs are affected, and the row previously implied it did. Label the columns uncalibrated below n=4 rather than presenting a 95 % CI. |

**Also corrected:** lane M's inbox claims "the average's composition is unchanged by the
disclosure". False on the merged tree — `scene_0022`'s EDT went `n_bands_kept` 1/2 → 2/2
and its `low_val` moved 0.010607 → 0.011365, i.e. the floor-limited band is now INSIDE
the average. That is the mechanism behind F-M2, and it means AC-38 changed a reported
value as well as a caveat.

### CORRECTION to the F-89 verdict I committed in `b5af7b9` (raised by AC-64)

**I withdraw the explanatory half of that verdict.** The commit said the measured
truncation indices "sit well INSIDE native support (~930-970 ms), so Lundeby is finding a
real crossing, not falling through to the pad." That is **false at code level**, and — the
part that should have caught me — **it contradicts AC-58, which I filed OPEN in the same
commit**, and which says exactly the opposite: the noise floor degenerates to the 1e-30
clamp.

Verified independently: `_fit_to_window` always zero-pads, native support ends at
~44647-46544 samples (0.93-0.97 s), and `_lundeby_truncate` estimates its noise floor from
the **last 10 % of the record — samples 183600…204000, i.e. 3.83-4.25 s**, which is
entirely zeros. So `noise_power` is the fallback constant 1e-30 and the threshold sits
**278 dB below the band peak**. There is no measured noise floor to cross.

What survives, and what does not:

- **SURVIVES — the finding.** The indices were directly measured on real renders and they
  MOVED with the ray budget (Δ 1222 / 1480 samples against a 480-sample rule fixed before
  the run). F-89's conclusion, and the restatement AC-17 needs, are unaffected.
- **WITHDRAWN — my explanation.** Why the index lands at 600-625 ms, some 330 ms INSIDE
  the live record, is **predicted by no model currently on the table** — not
  "native + ringing" (which predicts ~994/963 ms and is what a synthetic reconstruction
  of the same geometry returns), and not a real noise-floor crossing. An unexplained
  measurement is a reason to worry MORE, not less, and I had written it up as reassurance.
- **MY PROBE DESTROYED ITS OWN EVIDENCE.** Both renders went through `build_simulator`
  in memory with no retained run_dir, so the artifact that would settle this is gone. The
  re-measurement must retain it and record, per band: `native_ir_samples`, the last
  non-zero sample of the raw IR, the band-energy peak, `noise_power`, and the index.

Why it matters beyond bookkeeping (AC-64's measurement): a 600 ms window under-reads T30
by **−43 % at T60 = 2 s and −63 % at 3 s**, and `ir_duration: 4.25` with
`max_t60_over_ir_duration_frac: 0.0` exists precisely to admit those scenes. Inert at the
0.544 s probe scene (0.007 %), which is why one scene could not see it.

**AC-54, by contrast, is STRENGTHENED.** AC-67 re-derived it from the pinned upstream
source with **no render at all** — `reflec = sqrt(1-α)` per bounce into an energy-domain
`1/(4π(1+d²))`, with the synthesizer taking `sqrt(intensities)`, gives α_eff = 1−sqrt(1−α)
exactly — so the one-scene/one-realization objection does not bear on the conclusion. Two
caveats it adds: the competing explanation my pre-registration failed to name is
`IR_THRESHOLD` + `ADAPTIVE_IR_LENGTH` (both on), which is what makes `native_ir_samples`
differ between budgets rather than the 3.0 s cap; and the 500-vs-1000 Hz T30 spread of
**8.4 %, ~1.7× `d0b_t30_jnd_frac`**, is unexplained on a frequency-independent-α room.

---
## DECIDED: RD-128 and RD-143 (2026-08-11)

Both were routed to the user as decisions. The user's ruling was that neither needs to be:
*"It's not an issue if some issues are blocking other issues from being solved, just make
whatever that issue is a higher priority."* Correct, and both are now taken by the builder.

**RD-143 — the queue order. DECIDED: reordered.** Item 0 is backend renderability
(AC-54 + AC-55 + AC-56 as one decision), item 0b is the retained-artifact re-measurement.
Already written into the resume note above. No further arbitration needed: a blocker that
gates other work is simply scheduled first.

**RD-128 — RD-33a condition (i). DECIDED: SCOPE BY SEVERITY.**

Condition (i) reads "zero OPEN rows anchored in `src/amcd/scenes/**`,
`src/amcd/evaluation/**`, `config.py` split handling, `configs/*.yaml` split
declarations". Measured today that is **59 rows**, and the finding rate on that path list
exceeds the closure rate — so as literally written the gate is a bar that recedes, and the
720-scene render never happens. That is not a research decision so much as an arithmetic
one, so:

> **(i) lifts when zero OPEN BLOCKER or MAJOR rows are anchored in the path list.**
> Minors on the path list must each be explicitly DEFERRED with a reason and a gate —
> not merely outnumbered. **Today: 4 blocker + 16 major = 20 rows to clear** (was 59).

Why this is the honest reading rather than a relaxation:

- The bar it replaces was never satisfiable in practice. RD-33 already drifted once for
  exactly this reason; RD-76 narrowed it and it drifted again. A third restatement of an
  unreachable condition is not discipline, it is theatre.
- A minor is by this project's own definition a row whose consequence does not reach a
  reported number. Requiring zero of them before a render conflates *tidiness* with
  *validity*, which is what let the ledger grow while the gate stood still.
- The severity of every row is set by the raising reviewer, not by the builder wanting
  to render — so this cannot be gamed by re-labelling. If a reviewer calls something
  major, it blocks.

**This does NOT lift RD-33a.** Four blockers sit on the path list right now (AC-54,
AC-42-B1, AC-42-R1, F-M1) and condition (ii) — the RD-17 probe — has not run.

---
## FINDING CLUSTERS — what is actually one defect (2026-08-11)

244 OPEN rows is a misleading count, and the user was right to ask for this. Four lanes ×
four reviewers ran **16 review passes** in one cycle against a serial cycle's ~4, and the
same defect was frequently found by several agents from different angles. Those are not
244 pieces of work.

**Rule for these clusters: close the whole cluster together, or none of it.** A cluster
closed halfway is the F-M2 shape — a fix applied while the thing that made it visible is
still missing. Where one row CORRECTS another, both stay until the correction is confirmed.

| # | cluster | rows | what it really is |
|---|---|---|---|
| **C1** | the AC-42 C50 guard | `AC-42-B1` `AC-42-R1` `F-M1` **= ONE defect, three independent raisers** (builder, acoustics, falsifier). Successor: `F-99` | The first guard was a bogus "float32 residue" that censored the PHYSICAL legs. **Three agents deriving the same blocker independently is the strongest signal in the cycle.** Fix applied; F-99 says the REPLACEMENT is inert in the exact regime it was written for, so the cluster is NOT closed. |
| **C2** | render/gen-scenes are not cache-protected | `F-75` `RD-107` `AC-44` | One defect, three angles: falsifier reproduced it, research-director framed the policy call, acoustics showed the blast radius (the ISO **reference** leg goes stale, not just "the renders"). One fix. |
| **C3** | the render fingerprint carries non-IR params | `F-81` `F-82` `F-100` | One edit site. F-81 = host-scoped `render_python` (cross-platform blocker), F-82 = a disclosure-only threshold, F-100 confirms both and widens to `gen-scenes`, the chain root. |
| **C4** | D0b prints a false clearance | `AC-45` `RD-108` `S-F1` `F-101` `F-M6`(narrowed) | **Five rows, one artifact, four INDEPENDENT routes to the same wrong verdict**: stale from cache, unfingerprinted, blind to per-metric attrition, and decidable by band composition. Fixing any one still leaves "CARRIER CEILING CLEARS" untrustworthy. |
| **C5** | the Schroeder truncation window on real renders | `F-89` `AC-58` `AC-64` `RD-55` | One physical question. RD-55 predicted it at E4 and was DEFERRED; F-89 measured it; AC-58 gives the mechanism; AC-64 refutes my explanation of it. **RD-55 is no longer deferrable** — a real render has now reached it. |
| **C6** | is the declared population renderable at ISO fidelity? | `AC-54` `AC-55` `AC-56` `AC-66` `AC-67` `RD-144` | research-director's ruling (RD-143) plus the two acoustics rows that arrived after it. One decision, taken once, in the BACKEND's config. |
| **C7** | the reported caveat column | `F-M2` `AC-38` `F-70` + RD-65's report half | One column-plumbing change in `stats/aggregate.py` + `reporting/tables.py`. AC-38 must not close before it. |
| **C8** | the small-n CI | `F-M7` `F-105` | One guard. F-105 is F-M7's analytic explanation, not a separate defect. Both re-scoped to dry-run/smoke. |
| **C9** | the row-accounting machine | `RD-126` `RD-142` `F-103` `F-104` | Three holes in one machine plus the miss they let through: no id-allocation rule, nothing asserts inbox↔ledger, and a duplicate id is undetectable. Fix as one change to `test_lane_partition.py` + the protocol. |
| **C10** | the AC-37 headroom guard | `AC-37-R4` **=** `F-M3` (same finding, two raisers) · `AC-37-R5` (fix applied) · `F-M10` | The guard's operand is a spectral-flatness test over all 27 bands while its calibration is about 500/1000 Hz octave quantities. |
| **C11** | d_min / scene-acoustics disclosure | `AC-30` `AC-50` `AC-49` `AC-51` `AC-52` `S-1` | AC-50 CORRECTS AC-30's own numbers; the rest are the same disclosure block in `generator.py`. **Now gated on C6** — every number here is nominal-α. |
| **C12** | docstrings that re-tell bug reports | `RR-32` `RR-36` `RR-37` `RR-38` `RR-86` `RR-87` and much of `RR-46…RR-79` | One editing pass with one rule (contract + ledger id; evidence lives in `git log -S`). This project's most-repeated readability finding, now in its third cycle. |

**What this does to the count.** The twelve clusters above hold **~70 of the 244 rows** and
represent **12 pieces of work**, not 70. Of the remainder, the great majority are `RR-*`
rows inside C12's single editing pass, and ~22 are FIX APPLIED / awaiting re-review — i.e.
already done, pending confirmation. **The realistic independent-work-item count is closer
to 30-40 than to 244.**

**On the growth itself, stated honestly.** Three different things are mixed in it, and only
one is research complexity:

1. **A new surface became visible.** `GsoundSirSimulator.render` worked for the first time
   this cycle. AC-54, AC-55, AC-56, AC-66 and the whole C5 cluster were **unreachable
   before** — you cannot find that gsound's effective absorption is 1−√(1−α) until gsound
   runs. This is the good kind of growth: it is Research I's assumptions meeting a real
   raytracer, and finding it now is worth far more than finding it in an E1 table.
2. **Sixteen review passes instead of four.** Parallelism multiplied the reviewing, not
   just the fixing. Much of the apparent growth is the same defects found more times —
   which is exactly what the clusters above collapse.
3. **Bookkeeping churn that is NOT research complexity** — the id collisions, my fold's
   own defects, the anchor backfill. C9 exists to stop that recurring.

### From the backlog-verification pass (2026-08-11)

`research-director` re-derived its 15 fix-applied rows: **15/15 CONFIRMED FIXED, deleted.**
`readability-reviewer` re-derived its 6: **5 confirmed and deleted, RR-24 NOT FIXED** (3 of
4 clauses met; the `src/` map still omits `provenance.py`, `acoustics.py` and `runtime.py`,
and `README.md`'s "see Config, not code" pointer is stale — the section is
"Configuration is the source of truth"). RR-24 stays OPEN with that residual named.

Two rows carried a residual that is a NEW finding rather than an unfixed half, plus two
small items surfaced while confirming others:

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| RD-147 | research-director | minor | OPEN | docs/lanes/cycle5.yaml `integrator_queue`; docs/review_ledger.md resume note | **THE SERIAL QUEUE IS DECLARED TWICE AND ONLY THE PROSE COPY CARRIES THE DECISION.** `cycle5.yaml` lists every OPEN row alphabetically under one identical `reason:`, so AC-54 — item 0 — sits at position ~30; RD-143's ordering, item 0, item 0b and the "drop item 6" ruling exist only in the resume note. A cycle-5 planner reading the machine-readable partition, which the protocol tells it to read, gets **the pre-render order RD-143 was raised to overturn**. RD-92's failure mode (a second declaration that drifts) in the file RD-88 made authoritative. | Give `cycle5.yaml` an ordered `serial_queue:` for items 0, 0b and 1-7 and leave the rest `unassigned:`; repopulate `awaiting_re_review:` (RD-146); name the clusters. **Do this before drawing any cycle-5 lane** — otherwise lanes get drawn from a 244-row list whose true content is 30-40 work items, i.e. from the ledger rather than from the gate, which is the RD-81 mistake cycle 4 was replanned to avoid. |
| RD-148 | research-director | minor | OPEN | tests/test_lane_partition.py (`_CURRENT_CYCLE`, the skip branch) | `_CURRENT_CYCLE` is a module literal and nothing asserts it names an existing partition file. If it ever names a missing stem, `pytest.skip` fires for every parametrization and **the coverage identity is silently suspended with a green suite** — RD-88's own "asserted by nothing" shape, one level up. | One assertion that `docs/lanes/<_CURRENT_CYCLE>.yaml` exists and is the newest partition, so a forgotten bump fails loudly. |
| RR-88 | readability-reviewer | minor | OPEN | tests/conftest.py (`tiny_config` docstring); CLAUDE.md (the leading HTML comment) | Two small items surfaced while confirming RR-29 and RR-34, recorded so they are not lost with the deleted rows. (a) `tiny_config`'s docstring still says "base + test_tiny" while it composes THREE layers — the same pre-reorg understatement RR-29 was raised for, 46 lines lower. (b) `CLAUDE.md` opens with an HTML comment addressed to whoever installed the file ("this replaces the … portion of *your existing* CLAUDE.md"). It is a setup instruction, not a rule, and it is read every session. | (a) "(base + simulator_dry_run + test_tiny)". (b) Delete the comment. |
| RD-149 | research-director | minor | OPEN | docs/parallel_protocol.md gate step 4 | Confirming RD-91 showed the durable half holds but the protocol states the expected-effect declaration as a CYCLE-4 FACT ("all three declare none"), and nothing asserts that cycle-5 briefs re-declare it. The detector's discriminating power would quietly become an assumption again. | Make it a requirement on every cycle's briefs, and assert it in `tests/test_lane_partition.py` alongside RD-126's block check. |

### Backlog verification, parts 3 and 4 (2026-08-11)

`falsifier` re-derived its 8 rows: **8/8 CONFIRMED FIXED**, all deleted. It tried to break
each and could not — the attacks that failed are worth knowing: forcing a double-count in
F-67's mirror fold (ratio never exceeded 1.0 across 16 fc×duration cells), reaching F-61's
near-field population through a declared sub-millimetre bound (rejection sampling raises
first), and finding a physical-leg C50 censored anywhere in the declared T60 support (none).

`acoustics-reviewer` re-derived its 4: **AC-40 and AC-37-R5 CONFIRMED and deleted;
AC-42-R1 confirmed AS SCOPED and deleted; AC-27 REFUTED.**

**Two corrections made BEFORE deleting, because the git history of a deleted row is this
project's audit trail and stale numbers in it become the permanent record of what a fix
bought:**

- **F-67's resolution cell did not reproduce.** It claimed "EDR step at t=0 now −0.076 dB"
  and "MDES 0.0281". Measured over all 116 (scene, leg, band) cells of a fresh canonical
  run the worst step is **−0.1418 dB**, and MDES is **0.0306** (0.0281 was the
  `cycle4_baseline` value, moved by later lane changes). The load-bearing claim —
  `n_scored` back to 3 with a finite MDES — reproduces. Corrected here, then deleted.
- **AC-27's resolution claimed "floors reproduce exactly". They do not.** Measured through
  `_band_resolvable_decay_s` at 48 kHz: 500 Hz T30 **20.3597 ms** / EDT **9.5561 ms**;
  1000 Hz **10.1619 / 4.8018 ms**, against a docstring declaring 17.881/11.765 and
  8.924/5.771 — **+13.9 % T30, −18.8 % EDT**. The 1/f claim does hold (f·T30 = 9.88-10.18
  over 125-4000 Hz, and breaks outside it: 8000 Hz → 9.26). **AC-27 is deleted as a
  DUPLICATE of AC-65**, which is the surviving instance of the very defect AC-27 named —
  wrong physics written down in the source — and which stays OPEN.

**One row explicitly NOT deleted, against its own resolution cell.** `AC-42` asks that "a
degenerate pred must leave C50.pred unscored with a reason, not +203 dB". **That acceptance
test FAILS on the current tree** at physics T60 ≤ 0.025 s — measured `C50.pred = +207.08 dB`
SCORED at T60 = 0.025, 0.020 and 0.0179 s, all inside base.yaml's declared support and on
`test_material_shift`'s own α ∈ [0.85, 0.98] axis. AC-42-R1 (the censoring DIRECTION) is
genuinely fixed and deleted; AC-42 (the guard's PURPOSE) is not. The existing regression
test uses 0.30 s legs, i.e. it sits entirely inside the regime where the guard fires and
has no coverage of the inert one. **F-99 is the live successor and both agents reproduced
it independently** (204.118 dB and 207.08 dB).

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| AC-69 | acoustics-reviewer | major | OPEN | src/amcd/representations/spectrogram.py (`_check_min_db_headroom`); tests/test_filterbank.py; tests/test_metrics.py | **THE AC-37-R5 PROPERTY HAS ZERO REGRESSION COVERAGE — a one-token revert is invisible to CI.** Every test exercising the headroom guard encodes a SINGLE-CHANNEL IR, where `amax(dim=2)` and the pre-fix `amax(dim=(0,2))` are identical. DEMONSTRATED: the reviewer monkeypatched the guard back to the old semantics and the suite still passed. Compounding it, `tests/test_metrics.py` still computes its own reported headroom with the OLD `dim=(0,2)` form — the wrong definition sitting next to the guard as a template. | Add the reviewer's probe as a test: 4-channel field, W attenuated 70 dB, channels 1-3 native; assert `encode` RAISES and names channel 0. Boundary confirmed at exactly the declared 50.0 dB (W att 22 dB → accept, 24 dB → reject). Fix the stale `dim=(0,2)` in the test file too. |
| AC-70 | acoustics-reviewer | minor | OPEN | src/amcd/evaluation/room_acoustic.py (the comment naming the reported path) | The file contradicts itself about which function IS the reported path: one comment calls `channel_band_avg_metrics` "the reported path", while the module docstring and the code both have it as the single-IR probe unit and `compute_room_acoustic_metrics` as the reported one (confirmed against `evaluator.py`, which loads `<scene>_decoded_ir.npy` and calls the latter). Physics unaffected; the naming is what a future reader acts on. | One-line correction. |
| F-106 | falsifier | minor | OPEN | tests/test_metrics.py (AC-40's parametrization) | AC-40's known-answer padding test is parametrized over the module constant `_ISO = [500.0, 1000.0]`, **not** over `config.iso_eval_freqs`. They are equal today, so the test has teeth now — but a change to the declared band set would not reach it, which is the coupling AC-40 exists to prevent. | Parametrize from the config. |
