# Review ledger

**Unresolved findings only.** One row per finding:
`ID | agent | severity | status | anchor | finding | resolution`.

Status is **OPEN** or **DEFERRED** (out of scope for the current gate, with a
reason and the gate it belongs to). There is no resolved state: a finding that is
fixed is DELETED. Git is the audit trail — `git log -S '<id>' -p -- docs/review_ledger.md`.

This file exists so unsolved work survives a lost context window. **That is its
only job.** It is not a record of process: no cycle narration, no pass counts, no
rows about the ledger itself or the machinery around it. A row that changes
neither what the code does nor what a reported number means does not belong here.

**Nothing is added without the user's approval.** A finding that cannot be fixed
is raised as a research-limitation decision, so the project can move past it —
never filed for a later session to inherit and ignore.

**A cited row id that is not in this file is a finding that was fixed and
deleted.** It is kept where it names a defect SHAPE a reader already knows
("the AC-24 divergence shape" = two declarations of one fact drifting apart).
`git log -S '<id>' -p` recovers it. A citation may stay ONLY where the sentence
carries its own content; where it is the sole carrier of a defect or an action,
inline the fact instead.

**Write in-cell pipes as `&#124;`.** A raw `|` inside a cell truncates the row at
that point, and the text after it is gone from the file.

## research-director

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| RD-18 | research-director | major | OPEN | plan Step 4 (QC) vs docs/research_I_paper.md:480,503; docs/design_spec.md:217 | v3 implements only 2 of Research I's 4 QC criteria, and `base.yaml` min_energy_db −60.0 is ~40 dB stricter than RI's 1e-10 (~−100 dB re 1.0). Missing: non-empty retained-path file, max retained-path file 128 MB (declared fixed at design_spec:217 but ABSENT from config = hidden default). QC governs dataset admission and E1 is "reproduce the old null", so a silently different admission rule confounds the reproduction. The onset check is a PAIRED low-vs-high mismatch (RI l.480), not an absolute bound. | The two missing CONFIG KEYS — `max_path_file_mb` and a declared energy reference (dB re 1.0 FS) — are declarable today, cheaply, and should not wait for Step 4. The criterion implementations and the `max_onset_ms` → `onset_mismatch_tolerance_ms` rename belong to Step 4. Update design_spec l.217 + §6 QC-record row, not just the base.yaml comment. |
| RD-65 | research-director | minor | OPEN | src/amcd/scenes/generator.py `_disclose_and_gate_record_length` | The AC-22/RD-56 gate is the OVERALL over-limit fraction — the one aggregation invariant #9 forbids for results. research_i's 0.01 over 720 scenes permits 7 over-limit scenes that could ALL sit in the 30-scene `test_geometry_shift` (23 % of that split) and still pass, while the per-shift breakdown IS the research result. Per-split counts reach `placement_report.json` but surface only when the gate trips. | Keep the overall gate (its reasoning is right — a per-split gate lets the smallest split set the tolerance for train). ADD an always-emitted per-split WARNING naming any split whose OWN over-limit fraction exceeds the declared limit, and carry per-split counts into the E1 report table, not only the JSON. |
| RD-20 | research-director | minor | OPEN | src/amcd/pipeline.py, runtime.py, cli.py stage dispatch | Widening stage dispatch for a runtime-only value is a nine-stage-plus-tests touch; the roadmap makes a third foreseeable. | Introduce one frozen `RunContext(verbosity, host)` in runtime.py; dispatch as `(config, run_dir, ctx)`. Do it while the dispatch signature is being touched for the worker anyway. |
| RD-55 | builder (follow-on to RD-44) | minor | OPEN | src/amcd/evaluation/room_acoustic.py | Reported ISO absolutes are a function of the ray budget, because the shared Schroeder window is set by the leg whose record ends first and that record length moves with the budget. An E4 'metric vs ray count' curve would mix genuine convergence with a window that shrinks as the budget drops. | Implement Lundeby extrapolated-tail compensation (ISO 3382-1 Annex): estimate the decay slope over the admissible range and integrate the modelled tail beyond the record's end, so the limit stops being 'where this leg happened to stop'. Closes F-89 with it. Paired improvement — everything E1/E2 report — is unaffected and does not wait on this; budget-independent ABSOLUTES do. |
| RD-109 | research-director (lane P) | minor | OPEN | pyproject.toml; configs/ | `src/amcd/config.py` searches a packaged `amcd/configs/` FIRST and that directory does not exist — `configs/` ships only as a checkout sibling, and `pyproject.toml` declares no package data for it. A wheel install therefore resolves configs by a path that is only correct in a source checkout, which is the second-host portability the project requires. | Either move `configs/` under `src/amcd/` and declare it as package data in `pyproject.toml`, or delete the phantom first search path. Equally declinable as a decision — but the phantom path must not stay. |
| RD-186 | builder (lane M) | minor | OPEN | `configs/base.yaml` (`iso_eval_freqs` block, `:233`) + `src/amcd/config.py`; shipped form at `tests/test_metrics.py:736` (`_DECLARED_STOPBAND_DB`) | The octave filter's class-1 non-conformance is declared but its rejection threshold is a test-file literal rather than config; moving it to config, which cannot be done in one lane: `configs/base.yaml` is lane M's but its schema is `src/amcd/config.py` (lane P) with `extra: forbid`, so a new key fails validation. The measured bound it would carry is −37.43 / −38.49 dB at one octave out and −46.59 / −47.33 at two (500 Hz), shipped instead as `_DECLARED_STOPBAND_DB = {1: -36.5, 2: -45.5}` in the test file, argued as a declared property of the filter design rather than an experiment-governing value. | md` documents. Integrator/lane P: add the schema field and move the octaves-out → min-rejection mapping into base.yaml, or ratify the test-pinned form and close AC-68's config clause explicitly. AC-68 stays open until one of the two happens. |
| RD-187 | builder (lane M) | minor | OPEN | `src/amcd/representations/base.py:55` (`build_representation`; "sample_rate is the sole cross-cutting arg" at `:60`) | `min_db_headroom_octave_centres_hz` (`configs/representations/spectrogram.yaml:112`) is a SECOND declaration of the evaluation band set, `iso_eval_freqs` (`configs/base.yaml:233`) being the first — the AC-24 divergence shape, admissible only while `test_the_headroom_guard_reads_exactly_the_reported_metric_bands` (`tests/test_metrics.py:596`) forbids drift by EQUALITY, not containment. It exists solely because `build_representation` takes `sample_rate` as its only cross-cutting argument, so a representation cannot read the master config. | Give `build_representation` the evaluation band set as a second cross-cutting argument and derive the guard's operand from it, retiring the duplicate declaration; touches `data/preprocess.py` and `training/infer.py` (lane P) and `diagnostics/probe.py` (lane S). Until then the equality test is the only thing holding the two declarations together and must not be weakened to containment. |
| RD-188 | builder (lane M) | major | OPEN | `src/amcd/data/preprocess.py` (lane P) | F-M3's other half, and a disclosure REGRESSION. A minimum across frequency is a spectral-flatness constraint; restricting the headroom guard's operand to the two reported octave centres (500 / 1000 Hz) removes the false rejection but does not DISCLOSE the slope, so a strongly sloped render — the row's own 2nd-order 4 kHz lowpass case — now passes SILENTLY where it previously failed loudly. Supporting measurement from the falsifier pass: `preprocess.py` calls `rep.encode` and contains NO `except` anywhere, so an over-rejection aborts the run loudly and cannot silently drop scenes or bias split composition. | Not lane M's file. Lane P: write a per-scene spectral-slope disclosure into `preprocessed/meta.json`, and add the test the falsifier pass identified — pinning that `preprocess.py` has no `except` around `rep.encode`, since that absence is what makes "err toward rejecting" not a selection effect. |

## falsifier

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| F-70 | falsifier | minor | OPEN | src/amcd/evaluation/room_acoustic.py `compute_room_acoustic_metrics` | **AC-25 TRADED A GROUND-TRUTH CONTAMINATION FOR A SELECTION ON THE DEPENDENT VARIABLE, AND ONLY THE FIRST IS DISCUSSED.** The censoring probability correlates with room absorption — i.e. with `test_material_shift`'s own declared axis — so the loss is not uniform across splits. A pred NaN in ONE physically-kept band makes the metric NaN for pred in EVERY band, so that scene contributes nothing to `paired_improvement` — the reported improvement CI is conditioned on the model having produced a measurable value, i.e. model failures are removed from the estimate of how much the model helps. COUNTED (`n_pred_band_unresolved`) but not BOUNDED, and the direction is optimistic. Inert under dry_run today; live exactly where AC-37 says the decode floor first bites. | Keep the rule — it is the right fix for the contamination. ADD the bound: report the paired improvement over the full attempted population with pred-unscored scenes imputed at zero improvement, alongside the scored-population CI. Test: a run with one pred-unresolved scene must show both. |
| F-89 | falsifier (lane R, was RD-113) | major | OPEN | src/amcd/evaluation/room_acoustic.py `_lundeby_truncate`, `_shared_truncation_per_band`; src/amcd/simulators/gsound_sir.py (the zero pad) | **THE SHARED TRUNCATION INDEX TRACKS THE RAY BUDGET.** Measured on one scene at both budgets: index delta 1222 samples (25.5 ms) at 500 Hz and 1480 (30.8 ms) at 1000 Hz, against a 480-sample tolerance fixed before the run, with the HIGH budget giving the SHORTER index. The mechanism is now settled and is not a noise floor: a gsound record is zero-padded from its native length, so the index lands where the backend stopped writing, and that native length is itself budget-dependent (+5.4 % for a 40x change, same room and seed). Sharing the window removes this from the PAIRED comparison; it does not remove it from a reported ABSOLUTE. | Close with RD-55 — one fix, extrapolated-tail compensation, and this row is the measurement that justifies it. Do NOT close it by sharing the window: that is already done and is what makes paired improvement safe, while leaving every absolute a function of the low leg's ray budget. Until then no absolute T30/EDT/C50 may be compared against Research I or the literature, only within a pair. |
| S-2 | falsifier (lane S) | minor | OPEN | tests/test_probe.py; tests/test_dataset_integrity.py:256 | Probe coverage is split across two files: `TestD0bEnumeratesDeclaredSplits` in `tests/test_dataset_integrity.py` and the rest in `tests/test_probe.py`, so a reader asking what covers the probes must find both. | Consolidate into `tests/test_probe.py`. |
| F-83 | falsifier (lane R) | major | OPEN | src/amcd/simulators/gsound_sir.py:481-492; configs/simulators/gsound_sir.yaml (max_discarded_tail_db: -60.0); configs/base.yaml:88 | `truncation_qc_flag` is structurally unable to fire under `base.yaml`. The flag fires only when the native IR exceeds the window, and the backend's compiled 3.043 s ceiling is below `ir_duration: 4.25`, so `_fit_to_window` always takes the pad branch. It IS live under `configs/research_i.yaml`, whose `ir_duration` is 3.0 s. | Either derive the threshold from a criterion independent of `ir_duration`, or state at the flag that it is a backstop for over-limit-tolerant configs only. Test: exercise the firing case through a CONFIG, not a hand-built array. |
| F-101 | falsifier | major | OPEN | src/amcd/diagnostics/probe.py (paired residual; JND threshold); src/amcd/evaluation/room_acoustic.py `channel_band_avg_metrics` | **D0b'S CARRIER-CEILING VERDICT CAN BE DECIDED BY BAND COMPOSITION RATHER THAN ACOUSTICS.** The residual is built from a NON-band-intersected unit that also applies the resolvability floor per leg, so oracle (low-ray carrier) and reference (raw high-ray) can average over DIFFERENT band sets. MEASURED at identical true T60 = 0.045 s with only different noise realizations: oracle averages [1000] while reference averages [500, 1000], giving a residual of 0.00244 from band composition alone. The oracle sits on the noisier carrier, so the asymmetry is DIRECTIONAL. AC-08 closed exactly this in eval; D0b never got it, and RD-96 covers only the AC-38 policy divergence. | Band-intersect across legs in D0b as eval does. It is the LAST of the three routes to a false D0b clearance — the cached-verdict route is closed (diagnostics is fingerprinted) and the per-metric-attrition route is closed (the verdict now requires `d0b_min_scored_frac` coverage) — and alone it still leaves "CARRIER CEILING CLEARS" untrustworthy. |
| F-M7 | falsifier (lane M) | major | OPEN | src/amcd/stats/aggregate.py `bootstrap_ci`; src/amcd/reporting/tables.py | It is a **dry-run / RI-smoke artifact, not an E1 defect**: `configs/base.yaml` declares 30/30/40 scenes per shift split, where the bootstrap is well behaved. The integrator's own step-4 A/B evidence (mean moved, CI bit-identical) is real but is n=3 evidence. | Still worth fixing — the smoke and dry-run tables are read and quoted — but it does NOT mean the E1 CIs are affected. Label the columns uncalibrated below n=4 rather than presenting a 95 % CI. |
| F-116 | falsifier | minor | OPEN | src/amcd/simulators/render.py:67-107, :204-216; src/amcd/pipeline.py:76 `_render_fingerprint`; tests/test_simulator_seam.py:763-777 | F-90's digests are written and never verified by any pipeline code. `grep -rn artifact_sha256 --include=*.py` shows exactly one writer (render.py:97, :206) and no reader outside the tests; `test_a_corrupted_artifact_no_longer_matches_its_provenance` recomputes the digest itself. The cache-reuse path admits a cached render on a config fingerprint alone and never touches the digests, so the row's own rationale ("`rng_seeded: false` puts reproducibility on the cached artifacts and they carried no integrity check") is half closed — the record exists, the check does not: a number in canonical output contributing nothing to the inferential result. Adjacent: `_canonical_meta`'s `artifact_sha256: dict \| None = None` defaults to `{}` (:97), indistinguishable from "this render wrote nothing" (reached today only at test :1059). | py`, lane P's file. Verify the digests where a cached render is REUSED, or state in the docstring that this is a provenance record with no verifier; make the `None` default raise rather than render `{}`. Needs cross-lane scheduling with C3's `_render_fingerprint` work. |
| F-117 | falsifier | minor | OPEN | src/amcd/simulators/base.py:207-232 (`to_parquet`); src/amcd/simulators/render.py:206 | The parquet digests are library-version-scoped, so F-90's record is not host-portable for the path artifacts. PROBE: every file `to_parquet` writes embeds `created_by: parquet-cpp-arrow version 24.0.0`, so the same logical PathData written under a different pyarrow yields a different sha256. The `.npy` digests ARE portable (`np.save` pins dtype and endianness). Same defect class as the AppleDouble sidecar bug — a host/toolchain fact entering canonical provenance — one layer down, and not covered by the "only what the stage wrote" guard, which fixes the file SET, not the file BYTES. | Either state at `_sha256` that a digest is a within-host integrity check and not a cross-host identity, or digest the logical content (arrays + descriptor) rather than the container bytes. |
| F-118 | falsifier | minor | OPEN | src/amcd/simulators/render.py:139-146, :181-206 | A stale artifact inside a RETAINED scene dir survives pruning and is invisible to provenance. Pruning removes whole orphan scene DIRS whose name is not in `current_ids`; it never removes stale FILES inside a dir that is kept, and F-90 digests only `written`, so the stale file is absent from `artifact_sha256` too. PROBE: plant `renders/scene_0000/paths_low.parquet` before a dry_run render → afterwards files on disk are ['high.npy','low.npy','meta.json','paths_low.parquet'] while `artifact_sha256` keys are ['high.npy','low.npy']. A backend switch (paths-exporting → not) or a retention change therefore leaves a readable path file no meta.json describes and no prune removes. | Prune unexpected files inside a retained scene dir, or record the (unit, reason) for every file present but not written. Confirming test is the probe above, asserting the stale file is gone or is reported. |
| F-122 | falsifier | minor | OPEN | src/amcd/simulators/base.py:184-198, :265-270 | The declared PathData dtype is enforced on read but not on write, so a float64 array is silently narrowed to float32. Widening that: F-95's cast is value-destroying and for some inputs warning-free. PROBES at default numpy settings — float64 [3.7, -1.0, 4.29e9] → uint32 [3, 0, 4290000000] (a RuntimeWarning to stderr, no (unit, reason)); float64 [0.0, nan, 1.5] → uint64 [0, 0, 1], so a corrupt value becomes a valid-looking source index 0; int64 [-5, 1, 2] → uint64 [18446744073709551611, 1, 2] with NO warning at all. `test_the_declared_dtype_is_the_dtype_that_gets_written` (:588) asserts dtype conformance only, on exactly-representable values, so no test covers a lossy cast. Round-trip itself is faithful (all 8 arrays + descriptor + a None `kept_energy_percentage` verified identical). Smaller hole: `from_parquet` (:265-270) raises a bare `TypeError` if a non-nullable scalar is null. | Preferred: raise on a narrowing/lossy cast, and make the non-nullable-null case raise the same explanatory `ValueError` the missing-descriptor case does. Close the two rows together. |
| F-123 | falsifier | minor | OPEN | src/amcd/simulators/gsound_sir.py:37-48, :141, :149 | `_RECEIPT_NAME` and `_RECEIPT_SHA_KEY` are dead, and their 8-line comment asserts a contract that does not hold. `grep -rn '_RECEIPT_NAME\|_RECEIPT_SHA_KEY' src/ tests/ scripts/` returns only the two definition lines; the worker hardcodes `"amcd_gsound_install.json"` at :141 and `["commit_sha"]` at :149 inside the raw string. The comment explains at length why the duplication with `scripts/setup_gsound_sir.py` is deliberate, but changing either constant changes nothing — they are a third copy, not a single source of truth, while the comment makes them look load-bearing. | The constants are still dead. Decide: interpolate them into `_WORKER_SRC` (already a formatted literal elsewhere) or delete them and move the rationale to the worker's own lines; confirming test asserts `_RECEIPT_NAME in _WORKER_SRC`. |
| F-143 | falsifier (lane M) | minor | OPEN | `src/amcd/evaluation/room_acoustic.py:122-200` (`_butter_octave_filter`, the `butter(4, ...)` literal) + `src/amcd/config.py` | The octave filter's ORDER is an experiment-governing literal. The module's own docstring argues the order sets the ringing `_band_resolvable_decay_s` measures (floors T30 20.36 / 10.16 ms, EDT 9.56 / 4.80 ms at 500 / 1000 Hz), so steeper skirts buy selectivity with a longer unresolvable floor — a research trade that decides which bands carry the AC-38 caveat into reported metrics. CLAUDE.md allows CLI, config or raise; it is a literal. `_DECLARED_FLOORS_48K` and `_DECLARED_STOPBAND_DB` were explicitly CLEARED as legitimate pins on measured OUTPUTS — the defect is the INPUT that produces them. | rule-4 spanning row (same shape as RD-186). Integrator/lane P: add a `metric_octave_filter_order` field to `src/amcd/config.py` (`extra: forbid` blocks lane M) and declare it in base.yaml, or state in the docstring why the order is fixed by the ISO band definition rather than tunable. |
| F-161 | falsifier | major | OPEN | src/amcd/provenance.py:130-145 `code_version`, :46-49 `_CORE_SOURCES`; src/amcd/config.py stamp comment | A COMMENT-ONLY EDIT INVALIDATES EVERY FINGERPRINTED STAGE. `code_version` hashes raw bytes (`digest.update(path.read_bytes())`), so a comment is indistinguishable from a semantic change, and config.py is in `_CORE_SOURCES`, i.e. every scope. MEASURED v3-rebuild vs lane/P-cycle5: all six stage `code_version`s moved; `stats` (scope `("stats","evaluation")`) moved although NOTHING in stats/ or evaluation/ changed — solely because RR-56 trimmed a config.py comment. provenance.py:118-122 argues for scoping because irrelevant refusals teach `--force`; comment granularity is that failure one level down. Bounded only while the dry run is seconds. | Render and gen-scenes NOW carry a `code_version` (`pipeline.py` `_BACKEND_SCOPED_STAGES`), so a comment-only edit ALREADY forces the multi-hour emulated re-render — this is live, not prospective. Either hash semantics (`ast.dump(ast.parse(src))`, docstrings stripped) or price the forced re-render into RD-107. TEST: invariant to an inserted comment, moves on any executable change; then re-run the F-63/F-64/F-66/F-77 mutation probes — all four must still refuse. |
| F-162 | falsifier | minor | OPEN | src/amcd/reporting/tables.py `_stamped_value_domain`; src/amcd/pipeline.py:221-232 `_report_fingerprint`, :287 `STAGE_CODE_SCOPE["report"]` | REPORT READS A PREPROCESS ARTIFACT IT DOES NOT DECLARE. PROBE: copy a complete run_dir, flip preprocessed/meta.json `value_domain` db→amplitude, `amcd report --force` → EXIT 0 with every `energy_mse` row relabelled over numbers computed in dB, while `energy_snr_db` stays SCORED and labelled dB — impossible under amplitude, where evaluation/signal.py NaNs both SNR legs. The realistic route IS protected (verified: a config-driven rep change is refused through five chain links), so this is an undeclared dependency covered incidentally by chain transitivity, not a live wrong number. | Declare preprocessed/meta.json as an input of `report`, and cross-check the stamp against the rows being labelled (a scored `energy_snr_db` row is impossible under amplitude). |
| F-166 | falsifier | minor | OPEN | src/amcd/reporting/tables.py `_metric_row`, footer | THE TABLE NOW DISCLOSES THE UNIT AND STILL NOT THE KIND. `kind` decides whether `Imp mean` is pred−low (`maximize`) or \|low−high\|−\|pred−high\| (`match_reference`). In the canonical run `energy_snr_db` (−47.8947, maximize) and `C50` (−0.6738, match_reference) sit in one column, both labelled dB. `kind` IS already carried in stats/summary.json, ci_table.csv and metrics_table.csv — it is dropped exactly at the human-readable artifact. Partially mitigated by the AC-128 legend, which states the two meanings but not which applies per row. | Add a `Kind` column; the value is already in the row dict, so this is report-local and does NOT move ci_table.csv. |
| F-167 | falsifier | minor | OPEN | src/amcd/pipeline.py:558-565 (`_effective_fingerprint` upstream leg) vs :606-620 (`_is_done`) | F-76'S GUARD WAS APPLIED TO ONE OF THE TWO LEGS. PROBE: forge `{"fingerprint": null}` into stages/stats.done of a populated run_dir, `amcd report --force` → "Stage 'report' depends on 'stats', which has not completed in \<dir\> (no readable fingerprinted sentinel) … running 'report' now would record a provenance chain for artifacts that do not exist." Both claims are FALSE: stats/ is fully populated and the stage DID complete; only its sentinel predates fingerprinting. `_is_done` handles the identical shape correctly, its comment saying the case is guarded "here rather than at the call site". Live population: any run_dir predating F-63. | Route `_recorded_fingerprint() is None` on the upstream leg to the same actionable message as `_is_done`, distinguishing "never ran" from "ran before fingerprinting". Lane-P-owned file; cross-lane at the time it was found. |
| F-215 | falsifier (integration) | major | OPEN | tests/test_simulator_seam.py:1096-1099 | **F-115'S FIX IS PINNED TO A HARDCODED HOST PATH.** `source = Path("/Volumes/T7/Monte_Carlo_Research/v3/external/GSound-SIR/.../Context.cpp")` is the ONLY `/Volumes/...` literal in `src/` or `tests/`. On a native x86_64 Linux/Windows desktop — a declared supported host — and in every lane WORKTREE of this same repo, the path does not exist, so `pytest.skip` fires and the anchor tying the declared band centres to pygsound's compiled set is INERT exactly where band identity could silently differ. Directly violates CLAUDE.md's portable-path rule; same class as F-168, which lane P fixed for host coupling in this same cycle. Two lines later the same test uses CWD-relative `Config.load(...)`, so one test carries two incompatible path conventions. | Derive from `Path(__file__).resolve().parents[1] / "external" / ...`, and assert the test does NOT skip on a host where the checkout is present. Lane B's file; cycle 6. |
| F-216 | falsifier (integration) | major | OPEN | src/amcd/simulators/base.py:336-337; ledger F-115 | **F-115 IS PARTIALLY FIXED — its own resolution asked the re-review to confirm the second half and the answer is NO.** `validate_path_descriptor` still checks `len(centres) != n_bands or len(edges) != n_bands - 1` and nothing else, so a descriptor whose `band_centres_hz` is a STRING of the right length passes, and an int raises `TypeError` rather than the shaped `ValueError`. The Params-level half IS closed (probe: `_gsound_sim(band_centres_hz='63125250')` → pydantic `list_type` ValidationError). | Type-check descriptor band values as sequences of numbers, not by `len()`. Confirming test: `validate_path_descriptor` with `band_centres_hz="12345678"` against 8 intensity columns must raise `ValueError`.  |
| F-217 | falsifier (integration) | major | OPEN | tests/test_stage_cache.py:654-666, :668-682 | **TWO TESTS WRITE TO TRACKED PACKAGE SOURCE.** `test_patching_denormalize_moves_eval_and_infer` and `test_the_package_init_is_in_every_scope` do `target.write_bytes(original + b"…")` on `src/amcd/data/normalization.py` and `src/amcd/__init__.py`, restoring in `finally`. Any hard interrupt, `pytest -x` kill or OOM leaves the package MODIFIED in the checkout; and for the test's duration the tree is dirty, so anything computing `provenance.git_dirty` / `code_version` concurrently records a false value — the field every evidence claim in this project is stamped against. | Compute the probe's `code_version` against a `tmp_path` COPY of the package (the `_module_file` pattern already makes this cheap), or hash a synthetic file list. Never mutate the checkout to test it. Lane P's file; cycle 6. |
| F-219 | falsifier (integration) | minor | OPEN | tests/test_metrics.py `_IR_DURATION_S = 4.25`, `_T30_JND_FRAC = 0.05`, `_SEEDS`, `_padded_decay(..., seed=...)` | EXPERIMENT-GOVERNING LITERALS BURIED IN A TEST FILE, with a `# configs/base.yaml` comment standing in for a read. Verified duplicated: `ir_duration: 4.25` at `configs/base.yaml:45` and `d0b_t30_jnd_frac: 0.05` at `:219`. Both govern the acceptance of these five tests; if either moves, all five tests silently measure the wrong record and the wrong JND. Neighbouring tests in this same file already do `Config.load(Path("configs/base.yaml"))`, so the correct pattern is present. CLAUDE.md: do not bury experiment-governing values as literals in test fixtures. | Read both from the config; declare the probe seed range as a named, config-sourced value. |

## acoustics-reviewer

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| AC-187 | acoustics-reviewer (integrator, RENDER-DERIVED) | blocker | OPEN | src/amcd/reporting/tables.py (footer caveat); configs/base.yaml `convergence.c50_db`, `high_ray_budget`; experiments/rd17_convergence/rd17_results.json; experiments/c50_noise_floor/c50_noise_floor.json | **THE 200,000-RAY REFERENCE LEG IS CONVERGED FOR T30 AND IS NOT CONVERGED FOR C50.** Probe at 200k vs 800k over 10 scenes through the production ISO path: **T30 16/18 scored cells within the declared 5 % (2 unscored, worst exceedance 7.0 %); C50 12/20 within the declared 1.0 dB, worst 3.24 dB — 3.2x the ISO JND.** Control for single-realization variance (nominally identical 200k/210k/220k budgets, which no convergence argument distinguishes; pygsound exposes no RNG seed): the 200k-vs-800k delta exceeds that spread by **3.5x to 21.2x in 5 of 6 cells**. The exception is the most anechoic cell at 500 Hz, where the spread is 2.038 dB — larger than the tolerance itself, so that cell is unassessable rather than converged. Ray-count cost is strongly superlinear: 30.5 s at 200k against 1067 s at 800k, 35x for 4x the rays. | **Do NOT loosen `convergence.c50_db` — it is the ISO JND, and moving it changes what 'converged' means to hide the result.** Every paired C50 improvement treats the high leg as ground truth and that premise is measured false, so C50 needs a stated caveat wherever it is reported (`reporting/tables.py` footer, keyed on `convergence.c50_db`). T30 is converged and carries the headline claims. Two things are NOT established and must not be assumed: that 800k WOULD converge — verifying it needs 3.2M rays, ~10 h per scene, which is not affordable — and that the deficit is uniform, since it grows with room size and is worst at 1000 Hz. Raising `high_ray_budget` is therefore a decision to spend ~9 days for an unverified reference, not a fix. E1 is unaffected either way: RI pins 200,000 (paper Figure 5), so faithfulness fixes the budget. |
| AC-37 | acoustics-reviewer | major | OPEN | src/amcd/representations/spectrogram.py (`min_db`, `decode`); consumed at src/amcd/evaluation/evaluator.py | **`min_db` BECOMES AN INJECTED ENERGY FLOOR IN THE DECODED PRED, AND THE REPORTED ISO METRICS HAVE ONLY ~26 dB OF ABSOLUTE-LEVEL HEADROOM.** `decode` rescales the carrier's band-frame power to `10**(env/10)`, and `env` is clamped at an ABSOLUTE `min_db`, so wherever true power is below the floor the decode BOOSTS the carrier up to it — injecting a non-decaying floor into the pred waveform INSIDE the shared Schroeder window. MEASURED with an ORACLE pred = `decode(encode(high), low)`, a DEFINITIONALLY PERFECT prediction: pred band energy exceeds high by +38.2 dB at t = 1.5 s and +152 dB at 3.0 s, with the Lundeby cut at 3.109 s. Scaling the same scene by a gain (high T30 = 0.7750 s throughout): oracle-pred T30 error +0.00 % at 0 dB, +1.48 % at −25, **+24.4 % at −30, +411 % at −35, +935 % at −40**; at −60 dB EDT reads 8.80 s against 0.806 s and C50 −12.8 dB against +4.68 dB. CONTROL at `min_db: -200`: ≤ +0.38 % everywhere — so the model, carrier, filter and Schroeder path are all innocent. `d0b_t30_jnd_frac` is breached at ~−27 dB of level. **INERT under dry_run** (worst \|ΔT30\| = 0.25 % over every declared corner) because the scaffold's level convention is `direct = 1/d` with a room-constant tail — so this will FIRST APPEAR on the emulated gsound render RD-33a gates, the most expensive place to find it, and it will look like a model failure rather than a representation constant. AC-33 declared the reference and judged it inert on dynamic-range grounds; this is the consequence that measurement did not reach. | An unexercised path producing a physically wrong result on VALID input — it needs a GUARD, not removal. **USER DECISION 2026-08-10: remedy (a) GUARD + (c) SWEEP; NOT (b).** (b) — a per-scene-relative decode floor — was rejected because it changes the DECODE contract, i.e. the Research II energy-domain reframe itself, and would move every decoded waveform on top of lane M's other metric changes, muddying the single fixed-seed A/B M is judged on. **(a) is assigned to lane M** and belongs in `spectrogram.encode` (fail loud when a scene's per-band peak sits closer than a config-declared N dB to `min_db`) — NOT in `data/preprocess.py`, which lane P owns and which would make the row unfinishable in its lane (RD-83's check caught this). **(c) is on the integrator queue**: the D0b level sweep needs `diagnostics/probe.py` (lane S). **KNOWN-ANSWER TEST first, it is remedy-independent, cheap, and needs no render: `decode(encode(high), low)` must reproduce high's T30 within `d0b_t30_jnd_frac` at gain ∈ {0, −20, −30, −40} dB.** |
| AC-41 | acoustics-reviewer | minor | OPEN | src/amcd/scenes/generator.py `_room_acoustics` vs src/amcd/simulators/dry_run.py `render` | **THE α CLIP DIFFERS BETWEEN THE ROOM THE REPORT DESCRIBES AND THE ROOM THE SCAFFOLD RENDERS** — the exact divergence AC-24 exists to prevent, on a second variable. `_room_acoustics` clips α to [1e-6, 1−1e-6]; `dry_run.render` clips to [0.01, 0.99]. Both then feed `acoustics.room_constant`. MEASURED (5×4×3 m): identical at α ≤ 0.99, but α = 0.995 → published R = 18706 m² vs rendered 9306 m², a **+3.03 dB gap** between `placement_report.json`'s `drr_db` and the DRR the scaffold realizes (r_c 19.29 vs 13.61 m); α = 0.999 → +10.04 dB. The T60 clip is recorded (`rt60_clipped`); the α clip is recorded nowhere. Not reachable at base/research_i's α ≤ 0.98; a config raising `ceiling_absorptive` reaches it silently. | Share ONE clip (or one guard) between the two call sites, as AC-24 did for `SABINE_K`, and stamp `alpha_clipped` into `IRResult.meta` beside `rt60_clipped`. TEST: at α = 0.995 the scaffold's `expected_drr_db` must equal the report's `drr_db`, or the scene must raise. |
| AC-43 | acoustics-reviewer | minor | OPEN | src/amcd/simulators/dry_run.py (the "NOT MODELLED" note) — completes AC-28 | The missing early-reflection cluster is an acceptable scaffold simplification, but it leaves **EDT nearly inert on the placement axis** and nothing says so. Because the diffuse tail begins at the direct arrival, the first 10 dB has no reflection structure. MEASURED at 10×8×3.5 m, α 0.2: EDT = 0.7504 / 0.8056 / 0.7828 / 0.7803 / 0.7805 s at d = 0.5/1/2/4/8 m — NON-MONOTONE, 5.5 % spread — against C50's monotone swing over the same 16× range. In a real room EDT moves systematically with distance. So AC-28 made C50 live on the placement axis and left EDT essentially dead there; `test_placement_shift`'s EDT column is a plumbing result, not an acoustic one. | AC-28's own unfinished clause ("state in the D0a/D0b artifacts that the placement axis is not exercised"), now NARROWED from "the axis" to "EDT on the axis" and measured. State it in the D0a/D0b artifacts and the E1 report. Do NOT add an early-reflection model to the scaffold — that is the real simulator's job. |
| AC-67 | acoustics-reviewer (integration) | minor | OPEN | ledger AC-54 (evidence basis); external/GSound-SIR `SoundMesh.cpp:223`, `gsSoundPropagator.cpp:1531,1578,4518`, `auralizer/src/cpp/binding.cpp:417,454` | **AC-54 IS CONFIRMED FROM SOURCE, WHICH REMOVES THE "ONE SCENE, ONE REALIZATION" OBJECTION — AND THE RENDER'S RESIDUAL IS STILL UNEXPLAINED.** Independent derivation, no render: `createbox` sets `reflec = sqrt(1-α)` (`SoundMesh.cpp:223`); `diffuseAttenuation *= getReflectivityBands()` per bounce (`:1531`); `getDistanceAttenuation` is `1/(4π(1+d²))`, an INTENSITY (`:4518`); and the synthesizer takes `sqrt(intensities)` to get pressure (`binding.cpp:417,454`). So pressure ∝ (1/d)·(1-α)^(n/4) → the per-bounce ENERGY factor is sqrt(1-α) → **α_eff = 1-sqrt(1-α)**, exactly. The 1/d law coming out CORRECT is what forces it: the two cannot both be right. AC-54's pre-compensation algebra also checks out — `sqrt(1-(1-(1-α_t)²)) = 1-α_t`. TWO CAVEATS THE ROW SHOULD CARRY. (a) A competing explanation the pre-registration did not name: `IR_THRESHOLD` and `ADAPTIVE_IR_LENGTH` are both ON (`Context.cpp:19,21`), so the native IR length is set adaptively by an energy threshold — that, not the 3.0 s `maxIRLength` cap, is why `native_ir_samples` differs between budgets (44647 vs 46544), and it prunes the tail the T30 fit sits on. None of it, nor `diffuse_depth` 100 (≈ 0.74 s of path, against a −35 dB point at 0.336 s), nor air absorption (AC-66, ≤ 0.4 %), can move a 1.8× discrimination, so the verdict stands. (b) The 500-vs-1000 Hz spread — 0.5660 vs 0.5222 s, **8.4 %, ~1.7× the project's own `d0b_t30_jnd_frac`** — is unexplained on a room with frequency-independent α, and it was measured on ONE realization of the 5,000-ray leg. That is an unbudgeted single-realization T30 uncertainty which the pre-registered rule did not account for; it is recorded here because RD-55 will need it. | Restate AC-54's evidence as SOURCE-DERIVED with the render as confirmation, so the row does not stand or fall on one scene. Add the two caveats. When AC-54 is fixed, the known-answer test is the direct path, not T60: render one scene and check that direct-arrival ENERGY falls 6.02 dB per doubling of d (already the case) AND that T30 matches Eyring at the DECLARED α to a stated tolerance. |
| AC-50 | acoustics-reviewer (lane S, was AC-46) | major | OPEN | configs/base.yaml (`distance_range` justification); src/amcd/scenes/generator.py | **THE d_min RANGE AC-30 ASKS THE INTEGRATOR TO WRITE INTO base.yaml IS ITSELF WRONG.** the earlier [0.41, 5.16] m d_min range is the `mixed`-regime span only (α ≤ 0.80); `ceiling_absorptive` declares α ∈ [0.85, 0.98] on the SAME shoebox family, and `test_material_shift` selects exactly that regime. Over every geometry × material corner: Sabine **[0.412, 5.712] m**, Eyring **[0.417, 11.413] m**, max at 12×10×5 m, α = 0.98. Writing 5.16 would install a fresh understatement of 1.11× (Sabine) / 2.20× (Eyring) on the split whose realized shortfall is 92.5–100 %. Caught independently by lane S, its falsifier and its acoustics-reviewer. | Write the full product range, both variants. **AND NOW: these are NOMINAL-α numbers, which AC-54 has just shown is not the rendered room** — at α_eff the same corner gives ≈5.35 m Sabine / ≈8.07 m Eyring. Either resolve AC-54 first or write the range explicitly as "nominal α as configured; pending AC-54", and carry the same caveat onto `placement_report.json`'s `below_iso_min_distance` block, which is derived the same way. |
| AC-78 | acoustics-reviewer | minor | OPEN | tests/test_simulator_seam.py:942 | The known-answer test hardcodes ambisonics order 1 while production renders order 3: `configs/base.yaml:46` sets `ambisonics_order: 3` (16 channels, config.py:724) and `gsound_sir._ambisonics_order` derives 3. So `calculate_sh_normalization` is validated only at l=0,1 and the Condon-Shortley phase only at \|m\|=1; a sqrt(2l+1) normalization or phase error at l=2,3 — the failure mode AC-57 names — passes untouched. Reviewer supplied predicted ratios from compiled upstream: +z ACN 2/6/12 = +1.73205/+2.23607/+2.64575; +x ACN 3/6/8/13/15 = -1.73205/-1.11803/+1.93649/+1.62019/-2.09165; +y ACN 1/6/8/9/11 = -1.73205/-1.11803/-1.93649/+2.09165/+1.62019. | One-line change (order 1 → 3) plus the predicted ratios, but it needs the render env this lane pre-declared it would not use; the lane explicitly refused to paste the reviewer's ratios in as fact without running them. Re-review should run it on a render host and assert the order-3 ratios, where the (-1)^\|m\| sign pattern across m=1,2,3 first becomes visible. |
| AC-108 | acoustics-reviewer (lane M) | minor | OPEN | `src/amcd/evaluation/room_acoustic.py:135-137`; `configs/base.yaml:240-245` | A second undeclared departure: because `sosfiltfilt` squares \|H\|^2, the REALIZED band is narrower than an octave. Measured at 48 kHz — 500 Hz band effective −3 dB span 366.2-682.8 Hz (316.6 Hz wide) against nominal 353.6-707.1; ENBW 317.46 Hz vs nominal 353.55 (−10.2 %); 1000 Hz 634.96 vs 707.11 Hz. Under IEC 61260 the −3 dB points belong AT the band edges. It also moves AC-26-R6's arithmetic: BT > 16 against the realized B needs T > 50.40 ms at 500 Hz (25.20 ms at 1000), so the shipped 40.72 ms floor is more permissive than ISO 3382-2 by 9.68 ms, not the 4.54 ms base.yaml states from nominal B = 353.55 Hz. | unfixed — the realized ENBW is declared beside the −6 dB edges. Keep the nominal-B figure in `configs/base.yaml` (it is the standard's own quantity) but the 4.54 ms gap must never be quoted alone; carry the 9.68 ms realized-B figure with it. Re-measure both spans on the merged tree. |
| AC-129 | acoustics-reviewer | major | OPEN | src/amcd/reporting/tables.py (`df.to_csv(report_dir / "metrics_table.csv")`) | THE DISCLOSURE IS HALF-APPLIED INSIDE THE FILE THAT WAS CHANGED. summary.txt gained a `Unit` column; report/metrics_table.csv, written five lines later by the same lane-owned file, still carries `pred_mean`, the CIs, `improvement_mean` and `improvement_mdes` as bare floats across rows mixing s, dB and dB² — 21 columns, zero units. This is the machine-readable twin a paper table would be built from and it needs no cross-lane change. | Add a `unit` column from the same `_unit_for` before the `to_csv`. NOTE: this MOVES report/metrics_table.csv, so the expected effect must be RE-DECLARED in advance, not amended after the fact (the ci_table.csv claim is unaffected). |
| AC-178 | acoustics-reviewer (integration) | major | OPEN | tests/test_metrics.py `_padded_decay`, `TestARoomTooReverberantForItsRecord` | **`_padded_decay` IS NOT THE PRODUCTION RECORD SHAPE AND ITS DOCSTRING'S PREMISE IS FALSE IN THE TESTS THAT USE IT.** (a) Its native support is `2*T60` — a free parameter with no backend meaning; production's is bounded at <= 3.043 s. At T60 >= 2.125 s the helper writes MORE decay than any render can carry, so the T30 recovery claim these tests make rests on an easier signal than production emits: at T60 6.0 s the T30 error moves from -5.41 % (test shape) to **-10.84 %** (production shape) at 500 Hz. (b) The docstring asserts `_fit_to_window` ALWAYS pads — it TRIMS when native > n_samples, and in `TestARoomTooReverberantForItsRecord` BOTH cases have `n_native = 204000`, i.e. **NO PAD AT ALL**, so the class meant to demonstrate the zero-pad regime does not exercise it. As a gsound stand-in it also has no direct arrival, no discrete early reflections, and dense Gaussian statistics where a 5,000-ray render is a sparse histogram. | Rebuild the helper on the production shape: decay written into the backend-declared native support, zero-padded to `ir_duration`, with the support declared in config rather than as `2*T60`. Keep a second construction for the trim branch and say which each test exercises. Correct the docstring's 'ALWAYS pads' claim. |
| AC-181 | acoustics-reviewer (integration) | major | OPEN | src/amcd/evaluation/room_acoustic.py `_find_onset` (the AC-07 ASSUMPTION paragraph) | **AC-07's 'revisit if real renders exhibit occluded-direct geometries' IS ALREADY ACTIVE, AND NOT FROM GEOMETRY.** `_find_onset` thresholds below the GLOBAL peak, assuming the direct sound is loudest. Under AC-180's carrier the direct arrival's realized amplitude carries a random weight and can fall below a later bin. Replica of upstream's late-field shape: onset misses the direct arrival on **3.8 % of delays at DRR 20 dB, 12.0 % at 10 dB, 40.6 % at 0 dB** — and base.yaml's placement support admits d >= r_c, i.e. DRR <= 0 dB. A missed onset shifts t=0 to a reflection, so the 50 ms C50 split, the EDT anchor and the Schroeder start are all referenced to the wrong instant. | Needs a guard, not removal. The scene spec knows the geometric direct delay (|src-rcv|/c), so search for the onset in a window around it and record `(unit, reason)` when the detected onset disagrees. Confirming test on the render host: one render at d ~ r_c, compare `_find_onset` against floor(|src-rcv|/c * fs). |

## readability-reviewer

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| RR-94 | readability-reviewer | major | OPEN | src/amcd/simulators/gsound_sir.py:184 `_WORKER_SRC`; :140 | RR-83 re-derived and still live: a complete 140-line program — four functions, its own docstrings and imports — lives as a raw string inside a module, invisible to linting, type checking and syntax highlighting. | Promote it to a real module the worker imports. The comment at `:140` says promotion is "blocked on that ownership", a lane-ownership constraint that dissolved at merge; it must go with the fix. |

## DEFERRED

| ID | agent | sev | status | anchor | finding | resolution |
|----|-------|-----|--------|--------|---------|------------|
| AC-09 | acoustics-reviewer | major | DEFERRED (gate: E1 report) | src/amcd/scenes/generator.py (placement_report.json) vs RD-29 | `placement_report.json` records volume, Sabine AND Eyring T60, r_c, d/r_c and DRR per split; formulas independently verified. Headline reproduced: train median DRR −5.75 dB vs test_material_shift +4.86 dB at near-identical median distance. | Only the E1 REPORT DISCLOSURE remains, which cannot be written before the report exists.  |
| AC-16 | acoustics-reviewer | minor | DEFERRED (gate: E2 write-up) | src/amcd/representations/spectrogram.py vs configs/simulators/gsound_sir.yaml | The representation resolves 27 third-octave bands over a simulation with only 8 octave bands of spectral freedom: exactly 3 third-octaves per simulated band except the top band, which backs 6, and three bands sit inside the DC-88.7 Hz band. ~22 % of dimensions carry no independent simulated content. | Record-only obligation: ~22 % of representation dimensions carry no independent simulated content. Nothing to implement; it is a disclosure the E2 write-up must carry.  |
| RD-46 | research-director | major | DEFERRED (gate: E1 report) | configs/research_i.yaml `ceiling_absorptive` | The split's absolutes are diagnostic-only. Was OPEN pending AC-25/AC-26. | The user's 2026-08-10 decision was to suppress absolutes below `metric_min_measurable_t60_s`; that threshold is gone, because at this split's Eyring median T60 of 0.059 s it suppressed the MAJORITY of EDT realizations and biased the survivor mean +31.2 %. Censoring an estimator on its own value is a distortion, not a disclosure. The filter-derived floor (~0.010-0.020 s) does not bite, so the numbers ARE now reported — and labelled instead, via the per-split `n_estimator_variance_limited` count. **The change is deliberate and is written into `configs/research_i.yaml` deviation 2.** What remains is E1 REPORT PROSE stating the split is high-variance diagnostic, never physical. |
| F-06 | falsifier | minor | DEFERRED | src/amcd/representations/spectrogram.py (loss); src/amcd/training/loss.py | Dual loss source of truth: `rep.loss(pred,target,delta)` takes a RAW-dB δ; a future caller wiring it against z-scored operands would silently reintroduce F-01. | Belongs to the E2 loss-architecture work (unify `build_criterion` / `rep.loss`). Mitigation in place: docstring states δ must be operand-domain; the sole caller already scales it. |
| RD-12 | research-director | minor | DEFERRED (gate: E4) | report/stats ray-budget axis label | The swept variable is the DIFFUSE ray budget with specular fixed; "reduce ray count" could be read as a total. | Only the E4 report axis label remains, and there is no axis to label until E4. |
| RD-14 | research-director | minor | DEFERRED (gate: Step 4) | render QC gate | Per-scene QC that raises on first failure aborts a full batch — costly under emulation. | Persist ALL renders first, write `renders/qc_failures.csv`, fail loudly after the batch. Design commitment for code that does not exist yet. |
| RD-15 | research-director | minor | DEFERRED | scripts/setup_gsound_sir.py (version selection) | Auto-fetch/auto-switch of GSound-SIR versions, side-by-side multi-version envs — wanted eventually, not needed for this gate. | The ref-addressable installer + config `commit_sha` pin + runtime installed==pinned verification already let the researcher choose any version; automation belongs to a later tooling pass. |
| RD-23 | research-director | minor | DEFERRED (gate: E4) | render realization variance | pygsound exposes no RNG seed, so ONE render per (scene, budget) conflates Monte-Carlo realization variance with the budget effect — and MC variance at low budgets IS the phenomenon under study. | E4 needs ≥N realizations per (scene, budget) or an explicit argument that between-scene variance dominates. Requirement on THIS gate: the render artifact layout must not foreclose adding a realization index. |
| RD-27 | research-director | major | DEFERRED (gate: E1 report) | `test_geometry_shift` comparability | RI's `test_geometry_shift` is DUAL-axis (corridor AND near_wall+far_pair), which violates invariant #10. Reproducing it verbatim imports RI's confound; deviating makes v3's split NOT comparable to RI's 0.61 baseline rel-L2. | ARBITRATED: keep invariant #10. The config-side disclosure is written into `configs/research_i.yaml`; what remains is E1 REPORT PROSE — state the non-comparability per split and relabel the split "v3-corrected single-axis geometry shift". |
| RD-28 | research-director | major | DEFERRED (gate: per-surface materials / E1 report) | material-shift comparability | RI's material shift is "ceiling_absorptive AND asymmetric_walls"; `asymmetric_walls` needs per-surface absorption, which a scalar `material_absorption` and `createbox(absorp)` cannot express. **Sign corrected:** the scalar α applies to ALL SIX SURFACES, giving Sabine T60 0.117/0.161/0.209 s vs an area-weighted ceiling-only reading's 0.42/0.51/0.65 s — 3-4× STRONGER, not weaker. | Implementation deferred to a per-surface-materials gate; the disclosure is written into `configs/research_i.yaml` and remains an E1 report obligation. **CONTINGENCY:** gsound's `createbox(absorp)` takes scalar-or-per-band and NOT per-surface (confirmed, `docs/gsound_sir_setup.md` §4), so if per-surface proves unreachable this decision reopens — itself an argument for an eventual custom simulator. |
| RD-29 | research-director | major | DEFERRED (gate: E1 report) | placement distance distribution | RI never gives `mid_pair`/`far_pair` source-receiver distance sub-ranges numerically. Substituting the global 1.0-10.0 m range changes the distance distribution of five of six splits, and distance sets DRR → C50/D50/EDT. | Do not invent sub-ranges. The quantification exists (`placement_report.json` records the realized per-split distribution, AC-09 confirmed); what remains is the E1 report stating exactly what was substituted. See also AC-30. |
| RD-38 | research-director | minor | DEFERRED (gate: cross-model comparison / multiple raytracers) | src/amcd/config.py role grammar | The role grammar cannot express a sweep over a plugin NAME (`model.name`, `simulator.name`): `_attach_params_block` runs before `_resolve_roles` and requires a concrete name. The roadmap wants both cross-model comparison and multiple raytracers. | Recorded so it is discovered now rather than at E4. |
| RD-52 | research-director | minor | DEFERRED (gate: Step 6) | ray-budget probe artifact | AC-17's fix is exercised only by a synthetic known-answer test, because the defect is inert under dry_run by construction. Its first exercise on real data would otherwise be the expensive emulated render it was written to protect. | Add to the RD-17 probe artifact: per-leg vs shared truncation indices on REAL IRs, plus T30/C50 computed both ways. Costs nothing extra. See also AC-34, which needs the same evidence. |
| AC-19-value | acoustics-reviewer / research-director | minor | DEFERRED (gate: E2) | configs/representations/spectrogram.yaml (`min_bins_per_band` VALUE) | AC-19's MECHANISM closed in cycle 2 (config-declared ladder, recorded band description, measured in-band fractions). The VALUE is shipped as 1 — today's behaviour — because raising it is a research decision with real cost: at production framing 3 bins drops every band below ~315 Hz, i.e. all low-frequency coverage. Measured basis: the five lowest bands hold ONE FFT bin each and a 63 Hz tone peaks in the WRONG band (58.7 % in the 78.7 Hz band vs 35.5 % in 49.6 Hz). | Blast radius is bounded: reported ISO-3382 metrics are 500/1000 Hz octave-band quantities computed from DECODED WAVEFORMS, not from these bands, so this cannot corrupt an E1 headline number — it affects the learned representation and low-frequency reconstruction. The roadmap fix is **multi-resolution sampling** (paper §6); merge-or-reframe the low bands is preferred, dropping them is the fallback. Decide at E2 with `preprocessed/meta.json`'s recorded in-band fractions in hand. |

## FINDING CLUSTERS — what is actually one defect

**Close a cluster whole, or not at all.** A cluster closed halfway is a fix applied
while the thing that made it visible is still missing.

Clusters whose members have all been fixed are gone; a cluster that fell to one
member has been folded back into that row, because a "cluster" of one enforces
nothing.

| # | cluster | rows | what it really is |
|---|---|---|---|
 Two of three routes are closed: `diagnostics` is fingerprinted and chained, and the verdict now requires `d0b_min_scored_frac` coverage before it will clear a split. The third remains and alone still leaves "CARRIER CEILING CLEARS" untrustworthy: the residual is decidable by band composition rather than by acoustics. |
| **C5** | budget-dependent ISO absolutes | `F-89` `RD-55` | The gain-invariance half is closed: the index is now taken where band energy falls to `peak * eps`, exactly invariant, verified on the retained real renders. What remains is one fix — extrapolated-tail compensation — for the fact that the window still ends where the backend stopped writing, and that point moves with the ray budget. |
| **C6** | the per-band convention | `AC-67` `F-83` | The same domain confusion as air absorption, source-confirmed at the reflection site (AC-67), and the truncation QC flag that cannot fire under `base.yaml` (F-83). The record-length and air-absorption halves of this cluster are closed. |
| **C7** | the reported caveat column | `F-70` `RD-65` | Two caveats that are counted but not bounded, or not surfaced per split: F-70 needs the attempted-population CI beside the scored one, so pred-side censoring is bounded rather than invisible; RD-65 needs the per-split over-limit count carried into the E1 table, which starts in `placement_report.json`. |
| **C11** | d_min disclosure | `AC-50` | The d_min range written into `base.yaml` and `placement_report.json`, and the helper that computes it, which lives in `generator.py` rather than beside the other closed forms in `acoustics.py`. |
